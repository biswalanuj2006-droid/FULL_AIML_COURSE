================================================================================
LEETCODE PATTERN: GRAPHS — 25 PROBLEMS
================================================================================

================================================================================
EASY (10 Problems)
================================================================================

1. NUMBER OF ISLANDS (LC #200)
   def numIslands(grid):
       if not grid: return 0
       count = 0
       for i in range(len(grid)):
           for j in range(len(grid[0])):
               if grid[i][j] == '1':
                   dfs(grid, i, j)
                   count += 1
       return count

   def dfs(grid, i, j):
       if i < 0 or i >= len(grid) or j < 0 or j >= len(grid[0]):
           return
       if grid[i][j] != '1': return
       grid[i][j] = '0'  # mark visited
       dfs(grid, i+1, j); dfs(grid, i-1, j)
       dfs(grid, i, j+1); dfs(grid, i, j-1)

2. FLOOD FILL (LC #733)
3. MAX AREA OF ISLAND (LC #695)
4. CLONE GRAPH (LC #133)
5. PACIFIC ATLANTIC WATER FLOW (LC #417)
6. VERifying ALIEN DICTIONARY (LC #953)
7. TOWN JUDGE (LC #997)
8. FIND CENTER OF STAR GRAPH (LC #1791)
9. MINIMUM NUMBER OF VERTEXES (LC #1557)
10. FIND IF PATH EXISTS (LC #1971)

================================================================================
MEDIUM (10 Problems)
================================================================================

11. COURSE SCHEDULE (LC #207)
    def canFinish(numCourses, prerequisites):
        from collections import deque
        graph = [[] for _ in range(numCourses)]
        in_degree = [0] * numCourses
        for dest, src in prerequisites:
            graph[src].append(dest)
            in_degree[dest] += 1

        queue = deque([i for i in range(numCourses) if in_degree[i] == 0])
        count = 0
        while queue:
            node = queue.popleft()
            count += 1
            for neighbor in graph[node]:
                in_degree[neighbor] -= 1
                if in_degree[neighbor] == 0:
                    queue.append(neighbor)
        return count == numCourses

12. COURSE SCHEDULE II (LC #210)
13. REDUNDANT CONNECTION (LC #684)
14. WORD LADDER (LC #127)
15. GRAPH VALID TREE (LC #261)
16. NUMBER OF PROVINCES (LC #547)
17. IS GRAPH BIPARTITE? (LC #785)
18. ROTTING ORANGES (LC #994)
19. WALLS AND GATES (LC #286)
20. ALL PATHS FROM SOURCE TO TARGET (LC #797)

================================================================================
HARD (5 Problems)
================================================================================

21. ALIEN DICTIONARY (LC #269)
22. WORD LADDER II (LC #126)
23. CRITICAL CONNECTIONS (LC #1192)
24. LONGEST INCREASING PATH IN MATRIX (LC #329)
25. RECONSTRUCT ITINERARY (LC #332)

================================================================================
GRAPH TEMPLATE
===============
from collections import deque, defaultdict

def bfs(graph, start):
    visited = set([start])
    queue = deque([start])
    while queue:
        node = queue.popleft()
        for neighbor in graph[node]:
            if neighbor not in visited:
                visited.add(neighbor)
                queue.append(neighbor)

def dfs(graph, node, visited=None):
    if visited is None: visited = set()
    visited.add(node)
    for neighbor in graph[node]:
        if neighbor not in visited:
            dfs(graph, neighbor, visited)

================================================================================
END OF GRAPHS PATTERN
================================================================================
