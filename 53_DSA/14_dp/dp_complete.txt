================================================================================
DSA MODULE 14: DYNAMIC PROGRAMMING — COMPLETE GUIDE
================================================================================

LEARNING OBJECTIVES
-------------------
After this module you will be able to:
- Recognize DP problems (overlapping subproblems + optimal substructure)
- Convert recursion to memoization to tabulation
- Solve major DP patterns (1D, 2D, knapsack, interval, tree DP)
- Solve 20+ LeetCode DP problems

================================================================================
1. WHAT IS DYNAMIC PROGRAMMING?
================================================================================

INTUITION
---------
DP = Recursion + Memoization (or bottom-up tabulation)

A problem has DP if:
1. OPTIMAL SUBSTRUCTURE: optimal solution contains optimal sub-solutions
2. OVERLAPPING SUBPROBLEMS: same sub-problems solved multiple times

CLASSIC EXAMPLE: Fibonacci
----------------------------
# Recursion: O(2ⁿ) time — exponential!
def fib(n):
    if n <= 1: return n
    return fib(n-1) + fib(n-2)

# Memoization: O(n) time, O(n) space
def fib_memo(n, memo={}):
    if n in memo: return memo[n]
    if n <= 1: return n
    memo[n] = fib_memo(n-1, memo) + fib_memo(n-2, memo)
    return memo[n]

# Tabulation: O(n) time, O(n) space
def fib_tab(n):
    if n <= 1: return n
    dp = [0] * (n + 1)
    dp[1] = 1
    for i in range(2, n + 1):
        dp[i] = dp[i-1] + dp[i-2]
    return dp[n]

# Space optimized: O(n) time, O(1) space
def fib_opt(n):
    if n <= 1: return n
    a, b = 0, 1
    for _ in range(2, n + 1):
        a, b = b, a + b
    return b

================================================================================
2. DP APPROACH
================================================================================

STEP-BY-STEP
-------------
1. Define the STATE: what does dp[i] represent?
2. Define the TRANSITION: how to compute dp[i] from smaller states
3. Define the BASE CASE: simplest sub-problem
4. Determine the ORDER: compute smaller states first
5. Extract the ANSWER: what is the final answer?

================================================================================
3. 1D DP PROBLEMS
================================================================================

CLIMBING STAIRS
----------------
# dp[i] = number of ways to reach step i
# dp[i] = dp[i-1] + dp[i-2]
def climb_stairs(n):
    if n <= 2: return n
    a, b = 1, 2
    for i in range(3, n + 1):
        a, b = b, a + b
    return b

HOUSE ROBBER
-------------
# dp[i] = max money robbing houses 0..i
# dp[i] = max(dp[i-1], dp[i-2] + nums[i])
def rob(nums):
    if len(nums) <= 2: return max(nums)
    a, b = nums[0], max(nums[0], nums[1])
    for i in range(2, len(nums)):
        a, b = b, max(b, a + nums[i])
    return b

COIN CHANGE
------------
# dp[i] = min coins to make amount i
# dp[i] = min(dp[i - coin] + 1) for each coin
def coin_change(coins, amount):
    dp = [float('inf')] * (amount + 1)
    dp[0] = 0
    for i in range(1, amount + 1):
        for coin in coins:
            if coin <= i:
                dp[i] = min(dp[i], dp[i - coin] + 1)
    return dp[amount] if dp[amount] != float('inf') else -1

LONGEST INCREASING SUBSEQUENCE (LIS)
--------------------------------------
# dp[i] = length of LIS ending at index i
def length_of_LIS(nums):
    n = len(nums)
    dp = [1] * n
    for i in range(1, n):
        for j in range(i):
            if nums[j] < nums[i]:
                dp[i] = max(dp[i], dp[j] + 1)
    return max(dp)

================================================================================
4. 2D DP PROBLEMS
================================================================================

UNIQUE PATHS
-------------
# dp[i][j] = number of ways to reach (i, j)
# dp[i][j] = dp[i-1][j] + dp[i][j-1]
def unique_paths(m, n):
    dp = [[1] * n for _ in range(m)]
    for i in range(1, m):
        for j in range(1, n):
            dp[i][j] = dp[i-1][j] + dp[i][j-1]
    return dp[m-1][n-1]

EDIT DISTANCE
--------------
# dp[i][j] = min operations to convert word1[0..i] to word2[0..j]
def min_distance(word1, word2):
    m, n = len(word1), len(word2)
    dp = [[0] * (n + 1) for _ in range(m + 1)]

    for i in range(m + 1): dp[i][0] = i
    for j in range(n + 1): dp[0][j] = j

    for i in range(1, m + 1):
        for j in range(1, n + 1):
            if word1[i-1] == word2[j-1]:
                dp[i][j] = dp[i-1][j-1]
            else:
                dp[i][j] = 1 + min(
                    dp[i-1][j],    # delete
                    dp[i][j-1],    # insert
                    dp[i-1][j-1]   # replace
                )
    return dp[m][n]

================================================================================
5. KNAPSACK PATTERNS
================================================================================

0/1 KNAPSCAK (each item used at most once)
-------------------------------------------
def knapsack(weights, values, capacity):
    n = len(weights)
    dp = [[0] * (capacity + 1) for _ in range(n + 1)]

    for i in range(1, n + 1):
        for w in range(capacity + 1):
            dp[i][w] = dp[i-1][w]  # don't take item i
            if weights[i-1] <= w:
                dp[i][w] = max(dp[i][w],
                    dp[i-1][w - weights[i-1]] + values[i-1])

    return dp[n][capacity]

UNBOUNDED KNAPSACK (items can be used unlimited times)
-------------------------------------------------------
def unbounded_knapsack(weights, values, capacity):
    dp = [0] * (capacity + 1)
    for w in range(1, capacity + 1):
        for i in range(len(weights)):
            if weights[i] <= w:
                dp[w] = max(dp[w], dp[w - weights[i]] + values[i])
    return dp[capacity]

================================================================================
6. LEETCODE PROBLEMS
================================================================================

EASY
----
1. Climbing Stairs (LC #70)
2. Min Cost Climbing Stairs (LC #746)
3. Best Time to Buy/Sell Stock (LC #121)
4. Maximum Subarray (LC #53)
5. Range Sum Query 2D (LC #304)

MEDIUM
------
6. Coin Change (LC #322)
7. Longest Increasing Subsequence (LC #300)
8. Word Break (LC #139)
9. Unique Paths (LC #62)
10. Longest Common Subsequence (LC #1143)

HARD
----
11. Edit Distance (LC #72)
12. Burst Balloons (LC #312)
13. Regular Expression Matching (LC #10)

================================================================================
DP PROBLEM-SOLVING CHECKLIST
=============================
1. Can I define the state clearly?
2. What is the base case?
3. What is the recurrence relation?
4. What order should I fill the DP table?
5. Can I optimize space?

================================================================================
END OF MODULE 14
================================================================================
