# SORTING — PRACTICE SET
# 30 Questions + Coding + Debugging

---

## EASY (8 Questions)

1. What is the time complexity of bubble sort?
   Answer: O(n²) average and worst, O(n) best (already sorted)

2. What is the time complexity of merge sort?
   Answer: O(n log n) in all cases

3. What is the time complexity of quicksort?
   Answer: O(n log n) average, O(n²) worst case

4. What is the time complexity of insertion sort?
   Answer: O(n²) average and worst, O(n) best

5. What is a stable sorting algorithm?
   Answer: One that preserves the relative order of equal elements

6. Which sorts are stable?
   Answer: Bubble sort, insertion sort, merge sort, counting sort, radix sort

7. What is the space complexity of merge sort?
   Answer: O(n) — requires auxiliary array

8. What is the space complexity of quicksort?
   Answer: O(log n) average (recursion stack)

---

## MEDIUM (12 Questions)

9. Implement merge sort.
    ```python
    def merge_sort(arr):
        if len(arr) <= 1: return arr
        mid = len(arr) // 2
        left = merge_sort(arr[:mid])
        right = merge_sort(arr[mid:])
        return merge(left, right)
    def merge(left, right):
        result = []
        i = j = 0
        while i < len(left) and j < len(right):
            if left[i] <= right[j]:
                result.append(left[i]); i += 1
            else:
                result.append(right[j]); j += 1
        result.extend(left[i:])
        result.extend(right[j:])
        return result
    ```

10. Implement quicksort.
    ```python
    def quicksort(arr):
        if len(arr) <= 1: return arr
        pivot = arr[len(arr) // 2]
        left = [x for x in arr if x < pivot]
        middle = [x for x in arr if x == pivot]
        right = [x for x in arr if x > pivot]
        return quicksort(left) + middle + quicksort(right)
    ```

11. Implement heap sort.
    ```python
    def heap_sort(arr):
        import heapq
        heapq.heapify(arr)
        return [heapq.heappop(arr) for _ in range(len(arr))]
    ```

12. Sort an array of 0s, 1s, and 2s (Dutch National Flag).

13. Find the kth largest element using quickselect.

14. Merge two sorted arrays.

15. Sort colors.

16. Relative sort array.

17. Minimum absolute difference.

18. Height checker.

19. Sort array by parity.

20. Wedge sort (sort even and odd indices separately).

---

## HARD (5 Questions)

21. Implement external sort for large files.
22. Sort a nearly sorted array in O(n log k).
23. Counting sort implementation.
24. Radix sort implementation.
25. Bucket sort implementation.

---

## DEBUGGING (5 Problems)

```python
# Debug 1: Quicksort worst case pivot
def quicksort(arr):
    if len(arr) <= 1: return arr
    pivot = arr[0]  # Bug: always first element → O(n²) on sorted input
    left = [x for x in arr[1:] if x <= pivot]
    right = [x for x in arr[1:] if x > pivot]
    return quicksort(left) + [pivot] + quicksort(right)
# Fix: Use random pivot or median-of-three

# Debug 2: Merge sort modifies original array
def merge_sort(arr):
    if len(arr) <= 1: return
    mid = len(arr) // 2
    left = arr[:mid]
    right = arr[mid:]
    merge_sort(left)  # Bug: creates new arrays but merge puts back
    merge_sort(right)
    # Original array not modified — this is fine for return-based version
    # But in-place version needs careful handling

# Debug 3: Bubble sort missing optimization
def bubble_sort(arr):
    n = len(arr)
    for i in range(n):
        for j in range(n - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
# No early termination — wastes time on nearly sorted arrays
# Fix: Add swapped flag: if not swapped: break

# Debug 4: Insertion sort off-by-one
def insertion_sort(arr):
    for i in range(1, len(arr)):
        key = arr[i]
        j = i - 1
        while j >= 0 and arr[j] > key:
            arr[j + 1] = arr[j]
            j -= 1
        arr[j + 1] = key  # Correct — inserts at correct position
    return arr

# Debug 5: Python's sort vs sorted
arr = [3, 1, 4, 1, 5]
arr.sort()  # In-place, returns None
result = arr.sort()  # Bug: result is None!
# Fix: Use sorted(arr) for a new list, or arr.sort() then use arr
```
