================================================================================
          AI ENGINEERING ULTRA-MASSIVE QUESTION BANK  -  QUESTIONS_BANK.txt
================================================================================
Scope      : Python -> NumPy/Pandas -> math/statistics -> data science ->
             ML -> DL -> PyTorch -> CNN/NLP -> Transformers -> LLMs ->
             fine-tuning/quantization -> embeddings/IR -> RAG -> vector DBs
             -> LangChain -> LangGraph -> AI agents -> multi-agent ->
             multimodal -> MLOps -> production AI -> security -> evaluation
             -> observability -> research -> system design.
Levels     : L0 fundamental -> L1 beginner -> L2 intermediate ->
             L3 advanced -> L4 interview -> L5 senior -> L6 expert ->
             L7 research -> L8 extreme.
Types      : THEORY / WHY / HOW / COMPARISON / MATH / CODING / DEBUGGING /
             CODE-REVIEW / INTERVIEW / SCENARIO / SYSTEM-DESIGN / PROJECT /
             RESEARCH / RAPID-FIRE / WHITEBOARD / INCIDENT / CAPSTONE.
ID scheme  : <DOMAIN>-<TYPE>-<NNNN>, e.g. ML-TH-0001, DL-CODE-0012,
             RAG-DEBUG-0007, LC-TH-0003, LG-SYS-0002, AGENT-PROJ-0001.
Difficulty : target mix ~15% beginner, 20% intermediate, 25% advanced,
             20% interview, 10% senior/expert, 10% research/extreme.
Rules      : prefer WHY/HOW/WHEN/WHAT-BREAKS over pure recall; every
             significant module has coding, debugging, projects and
             system-design questions; do not fabricate APIs - LangChain/
             LangGraph questions use the modern 1.x concept set.
Companions : ml_course/INTERVIEW_BANK.md (500 Q&A), COURSE.txt files in
             llm_course / genai_agents_course / ml_course,
             AI_ENGINEERING/DEEP_LEARNING/, verify_course.py.

--------------------------------------------------------------------------------
TABLE OF CONTENTS
--------------------------------------------------------------------------------
  01 Python for AI              17 Fine-Tuning / PEFT       33 MLOps
  02 NumPy                      18 Quantization / Inference 34 Production AI
  03 Pandas                     19 Embeddings               35 Cloud AI
  04 Mathematics                20 Information Retrieval    36 AI Security
  05 Data Science               21 RAG                      37 AI Evaluation
  06 Machine Learning           22 Vector Databases         38 AI Observability
  07 ML Evaluation              23 RAG Evaluation           39 AI Research
  08 Feature Engineering        24 LangChain                40 AI System Design
  09 Deep Learning              25 LangGraph                41 Production Incidents
  10 PyTorch                    26 AI Agents                42 Master Interview (500)
  11 CNN / Computer Vision      27 Multi-Agent Systems      43 Rapid Fire (500)
  12 Sequence Models            28 Multimodal AI            44 Whiteboard
  13 NLP                        29 Computer Vision          45 Master Capstone
  14 Transformers               30 Generative Models        46 Final Master Exam
  15 LLMs                       31 Recommender Systems      +47 Data Leakage
  16 LLM Engineering            32 Time Series              +48 Failure Analysis

================================================================================
DOMAIN 01 - PYTHON FOR AI
================================================================================
THEORY / CONCEPTUAL
  PY-TH-0001  What is the difference between a list and a tuple? When is each
              the right choice in an ML codebase?
  PY-TH-0002  What is a generator, and why does it matter for large datasets
              (e.g. streaming training batches)?
  PY-TH-0003  What is a decorator? Give an AI use case (e.g. cache a model call,
              time a pipeline step, retry an API call).
  PY-TH-0004  What does the GIL limit? Which workloads in ML are unaffected and
              which are bottlenecked by it?
  PY-TH-0005  What is the difference between shallow and deep copies? Show the
              failure mode of copying a PyTorch model's state_dict this way.
  PY-TH-0006  What is a context manager? Name three AI/ML uses (file handles,
              torch.no_grad, lock acquisition).
  PY-TH-0007  What is the difference between an exception and a return-error
              code? Which is better for a training pipeline?
  PY-TH-0008  What are `__init__.py`, `__name__ == "__main__"`, and sys.path?
              Why do ML repos break when modules move?
  PY-TH-0009  What is the difference between threading, asyncio, and
              multiprocessing? Which fits GPU training, which fits I/O-bound
              FastAPI endpoints, which fits CPU-bound preprocessing?
  PY-TH-0010  What is a dataclass, and why is it used for configs, datapoints,
              and experiment metadata?
  PY-TH-0011  Why is `dict` insertion-ordered? Why does it matter for
              reproducible feature dicts?
  PY-TH-0012  What is the difference between `is` and `==`? Show an ML
              footgun (comparing numpy arrays or pandas values).
  PY-TH-0013  What is monkey patching, and why is it useful in tests (e.g.
              mocking an LLM client)?
  PY-TH-0014  What is a closure? Give a use case in a data pipeline.
  PY-TH-0015  What is pickling, and why can it be unsafe or version-fragile
              for model artifacts?
WHY QUESTIONS
  PY-TH-0016  Why do ML projects pin dependency versions and use virtual
              environments / lockfiles?
  PY-TH-0017  Why does eager loading of a 50 GB dataset crash a laptop, and
              what does lazy/streamed loading change?
  PY-TH-0018  Why is logging better than print in a training run that runs
              unattended for 12 hours?
  PY-TH-0019  Why do libraries return views where you expect copies (numpy
              slicing, pandas)? Where does this corrupt training data silently?
  PY-TH-0020  Why should a config live outside the code (yaml/env vars) rather
              than as constants in a training script?
COMPARISON
  PY-TH-0021  list vs dict vs set vs tuple: lookup cost, ordering, mutability.
  PY-TH-0022  `yield` vs returning a list: memory, laziness, pipelineability.
  PY-TH-0023  thread vs process vs asyncio task: which one is right for a
              DataLoader worker, a web request, a GPU kernel launch.
  PY-TH-0024  pydantic vs dataclass vs plain dict for LLM structured output.
  PY-TH-0025  `torch.no_grad` vs `torch.inference_mode` vs `detach()`.
CODING
  PY-CODE-0001  [L1] Write a generator that yields fixed-size windows of a
                token stream with stride.
  PY-CODE-0002  [L2] Implement a retry decorator with exponential backoff and
                jitter for an unreliable LLM API.
  PY-CODE-0003  [L2] Write a context manager that times a block and logs the
                duration at INFO level.
  PY-CODE-0004  [L3] Implement a thread-safe LRU cache (OrderedDict +
                lock) for model predictions.
  PY-CODE-0005  [L3] Given a list of dicts, write a function that groups
                them by key and aggregates a numeric field (no pandas).
  PY-CODE-0006  [L4] Implement `chunked(json.load)` streaming parser concept:
                read a large JSONL file and yield parsed records one at a time
                without loading the file.
  PY-CODE-0007  [L4] Write an asyncio task pool that fans out N LLM requests
                with a concurrency limit and collects failures.
  PY-CODE-0008  [L5] Write a lazy sequence wrapper that applies a transform
                to each element of an iterator, with exception context
                (which element failed).
  PY-CODE-0009  [L6] Write a small plugin registry (decorator-based) so
                eval metrics can be added without editing the registry.
  PY-CODE-0010  [L8] Implement a lock-free accumulator pattern using
                multiprocessing.shared_memory for aggregating worker results.
DEBUGGING
  PY-DEBUG-0001  Your training script prints different losses on two machines
                 with the same seed. List the top 5 nondeterminism sources
                 (dict order, hash seeds, GPU atomics, multithreaded loaders,
                 float reductions).
  PY-DEBUG-0002  `model.state_dict()` changes between runs even though you
                 saved and loaded a checkpoint. What happened?
  PY-DEBUG-0003  A generator re-runs from the start every epoch, so the model
                 sees the first batch repeatedly. What is the code smell?
  PY-DEBUG-0004  An API endpoint crashes only under load with
                 `RuntimeError: Event loop is closed`. Diagnosis?
  PY-DEBUG-0005  A cached function returns stale predictions after a model
                 redeploy. What cache key is missing?
  PY-DEBUG-0006  `ResourceWarning: unclosed file` flood in a training loop.
                 Fix pattern?
  PY-DEBUG-0007  Two threads corrupt a shared config dict mid-run. Why did
                 the GIL not protect it?
  PY-DEBUG-0008  A pipeline consumes 3x memory after a refactor from list to
                 list-of-views. Where do views retain the parent array?
CODE REVIEW
  PY-REVIEW-0001  Review: `results = [model(x) for x in loader]` for a 10M
                  example dataset. Issues: memory, no batching, no tqdm, no
                  error isolation.
  PY-REVIEW-0002  Review: a training loop that opens the CSV once per batch
                  inside the loop. Cost + fix.
  PY-REVIEW-0003  Review: `except Exception: pass` around the optimizer step.
  PY-REVIEW-0004  Review: a script that uses `random` for train/test split.
INTERVIEW
  PY-IV-0001  Walk through how you'd structure a reusable training script so
              a new researcher can change model/data/optimizer without
              editing core code.  Follow-up: how does that break when the
              researcher needs a new loss that touches both model and data?
  PY-IV-0002  You have 10 GB of JSONL. How do you process it without running
              out of RAM? Follow-up: the processing is CPU-bound, how do you
              parallelize deterministically?
  PY-IV-0003  Explain the GIL with a whiteboard. What does `multiprocessing`
              give you that threads cannot for CPU work?
  PY-IV-0004  When would you choose asyncio over FastAPI's sync endpoints?
              When would sync be correct?
  PY-IV-0005  What does `__slots__` do and when is it worth it for
              datapoints stored by the million?
PROJECTS
  PY-PROJ-0001  [D] Build a dependency-free CLI that chunks a text corpus into
                BPE-style token windows and streams them to a file.
  PY-PROJ-0002  [C] Build a config-driven experiment runner: yaml config,
                CLI override, seed handling, structured logging, artifact dir.
  PY-PROJ-0003  [B] Build a small task queue with retries, dead-letter queue,
                and progress reporting for batch LLM labeling jobs.
  PY-PROJ-0004  [A] Build a typed, validated dataset pipeline (pydantic
                schemas) with lazy loading, caching, and integrity checks.
RESEARCH
  PY-RES-0001  How would you measure the real cost of Python overhead in a
              PyTorch DataLoader (per-sample vs per-batch)? Design the
              experiment.
  PY-RES-0002  When is it worth rewriting a hot loop in Cython/Numba/CUDA?
              What profiling evidence should decide it?
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Coding [ ] Debugging [ ] Review
                          [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 02 - NUMPY
================================================================================
THEORY / CONCEPTUAL
  NP-TH-0001  What is an ndarray? How do shape, dtype, strides, and the
              memory layout interact?
  NP-TH-0002  What is broadcasting? Give three rules and a 3D example.
  NP-TH-0003  What is the difference between a view and a copy? Name the
              operations that return views (slicing, reshape, transpose) vs
              copies (copy, astype, fancy indexing).
  NP-TH-0004  Why is vectorization faster than Python loops for elementwise
              work? What does "cache-friendly contiguous" mean?
  NP-TH-0005  What are the three ways to write matrix multiplication and how
              do their complexities compare (ijk loops vs BLAS dgemm)?
  NP-TH-0006  What is the numerical-stability trap in softmax (subtracting
              the max)? Show the overflow path.
  NP-TH-0007  What does `np.random.seed` do vs `np.random.default_rng`? Why
              is the Generator API preferred?
  NP-TH-0008  What is a masked array, and when is it better than dropping
              NaN rows?
WHY QUESTIONS
  NP-TH-0009  Why does `np.mean(a)` over an entire array differ slightly from
              a chunked mean? (reduction order / floating point)
  NP-TH-0010  Why is einsum worth learning for ML (batch matmul, attention
              scores, tensor contraction)?
  NP-TH-0011  Why does `a = b.copy()` matter before you modify `a` where
              `b = x.T`?
  NP-TH-0012  Why does changing the axis order of a large array make
              subsequent ops 10x slower? (stride / cache locality)
COMPARISON
  NP-TH-0013  np.dot vs np.matmul vs @ vs einsum.
  NP-TH-0014  np.stack vs np.concatenate vs np.vstack/hstack.
  NP-TH-0015  np.where vs boolean masking vs fancy indexing.
  NP-TH-0016  np.linalg.inv vs np.linalg.solve: which should you never call
              and why?
MATHEMATICAL
  NP-MATH-0001  Given A (3,4) and B (4,5), write the output shape and the
                cost in flops of A@B.
  NP-MATH-0002  Broadcasting: (4,1) + (3,) -> shape? (1,4) * (3,1) -> shape?
  NP-MATH-0003  Compute the cosine similarity matrix of X (n,d) in one
                vectorized expression (no loops).
  NP-MATH-0004  Implement a numerically stable softmax over axis=-1 in numpy
                and explain each step.
  NP-MATH-0005  Write the vectorized cross-entropy: given logits (B,C) and
                targets (B,), compute the mean loss without loops.
  NP-MATH-0006  Derive the vectorized update for gradient descent on linear
                regression: w -= lr * X.T @ (X @ w - y) / n. What shapes does
                each term have?
  NP-MATH-0007  Write a vectorized row-wise L2 normalization.
  NP-MATH-0008  Given covariance matrix C (d,d), compute the whitening
                transform via eigendecomposition.
  NP-MATH-0009  Vectorize: for each row, subtract its own mean and divide by
                its own std (z-score) - no loops.
  NP-MATH-0010  Write the O(T^2) scaled-dot-product attention
                (Q,K,V each (T,d)) as three numpy ops: scores, softmax, out.
CODING
  NP-CODE-0001  [L1] Generate a (1000, 8) standard-normal matrix; compute
                per-column mean/std without loops.
  NP-CODE-0002  [L2] Implement a sliding-window mean (moving average) with
                cumsum in O(n).
  NP-CODE-0003  [L3] Implement k-means assignment + centroid update in pure
                numpy (vectorized distance matrix).
  NP-CODE-0004  [L4] Implement batched pairwise cosine similarity for
                (B,d) x (C,d) in one einsum + normalization.
  NP-CODE-0005  [L4] Implement a numpy softmax-with-temperature for a logits
                vector and return the sampled token index (numpy random).
  NP-CODE-0006  [L5] Implement truncated SVD-based dense embeddings: build a
                PPMI matrix from co-occurrence counts and reduce with SVD.
  NP-CODE-0007  [L6] Implement a fused forward pass of a 2-layer MLP
                (linear + relu + linear + softmax) purely in numpy, matching
                torch outputs to 1e-6.
  NP-CODE-0008  [L6] Vectorize top-k sampling: given probabilities (B,V),
                return indices sampled from the top-k per row, batched.
  NP-CODE-0009  [L7] Implement strided batched matmul via einsum with
                explicit output shape, then benchmark vs @.
  NP-CODE-0010  [L8] Implement an O(T^2) causal-masked attention forward +
                backward (grads for Q,K,V) in numpy and gradcheck it against
                torch.autograd.
DEBUGGING
  NP-DEBUG-0001  Broadcasting error: (3,4) + (4,3) - what went wrong and what
                 was the intent?
  NP-DEBUG-0002  Your cosine-similarity matrix has NaN rows after
                 normalization. Root cause and fix.
  NP-DEBUG-0003  MemoryError building an (n x n) distance matrix for n=200k.
                 What is the memory cost, and what alternatives exist
                 (chunking, approximate)?
  NP-DEBUG-0004  After `x = arr[np.random.permutation(n)]`, `arr` still
                 changes. Why?
  NP-DEBUG-0005  softmax overflows to inf in fp32 at logit 90. Show the fix
                 and the exact numeric path.
  NP-DEBUG-0006  np.linalg.solve gives garbage with "Singular matrix" only
                 sometimes. What data condition is intermittent?
  NP-DEBUG-0007  Your matrix multiply results differ from torch by 1e-5
                 between runs. Acceptable? How would you prove the error
                 source?
CODE REVIEW
  NP-REVIEW-0001  Review: three nested Python loops implementing matrix
                  multiplication on a 2000x2000 matrix.
  NP-REVIEW-0002  Review: `np.log(probs)` where probs contains zeros.
  NP-REVIEW-0003  Review: in-place `x /= x.sum(axis=0)` on a read-only array.
  NP-REVIEW-0004  Review: `for i in range(len(a)): b[i] = a[i] ** 2` - fix
                  with vectorization and explain the speedup.
INTERVIEW
  NP-IV-0001  Explain broadcasting on a whiteboard with (3,1) + (1,4).
  NP-IV-0002  How do you compute a batched softmax without a loop? What is
              the memory complexity?
  NP-IV-0003  You need the top-k indices per row of a (1M, 1000) matrix.
              How do you do it fast and why?
  NP-IV-0004  What is the difference between C-order and F-order, and when
              does it bite in ML?
  NP-IV-0005  When would you use np.einsum over np.matmul, and what does the
              "optimize=True" flag do?
PROJECTS
  NP-PROJ-0001  [D] Numpy-only linear regression with closed form + gradient
                descent, both verified against each other.
  NP-PROJ-0002  [C] Numpy-only logistic regression on a real dataset with
                ROC/PR computation from raw scores.
  NP-PROJ-0003  [B] Numpy-only mini transformer block: attention + FFN +
                layernorm forward, verified against a torch reference.
  NP-PROJ-0004  [A] Numpy implementation of k-means with k-means++ init,
                elbow, and silhouette, benchmarked against sklearn.
RESEARCH
  NP-RES-0001  Design an experiment measuring cache effects: stride sweep
              on a large array, plot bandwidth vs stride, explain the cliff.
  NP-RES-0002  Compare fp32 vs fp64 accumulation error for large dot
              products; where does pairwise summation help?
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research================================================================================
DOMAIN 03 - PANDAS
================================================================================
THEORY / CONCEPTUAL
  PD-TH-0001  Series vs DataFrame: index semantics, alignment, and why
              alignment is a feature AND a footgun.
  PD-TH-0002  What is a MultiIndex, and when is it worth it vs flattening?
  PD-TH-0003  What is the difference between `.loc`, `.iloc`, and chained
              assignment? Which one is the documented silent-corruption trap?
  PD-TH-0004  What is a categorical dtype and why does it save memory on
              high-cardinality labels?
  PD-TH-0005  What does `inplace=True` actually do, and why is it
              discouraged in modern pandas?
  PD-TH-0006  What is the difference between `merge` and `join`? `concat`
              vs `append`?
  PD-TH-0007  How does groupby work internally (split-apply-combine)?
  PD-TH-0008  What is a window/rolling operation, and how is it different
              from groupby?
  PD-TH-0009  What is `apply` vs vectorized ops? When is `apply` justified?
  PD-TH-0010  What are NaNs in pandas vs None vs np.nan? How do you
              distinguish missing from actual zero?
WHY QUESTIONS
  PD-TH-0011  Why does filtering with `df[df.a > 0]` return a copy warning
              in some versions? Why is `df.query` an alternative?
  PD-TH-0012  Why should preprocessing fit/transform use the same column
              order at train and serve time?
  PD-TH-0013  Why is reading a 5 GB CSV with default settings slow, and
              what does dtype=... + low_memory + chunksize change?
  PD-TH-0014  Why does joining two large DataFrames explode memory, and
              what does an index-backed merge change?
COMPARISON
  PD-TH-0015  pandas vs Polars vs raw numpy for a 10 GB feature table.
  PD-TH-0016  groupby.agg(['mean','std']) vs apply(lambda) vs transform.
  PD-TH-0017  merge on columns vs merge on index vs concat axis=1.
  PD-TH-0018  One-hot encoding via pd.get_dummies vs sklearn OneHotEncoder
              (handle_unknown, consistency across train/serve).
MATHEMATICAL
  PD-MATH-0001  Compute group-wise z-scores in one chain: subtract group
              mean, divide by group std - without a loop.
  PD-MATH-0002  Rolling mean of window 7 over a daily series: what does the
              first 6 values look like, and how is min_periods different?
  PD-MATH-0003  Resample daily -> monthly with multiple aggregations in
              one expression.
  PD-MATH-0004  Compute a pivoted mean/std table (rows=category, cols=month)
              in one `pivot_table` call.
  PD-MATH-0005  Shift vs diff vs pct_change: define each and give the
              formula behind it.
CODING
  PD-CODE-0001  [L1] Load a CSV, report shape/dtypes/missing %, and drop
              columns that are >90% missing.
  PD-CODE-0002  [L2] Given logs (user_id, ts, action), compute the per-user
              action count, first/last ts, and action-rate per day.
  PD-CODE-0003  [L3] Build a feature table: merge user table + event table
              on user_id, keeping users with zero events (left join) and
              fill NaN counts with 0.
  PD-CODE-0004  [L4] Implement a time-aware train/validation split that
              never leaks the future (sort by ts, cut at a date).
  PD-CODE-0005  [L5] Write a function that validates schema (dtypes,
              allowed categories, no target leakage columns) before
              training and raises on mismatch.
  PD-CODE-0006  [L6] Stream-process a 20 GB CSV in chunks with per-chunk
              aggregation, then combine - avoid loading the file.
DEBUGGING
  PD-DEBUG-0001  After `df[df.col > 0]`, you later modify a column and get
              SettingWithCopyWarning - trace exactly why.
  PD-DEBUG-0002  `df.merge` returns more rows than both inputs. What join
              semantics produced duplicates?
  PD-DEBUG-0003  Training accuracy is great offline but the served model
              sees strings where the trainer saw categories. Where did
              dtype/encoding drift?
  PD-DEBUG-0004  A groupby result has unexpected NaN for the first group.
              What is the index/level gotcha?
  PD-DEBUG-0005  Rolling('7D') throws on a non-monotonic index. Diagnosis
              and fix.
  PD-DEBUG-0006  Memory blows up building one-hot columns for a
              high-cardinality column (1M uniques). Design the fix.
CODE REVIEW
  PD-REVIEW-0001  Review: `df.apply(lambda r: r.a * r.b, axis=1)` for a
              5M-row frame. What is the right pattern?
  PD-REVIEW-0002  Review: dropping rows with `df.dropna()` before
              train/test split - the split leakage angle.
  PD-REVIEW-0003  Review: one-hot encoding fitted on train+test together.
  PD-REVIEW-0004  Review: `df['y'] = df['target']` inside the feature
              engineering script - what is this called?
INTERVIEW
  PD-IV-0001  How would you detect and fix silent data corruption from
              chained assignment at scale?
  PD-IV-0002  You have 100 GB of CSVs and a 32 GB machine. Walk through
              the full pipeline design (chunking, dtype narrowing,
              parquet, polars alternative).
  PD-IV-0003  Explain when you would NOT use pandas (GPU frames, huge
              data, streaming, production serving).
  PD-IV-0004  How do you guarantee the served features match training
              features byte-for-byte? Follow-up: what tests enforce it?
PROJECTS
  PD-PROJ-0001  [D] EDA report generator: profile a dataset (missing,
                distributions, correlations) into a markdown report.
  PD-PROJ-0002  [C] Leakage-safe feature pipeline: split -> fit
                imputer/encoder inside a pipeline object -> apply to test.
  PD-PROJ-0003  [B] Log-analysis pipeline: parse raw logs, build per-user
                + per-day aggregates, detect drift between week windows.
  PD-PROJ-0004  [A] Schema-validation + profiling gate that runs before
                every training job and fails the job on drift.
RESEARCH
  PD-RES-0001  Design an experiment: how much does dtype narrowing change
              memory and speed on a 1 GB frame? Report both.
  PD-RES-0002  Compare pandas groupby vs dask vs polars on a fixed task;
              what dominates (overhead vs vectorization)?
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 04 - MATHEMATICS
================================================================================
THEORY / CONCEPTUAL
  MT-TH-0001  Why is linear algebra the language of ML? Name three places
              each concept appears (dot product, eigendecomposition, norms).
  MT-TH-0002  What does a vector space, a basis, and linear independence
              mean geometrically? Why does rank matter for regression?
  MT-TH-0003  What is the difference between L1, L2, and L-infinity norms?
              Where does each show up in ML (regularizers, distances)?
  MT-TH-0004  What is the geometric meaning of the dot product (projection,
              cosine)? Why is cosine similarity a scale-invariant similarity?
  MT-TH-0005  What is the chain rule, and why is backpropagation just the
              chain rule on a computational graph?
  MT-TH-0006  What is a gradient? What is the difference between gradient,
              Jacobian, and Hessian, and which one does Adam actually use?
  MT-TH-0007  What does the Central Limit Theorem say, and why does it
              justify assuming Gaussian errors in many models?
  MT-TH-0008  What is a confidence interval vs a prediction interval?
  MT-TH-0009  What is entropy? Cross-entropy? KL divergence? How are the
              three related by one equation?
  MT-TH-0010  What is the difference between correlation and causation in
              one sentence, and one counterexample?
WHY QUESTIONS
  MT-TH-0011  Why does maximizing likelihood equal minimizing cross-entropy
              (or MSE for Gaussian noise)?
  MT-TH-0012  Why do we take logarithms of probabilities everywhere?
  MT-TH-0013  Why is (X^T X)^-1 X^T y dangerous when X is wide or
              collinear, and what does regularization change?
  MT-TH-0014  Why does the softmax over full logits have the same argmax
              as temperature-scaled softmax for any positive temperature?
  MT-TH-0015  Why does a model with high variance fit training perfectly
              but generalize badly? Frame it with bias-variance.
COMPARISON
  MT-TH-0016  Maximum likelihood vs MAP vs full Bayesian: what changes
              about the estimate and the uncertainty.
  MT-TH-0017  Gradient descent vs SGD vs mini-batch: noise, convergence
              behavior, and why mini-batch wins in practice.
  MT-TH-0018  Entropy vs Gini: both measure impurity - where do they
              differ in decision trees?
  MT-TH-0019  Euclidean vs cosine vs Mahalanobis distance: when is each
              the right similarity for embeddings?
  MT-TH-0020  Batch norm vs layer norm vs RMS norm: what is normalized
              over what, and why does that matter for transformers?
MATHEMATICAL
  MT-MATH-0001  Derive the closed-form least squares w = (X^T X)^-1 X^T y
              from the objective ||Xw - y||^2. Show the gradient step.
  MT-MATH-0002  Compute the gradient of MSE: d/dw (1/n)||Xw - y||^2.
  MT-MATH-0003  For softmax logits [2, 1, 0.1], compute the probabilities
              at temperature 1.0 and 2.0 (exact fractions/decimals).
  MT-MATH-0004  Entropy of a fair coin vs a 90/10 coin - compute H(X) for
              both and explain which is larger and why.
  MT-MATH-0005  KL(P||Q) for two Bernoulli distributions p=0.6, q=0.4 -
              compute it and interpret.
  MT-MATH-0006  Given covariance matrix [[1, 0.8], [0.8, 1]], find its
              eigenvalues/eigenvectors and the fraction of variance in PC1.
  MT-MATH-0007  Attention: Q,K,V each (T=3, d=2). Compute scores
              QK^T/sqrt(2), softmax, output for a given numeric input.
  MT-MATH-0008  Perplexity: if cross-entropy loss = 2.3, what is
              perplexity? What loss gives ppl 10?
  MT-MATH-0009  LoRA: W is (1024, 1024), rank r=8. How many trainable
              parameters in B and A, vs full fine-tuning? Ratio?
  MT-MATH-0010  KV cache: model with 32 layers, 32 heads, d_head=128,
              fp16. Bytes per token; size at 4k and 128k context.
  MT-MATH-0011  Compute the parameter count of a transformer block given
              d_model, ff dimension, heads, and vocab.
  MT-MATH-0012  Self-attention complexity: prove O(T^2 d) flops and
              O(T^2 + Td) memory for T tokens.
  MT-MATH-0013  Gradient of cross-entropy: show dL/dz = softmax(z) - y
              for one-hot y (the famous cancellation).
  MT-MATH-0014  Bayes: P(positive test | disease) = 0.99, P(test | no
              disease) = 0.05, prevalence 1%. Compute P(disease | positive).
  MT-MATH-0015  Variance of a sample mean: prove Var(mean) = sigma^2/n
              for i.i.d. samples, and what n must be to halve the SE.
CODING
  MT-CODE-0001  [L2] Implement entropy and Gini from a count vector.
  MT-CODE-0002  [L3] Implement gradient descent for w on ||Xw - y||^2 with
              exact gradient and a learning-rate schedule.
  MT-CODE-0003  [L4] Implement KL divergence between two categorical
              distributions with a NaN guard for zero probs.
  MT-CODE-0004  [L4] Implement PCA via eigendecomposition of the covariance
              matrix and verify against np.linalg.svd.
  MT-CODE-0005  [L5] Implement a numerically stable softmax + cross-entropy
              forward/backward and gradcheck.
  MT-CODE-0006  [L6] Implement AdamW update by hand (no torch.optim) and
              match torch's closed form on a random problem.
DEBUGGING
  MT-DEBUG-0001  Your model's loss is NaN after 3 steps. List the numeric
              suspects in order (LR, log(0), overflow, init scale).
  MT-DEBUG-0002  The closed-form regression returns huge weights - rank
              deficiency or collinearity? How do you confirm numerically?
  MT-DEBUG-0003  Two implementations of the same metric differ by 1e-8.
              How do you find which one is right?
  MT-DEBUG-0004  Variance of predictions ~0 for one class. Is this a
              degenerate softmax or a data problem? Test design.
  MT-DEBUG-0005  KL divergence is negative in your implementation. What
              invariant did you break (clamping, log-domain)?
INTERVIEW
  MT-IV-0001  Derive the gradient of MSE w.r.t. w on a whiteboard.
  MT-IV-0002  Why do we use log-likelihood instead of likelihood? Follow-up:
              prove it for Bernoulli.
  MT-IV-0003  What is the bias-variance decomposition, and what does it
              say about what adding data fixes vs adding capacity fixes?
  MT-IV-0004  Explain eigenvalues/eigenvectors to a product manager, then
              to a researcher, then implement PCA's top-k projection.
  MT-IV-0005  What is the difference between optimizing the loss and
              optimizing the metric you report? Give an example.
PROJECTS
  MT-PROJ-0001  [D] From-scratch linear regression + diagnostics (residual
                plot, R2, condition number of X).
  MT-PROJ-0002  [C] From-scratch logistic regression trained by gradient
                descent with cross-entropy, verified against sklearn.
  MT-PROJ-0003  [B] A small auto-diff engine (forward/backward graph) that
                differentiates a two-layer net - the skeleton of PyTorch.
  MT-PROJ-0004  [A] A numeric experiment on bias-variance: polynomial
                degree sweep with train/test error curves + bias-variance
                decomposition estimate.
RESEARCH
  MT-RES-0001  Compare your from-scratch AdamW vs torch's on 5 seeds;
              design the acceptance criterion for 'match'.
  MT-RES-0002  Empirically measure how LLM perplexity behaves on
              out-of-distribution text vs in-distribution: hypothesis and
              test design.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 05 - DATA SCIENCE (DATA QUALITY + EDA + PREPROCESSING)
================================================================================
THEORY / CONCEPTUAL
  DS-TH-0001  What is the full data lifecycle, and where do most projects
              actually fail? (data quality > modeling)
  DS-TH-0002  What is target leakage? Give five distinct types (target,
              preprocessing, temporal, duplicate, feature).
  DS-TH-0003  What is EDA, and what questions must it answer before
              modeling (distributions, missingness, relationships,
              anomalies, leakage hints)?
  DS-TH-0004  What is sampling bias? How does a biased training set look
              identical to a clean one at train time?
  DS-TH-0005  What is label noise vs feature noise? Which is harder to fix?
  DS-TH-0006  What is the difference between imputation, interpolation,
              and deletion for missing data, and when is each defensible?
  DS-TH-0007  What is distribution shift, and how is it different from
              concept drift? Give one example of each.
  DS-TH-0008  What is a train/validation/test split, and why do we need
              three sets (not two)?
  DS-TH-0009  What is an outlier - statistical vs domain-meaningful? Why
              must outliers be handled in context, not blindly dropped?
  DS-TH-0010  What is the difference between standardization and
              normalization, and which trees vs linear models care about?
WHY QUESTIONS
  DS-TH-0011  Why is more data not always better (duplicates, drift,
              leakage in the new data)?
  DS-TH-0012  Why does a 99% accurate model on 99:1 data mean nothing?
  DS-TH-0013  Why should preprocessing (scaling/encoding) be fit only on
              the train fold? Show the test-set contamination path.
  DS-TH-0014  Why is time-series CV (walk-forward) different from K-fold?
              What would random K-fold leak?
  DS-TH-0015  Why do we report error analysis (which classes fail) instead
              of just aggregate accuracy?
COMPARISON
  DS-TH-0016  Mean vs median imputation: bias vs robustness trade-off.
  DS-TH-0017  One-hot vs ordinal vs target encoding: when is target
              encoding valid (and how is it leaked)?
  DS-TH-0018  Random K-fold vs stratified vs grouped vs time-series split.
  DS-TH-0019  SMOTE vs class weights vs threshold tuning for imbalance.
  DS-TH-0020  Train-test split at random vs by time: when must it be time?
MATHEMATICAL
  DS-MATH-0001  Compute the class-weight ratio for a 99:1 dataset and
              derive what balanced accuracy changes.
  DS-MATH-0002  Z-score formula and its sensitivity to outliers vs
              median/IQR robust scaling.
  DS-MATH-0003  Show how one-hot encoding increases feature count and what
              that does to a linear model's effective capacity.
  DS-MATH-0004  Given a 80/10/10 split of n samples, how many samples are
              in each set? How many validation samples does n=1000 give?
  DS-MATH-0005  Compute information gain of a split given class counts
              before/after (use entropy).
CODING
  DS-CODE-0001  [L2] Write a leakage audit: given a feature list, flag
              columns that are functions of the target (heuristic + manual).
  DS-CODE-0002  [L3] Build a preprocessing pipeline (impute + scale +
              encode) with sklearn Pipeline/ColumnTransformer, fit on train
              only, transform test only - and prove no test fit happens.
  DS-CODE-0003  [L4] Implement walk-forward CV that never trains on the
              future, with an expanding window.
  DS-CODE-0004  [L5] Write an EDA report generator: per-column missing %,
              distribution stats, correlation heatmap, target profile -
              output markdown.
  DS-CODE-0005  [L6] Implement a data-quality gate that fails a training
              job on: new categories, drift in distributions, target
              leakage columns, or duplicate rows above a threshold.
DEBUGGING
  DS-DEBUG-0001  Model is 0.99 AUC offline, 0.55 in production. Rank the
              likely causes (leakage, drift, sampling, threshold).
  DS-DEBUG-0002  Accuracy 95% but F1 for the minority class is 0.03. What
              does the confusion matrix look like?
  DS-DEBUG-0003  Train and test distributions differ visibly on one
              feature. Where did the pipeline leak the fix?
  DS-DEBUG-0004  After adding a 'customer_lifetime_value' feature, offline
              metrics jump 15 points. What is the most likely explanation?
  DS-DEBUG-0005  Duplicate rows inflate CV scores: how do you find
              near-duplicates (not exact) across train/test?
  DS-DEBUG-0006  The model trained in January predicts June fine, but the
              served data is now July and quality drops. What drift tests
              do you run first?
CODE REVIEW
  DS-REVIEW-0001  Review: `StandardScaler().fit_transform(df)` called on
              the whole dataset before the split.
  DS-REVIEW-0002  Review: dropping rows with missing target, then
              imputing features with the full-data mean.
  DS-REVIEW-0003  Review: a test set that contains rows the model trained
              on (duplicate user IDs across split).
  DS-REVIEW-0004  Review: SMOTE applied before the split, then CV - what
              leaks?
INTERVIEW
  DS-IV-0001  Your model performs great offline and fails online. Walk
              through the investigation in order.
  DS-IV-0002  How do you split data for a time-based model? Follow-up:
              what about a model with grouped users?
  DS-IV-0003  When would you impute with 0 instead of the mean, and when
              is that a category-not-missing signal?
  DS-IV-0004  How do you detect leakage in someone else's notebook
              quickly? What are your top-3 red flags?
  DS-IV-0005  You have 1M rows, 2000 features, 1 target. How do you
              explore it without a 128 GB machine?
PROJECTS
  DS-PROJ-0001  [C] Complete EDA + preprocessing project on a real
                dataset: report, leakage audit, pipelines, baseline.
  DS-PROJ-0002  [B] Build an auto-ml-lite: clean -> pipeline -> baseline
                -> a few models -> error analysis report.
  DS-PROJ-0003  [A] Build a drift-detection harness: monitor feature
                distributions (PSI/JS) and model performance over time on
                rolling windows, with alerts.
  DS-PROJ-0004  [S] Production data-quality gate integrated into a CI
                training pipeline (schema, drift, leakage checks).
RESEARCH
  DS-RES-0001  Design an experiment measuring the cost of label noise:
              flip k% of labels, plot accuracy vs k, and fit a curve.
  DS-RES-0002  Compare imputation strategies (mean/KNN/iterative) across
              missingness mechanisms (MCAR/MAR/MNAR) - design the study.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 06 - MACHINE LEARNING (SUPERVISED + UNSUPERVISED)
================================================================================
THEORY / CONCEPTUAL
  ML-TH-0001  What is the difference between supervised, unsupervised,
              semi-supervised, self-supervised, and reinforcement learning?
              Give a real task for each.
  ML-TH-0002  What is the bias-variance trade-off? How do tree depth,
              regularization, and data size each move it?
  ML-TH-0003  What is overfitting vs underfitting, and what evidence
              distinguishes them on learning curves?
  ML-TH-0004  Linear regression: what are its four core assumptions, and
              what breaks when each is violated?
  ML-TH-0005  Why does L1 (lasso) give sparse solutions while L2 (ridge)
              shrinks but never zeros coefficients?
  ML-TH-0006  Logistic regression: why is it a linear classifier despite
              the sigmoid? What is the decision boundary?
  ML-TH-0007  KNN: why is it called lazy learning? What is the curse of
              dimensionality doing to distances?
  ML-TH-0008  Naive Bayes: what is the naive assumption, why is it false,
              and why does the model still work for text?
  ML-TH-0009  Decision trees: how are splits chosen (entropy/Gini)? Why
              are trees high-variance learners?
  ML-TH-0010  Random Forest: how do bagging + feature subsampling
              decorrelate trees, and why does that reduce variance?
  ML-TH-0011  Gradient boosting: what exactly is being fit at each step,
              and how is it different from bagging?
  ML-TH-0012  XGBoost vs LightGBM vs CatBoost: one-line difference each
              (regularized boosting / histogram + GOSS / ordered targets).
  ML-TH-0013  SVM: what is the margin, what are support vectors, and what
              does the kernel trick actually do?
  ML-TH-0014  K-means: what is the objective, and why is it sensitive to
              initialization and outliers?
  ML-TH-0015  DBSCAN vs k-means: what data can k-means never cluster
              correctly (arbitrary shapes, noise)?
  ML-TH-0016  PCA: what is it optimizing, and what does 'explained
              variance' mean?
  ML-TH-0017  What is the difference between parametric and non-parametric
              models, with two examples each?
  ML-TH-0018  What is the difference between generative and discriminative
              models? Where does Naive Bayes vs logistic regression land?
WHY QUESTIONS
  ML-TH-0019  Why does feature scaling matter for gradient-based and
              distance-based models but not trees?
  ML-TH-0020  Why does boosting fit residuals (or gradients) instead of
              the original target?
  ML-TH-0021  Why can a deeper tree memorize the training set? Why does
              the same depth work in a forest but not alone?
  ML-TH-0022  Why is the SVM dual formulation useful even though the
              primal is solvable? (kernels, support vectors)
  ML-TH-0023  Why does logistic regression output 'probabilities' that are
              not calibrated? What does Platt scaling fix?
  ML-TH-0024  Why does k-means fail on data with different cluster
              densities/variances, and what does GMM change?
  ML-TH-0025  Why is accuracy a poor metric on imbalanced data, and what
              three metrics replace it?
COMPARISON (pick two each and compare mathematically + practically)
  ML-TH-0026  Linear regression vs Ridge vs Lasso (bias, sparsity, when
              each wins).
  ML-TH-0027  Logistic regression vs KNN vs SVM on the same 2D problem.
  ML-TH-0028  Decision tree vs Random Forest vs gradient boosting
              (variance, bias, training cost, interpretability).
  ML-TH-0029  Random Forest vs XGBoost vs LightGBM vs CatBoost (accuracy,
              speed, categorical handling, regularization).
  ML-TH-0030  K-means vs DBSCAN vs hierarchical clustering (shapes,
              noise, K choice, stability).
  ML-TH-0031  PCA vs t-SNE vs UMAP (linear vs nonlinear, global vs local
              structure, when to use for features vs visualization).
  ML-TH-0032  Bagging vs boosting: reduce variance vs reduce bias.
  ML-TH-0033  Batch vs online (incremental) learning: when is online
              necessary and what breaks?
MATHEMATICAL
  ML-MATH-0001  Derive ridge regression's objective and its closed-form
              solution; explain what adding lambda I does to the inverse.
  ML-MATH-0002  Show the L1 vs L2 geometry: why does L1 hit the axes
              (sparsity)?
  ML-MATH-0003  Compute entropy and information gain for a toy split with
              counts; decide which split to pick.
  ML-MATH-0004  Write the SVM hard-margin objective and the hinge loss for
              soft margin; explain the C parameter's role.
  ML-MATH-0005  K-means: write the objective, prove the E-step and M-step
              each monotonically decrease it.
  ML-MATH-0006  Gini impurity: compute for a node with counts [7, 3].
  ML-MATH-0007  Logistic regression: derive dL/dw for cross-entropy with
              sigmoid.
  ML-MATH-0008  Gradient boosting: write the Newton/least-squares step for
              a regression tree on pseudo-residuals.
  ML-MATH-0009  R-squared: definition, why it can be negative, and why
              high R2 does not imply a good model.
  ML-MATH-0010  Explain why (and when) the normal equation is O(d^3) and
              gradient methods are O(nd) per epoch - which wins for d=100k?
CODING
  ML-CODE-0001  [L2] Implement k-fold CV by hand (no sklearn) and verify
              scores match sklearn's.
  ML-CODE-0002  [L3] Implement k-means with k-means++ init and elbow
              selection; verify against sklearn.
  ML-CODE-0003  [L3] Implement PCA from scratch via SVD; verify explained
              variance matches sklearn.
  ML-CODE-0004  [L4] Implement a decision tree (classification) with
              entropy splits, depth/pruning limits - no sklearn.
  ML-CODE-0005  [L5] Implement random forest: bootstrap + feature
              subsampling + majority vote, on top of your tree.
  ML-CODE-0006  [L5] Implement gradient boosting for regression with
              learning rate and early stopping, verified vs sklearn.
  ML-CODE-0007  [L6] Implement soft-margin SVM via SGD on the hinge loss
              with kernel option (RBF) - verify vs sklearn.
  ML-CODE-0008  [L6] Implement Naive Bayes (Gaussian + Multinomial) from
              scratch and compare likelihoods on a toy dataset.
  ML-CODE-0009  [L7] Implement k-NN with a KD-tree or ball tree and
              compare speed vs brute force.
  ML-CODE-0010  [L7] Implement DBSCAN from scratch with correct
              border/noise labeling; verify cluster assignments.
DEBUGGING
  ML-DEBUG-0001  Train accuracy 1.0, val 0.61. Name the top-5 hypotheses
              in order (leakage, memorization, split, label noise, class
              imbalance in split).
  ML-DEBUG-0002  Ridge with huge lambda drives val error UP from baseline.
              What regime is this and how do you choose lambda?
  ML-DEBUG-0003  K-means returns different clusters every run with the
              same data. What is nondeterministic and how do you fix it?
  ML-DEBUG-0004  Logistic regression diverges to NaN after a few epochs.
              List fixes (LR, scaling, clip, init).
  ML-DEBUG-0005  XGBoost is 3x slower than LightGBM on the same data.
              What structural differences explain it?
  ML-DEBUG-0006  A tree with max_depth=30 performs worse than depth=5.
              Why can capacity HURT trees specifically?
  ML-DEBUG-0007  PCA components look like noise instead of structure. What
              did preprocessing (scaling) have to do with it?
  ML-DEBUG-0008  SVM with RBF takes hours on 100k samples. What
              approximations exist and what is the trade-off?
  ML-DEBUG-0009  Your CV score is 0.9 but the leaderboard score is 0.5.
              List the leakage/contamination suspects.
CODE REVIEW
  ML-REVIEW-0001  Review: `model.fit(X, y); x_scaled = scaler.fit(X)` -
              order of fit vs transform.
  ML-REVIEW-0002  Review: tuning C on the test set and then reporting
              test accuracy as final.
  ML-REVIEW-0003  Review: `GridSearchCV` inside the same notebook that
              built the preprocessing on full data.
  ML-REVIEW-0004  Review: k=3 for KNN on 2 features with 10 samples - why
              is the estimate noisy and what would you change?
INTERVIEW
  ML-IV-0001  Explain bias-variance with a target-shooting analogy, then
              mathematically (E[(f-f*)^2] decomposition).
  ML-IV-0002  When would you pick logistic regression over XGBoost even
              if XGBoost scores higher? (interpretability, latency, small
              data, calibration, deployment constraints)
  ML-IV-0003  Walk through choosing the metric and model for a fraud
              detection system with 99.9% legitimate transactions.
  ML-IV-0004  How do you handle a new categorical value at serving time
              that the encoder never saw?
  ML-IV-0005  Explain the kernel trick to a non-technical person, then
              write the RBF kernel formula and its two hyperparameters.
SCENARIO
  ML-SCEN-0001  Your model must run on a phone with 10 MB budget and 5 ms
              latency. Which algorithms are excluded and why?
  ML-SCEN-0002  A stakeholder insists on accuracy; the business cost of a
              false positive is 100x a false negative. How do you reframe
              the metric conversation?
  ML-SCEN-0003  You inherit a notebook with 0.97 CV. Your job: make it
              production-safe. List your audit steps in order.
  ML-SCEN-0004  You have 2 weeks and a small labeled dataset for a
              high-stakes classifier. Design the full plan (baseline,
              validation, calibration, reporting).
PROJECTS
  ML-PROJ-0001  [D] From-scratch linear + ridge regression on a real
                dataset with residual diagnostics.
  ML-PROJ-0002  [C] Classification project: preprocessing -> baseline ->
                logistic/tree/SVM -> metrics -> confusion analysis.
  ML-PROJ-0003  [B] Ensemble project: RF + GBM + blending with proper CV,
                feature importance, and calibration.
  ML-PROJ-0004  [A] End-to-end ML system for a business problem: EDA,
                leakage-safe pipeline, multiple models, model card,
                deployment plan.
  ML-PROJ-0005  [S] Imbalanced-data platform: resampling + weights +
                threshold tuning with cost-based evaluation.
  ML-PROJ-0006  [S+] Unsupervised project: clustering + PCA + anomaly
                detection on logs with evaluation against ground truth.
RESEARCH
  ML-RES-0001  Design an ablation: does feature subsampling in RF matter
              more for wide or deep data? Test and quantify.
  ML-RES-0002  Compare calibration (Platt vs isotonic) across model
              families; what correlates with miscalibration?
  ML-RES-0003  Empirically test when XGBoost beats LightGBM (small data,
              many zeros); design the benchmark honestly.
  ML-RES-0004  Reproduce a published result from a paper's public code and
              quantify sensitivity to seed/hyperparameters.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 07 - ML EVALUATION (METRICS + VALIDATION)
================================================================================
THEORY / CONCEPTUAL
  EV-TH-0001  Define TP/FP/TN/FN and derive accuracy, precision, recall,
              specificity, F1 from them.
  EV-TH-0002  Precision vs recall: which do you maximize for spam
              filtering vs medical screening, and why?
  EV-TH-0003  What is the F-beta score, and what does beta control?
  EV-TH-0004  ROC curve: what is plotted against what, and what does AUC
              actually measure (rank quality)?
  EV-TH-0005  Precision-recall curve vs ROC: when is PR the honest choice?
  EV-TH-0006  What is log loss and why does it penalize confident wrong
              answers more than accuracy does?
  EV-TH-0007  What is Matthews correlation coefficient and why is it a
              better single number for imbalanced data?
  EV-TH-0008  Regression metrics: MAE vs MSE vs RMSE - which is robust to
              outliers and which punishes them?
  EV-TH-0009  What is R2 and adjusted R2? When is R2 misleading
              (nonlinear data, different-scale targets)?
  EV-TH-0010  What is a calibration curve, and what does 'well calibrated'
              mean? When does a perfectly calibrated model still lose
              money?
  EV-TH-0011  What are ranking metrics (NDCG, MAP, MRR) and what task
              needs them?
  EV-TH-0012  What is cross-validation vs holdout, and when is nested CV
              necessary?
WHY QUESTIONS
  EV-TH-0013  Why can a model with lower AUC beat a higher-AUC model in
              production? (operating point, costs, distribution)
  EV-TH-0014  Why does tuning on validation and reporting validation as
              final inflate the estimate?
  EV-TH-0015  Why do we need a fixed test set that is NEVER touched during
              development?
  EV-TH-0016  Why is averaging F1 across classes different from macro-F1
              vs micro-F1, and which hides minority failures?
  EV-TH-0017  Why should you report confidence intervals on metrics, not
              just point estimates?
MATHEMATICAL
  EV-MATH-0001  Given confusion matrix [[50, 5], [10, 35]], compute
              accuracy, precision, recall, F1, specificity.
  EV-MATH-0002  Prove: precision and recall are both 1 only when the
              confusion matrix is diagonal.
  EV-MATH-0003  Compute log loss for a single example: p=0.01, y=1.
              What is the loss, and what happens as p->0?
  EV-MATH-0004  Show that AUC equals the probability that a random
              positive scores above a random negative.
  EV-MATH-0005  Compute NDCG@3 for rankings [3,1,2] vs [1,3,2] with
              relevance [3,2,1].
  EV-MATH-0006  For a 99:1 dataset with a model that always predicts
              majority, compute accuracy, F1, AUC, and log loss - which
              metrics expose the failure?
  EV-MATH-0007  Derive the relationship RMSE >= MAE and when they are
              equal.
  EV-MATH-0008  Compute the standard error of a CV estimate given per-fold
              scores; how many folds to halve it?
CODING
  EV-CODE-0001  [L2] Implement accuracy/precision/recall/F1/confusion
              matrix from raw predictions - no sklearn, verify against it.
  EV-CODE-0002  [L3] Implement ROC curve + AUC via ranking (sort by score,
              walk thresholds) and verify against sklearn.
  EV-CODE-0003  [L4] Implement PR-AUC via trapezoid integration on the
              curve (beware of ties).
  EV-CODE-0004  [L5] Implement nested CV: inner loop tunes, outer loop
              estimates generalization - with a budget warning.
  EV-CODE-0005  [L6] Implement a calibration evaluator: bin predictions,
              plot reliability, compute ECE with correct binning.
  EV-CODE-0006  [L7] Implement NDCG with ties and missing judgments.
DEBUGGING
  EV-DEBUG-0001  F1 is 0.5 but accuracy is 0.99 - is this a bug or
              expected? Compute the confusion matrix that explains it.
  EV-DEBUG-0002  AUC is 1.0 on CV. Before celebrating, what must you
              check? (leakage, duplicate rows, wrong target alignment)
  EV-DEBUG-0003  Two models have identical AUC but different log loss.
              What does that tell you about their probability quality?
  EV-DEBUG-0004  Your PR curve looks like a staircase with few points.
              What is degenerate and why?
  EV-DEBUG-0005  Calibration curve is flat at 0.5 for a binary model.
              What happened and how do you verify?
  EV-DEBUG-0006  Test accuracy is 0.95 but per-class recall on class A is
              0.1 - your report only showed the aggregate. What now?
INTERVIEW
  EV-IV-0001  You can only report ONE metric to a CEO for a fraud model.
              Which and why? Follow-up: what would you hide by doing so?
  EV-IV-0002  Why is '99.9% accuracy on 0.1% positives' a red flag in a
              candidate's project?
  EV-IV-0003  Walk through computing AUC by hand from 5 scores.
  EV-IV-0004  How do you compare two models' performance with statistical
              rigor? (paired tests, McNemar, bootstrap CIs)
  EV-IV-0005  What is the difference between evaluating a classifier by
              threshold and by ranking? When is ranking not enough?
PROJECTS
  EV-PROJ-0001  [D] A metrics library from scratch with unit tests against
                sklearn on 10 random confusion matrices.
  EV-PROJ-0002  [C] An evaluation dashboard: ROC/PR/calibration for a
                model with class filters and cost sliders.
  EV-PROJ-0003  [B] A robust model-comparison harness: repeated
                train/test, bootstrap CIs, paired significance, report.
  EV-PROJ-0004  [A] Cost-aware evaluation: threshold optimization under an
                asymmetric cost matrix with expected-value curves.
RESEARCH
  EV-RES-0001  Design a study of metric instability: how many bootstrap
              samples give stable AUC CIs? Empirically justify.
  EV-RES-0002  When does accuracy paradox (F1 vs accuracy) mislead
              business decisions? Quantify with synthetic data.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 08 - FEATURE ENGINEERING + SELECTION
================================================================================
THEORY / CONCEPTUAL
  FE-TH-0001  What is feature engineering vs feature selection vs feature
              extraction? Give one example of each.
  FE-TH-0002  Why do domain features usually beat generic ones? Give a
              fraud/health example.
  FE-TH-0003  What are interaction features and polynomial features, and
              when do they help linear models but not trees?
  FE-TH-0004  What is target encoding, why is it powerful, and what makes
              it leak? How is it done safely (out-of-fold)?
  FE-TH-0005  What are filter vs wrapper vs embedded feature-selection
              methods? One example each and their cost.
  FE-TH-0006  What is mutual information, and why does it capture
              nonlinear dependence that correlation misses?
  FE-TH-0007  What is Recursive Feature Elimination and its failure mode
              (re-fitting cost, instability)?
  FE-TH-0008  Why can dropping correlated features stabilize coefficients
              without losing much accuracy?
  FE-TH-0009  What is the difference between date-time feature extraction
              (hour, weekday, holiday) and treating ts as a number?
  FE-TH-0010  What are lag and rolling features, and what leakage do they
              create in time series?
WHY QUESTIONS
  FE-TH-0011  Why does one-hot encoding a 10k-cardinality column explode
              memory, and what do target/frequency/hash encodings change?
  FE-TH-0012  Why must encoders be fit on train folds ONLY? Show the
              target-encoding leakage path.
  FE-TH-0013  Why does adding more features not always help, even with
              regularization? (noise features, interactions with splits)
  FE-TH-0014  Why do tree-importance and permutation-importance disagree,
              and which is safer to trust?
  FE-TH-0015  Why should you bucket/one-hot time-of-day instead of
              encoding it as a continuous hour for some problems?
MATHEMATICAL
  FE-MATH-0001  Compute mutual information for a toy joint distribution
              and compare with Pearson correlation.
  FE-MATH-0002  Polynomial features: for d raw features, how many
              degree-2 terms exist? (d(d+1)/2 + d)
  FE-MATH-0003  Target encoding: derive the smoothed estimator
              (global mean + k * group mean) / (n_group + k) - what does k
              control?
  FE-MATH-0004  Z-score vs log1p vs Box-Cox: what distribution does each
              assume/induce?
  FE-MATH-0005  Show why log-transform makes multiplicative effects
              additive for linear models.
CODING
  FE-CODE-0001  [L2] Build a feature pipeline: date -> hour/weekday/
              holiday, categorical -> one-hot/target, numeric -> scale -
              all leakage-safe via Pipeline.
  FE-CODE-0002  [L3] Implement out-of-fold target encoding and prove it
              does not leak (compare in-fold vs full-data encodings).
  FE-CODE-0003  [L4] Implement permutation importance by hand and verify
              vs sklearn's estimator.
  FE-CODE-0004  [L5] Implement mutual information for continuous features
              via kNN estimation (or verify against sklearn).
  FE-CODE-0005  [L6] Build lag/rolling feature generation for time series
              with a 'no-future' guarantee (shift with min_periods).
DEBUGGING
  FE-DEBUG-0001  Adding a categorical frequency feature drops CV by 5
              points. What is the most likely cause (future info)?
  FE-DEBUG-0002  One-hot encoder crashes at serving on an unseen category.
              Design the fix (handle_unknown + fallback bucket).
  FE-DEBUG-0003  Permutation importance says a noise column is top-3.
              What does that imply about the model?
  FE-DEBUG-0004  Target-encoded feature gives 0.99 CV. Before celebrating,
              name the leak and the fix.
  FE-DEBUG-0005  Log transform produces -inf on zeros. What is the
              standard guard and its effect?
CODE REVIEW
  FE-REVIEW-0001  Review: `df['target_enc'] = df.groupby('cat')['y'].mean()`
              computed on the full dataset before splitting.
  FE-REVIEW-0002  Review: scaling fitted with test data statistics.
  FE-REVIEW-0003  Review: `drop=True` in a get_dummies pipeline that
              removes the original - what breaks at serve time?
  FE-REVIEW-0004  Review: a 'row_id' and 'date' column fed into a tree
              model as numeric features.
INTERVIEW
  FE-IV-0001  How do you decide between keeping a high-cardinality
              categorical as-is, target-encoding it, or hashing it?
  FE-IV-0002  Walk through building features for a churn model from a
              user-event log (aggregates, recency, frequency, trends).
  FE-IV-0003  Why might removing a correlated feature IMPROVE a linear
              model but not a forest? Explain with variance.
  FE-IV-0004  How do you prevent feature engineering from leaking the
              future in a time-series setting?
PROJECTS
  FE-PROJ-0001  [C] Feature-engineering competition-style notebook: raw
                tables -> features -> CV score, with a leakage audit.
  FE-PROJ-0002  [B] Automated feature-selection pipeline: filter ->
                wrapper -> embedded comparison with a budget.
  FE-PROJ-0003  [A] A feature store lite: versioned feature definitions,
                point-in-time-correct joins, and a serving API.
  FE-PROJ-0004  [S] Feature drift monitoring: track per-feature
                distributions in production vs training and alert.
RESEARCH
  FE-RES-0001  Design a study: does target encoding with out-of-fold beat
              one-hot on 5 real datasets? Report the honest comparison.
  FE-RES-0002  How much accuracy does feature selection cost on
              tree ensembles vs linear models? Measure with ablations.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 09 - DEEP LEARNING (FUNDAMENTALS)
================================================================================
THEORY / CONCEPTUAL
  DL-TH-0001  What is a neuron? What does a layer compute, and why does
              depth (vs width) matter for representation?
  DL-TH-0002  What is backpropagation, exactly? Why is it 'just' the
              chain rule with memoized gradients?
  DL-TH-0003  Activation functions: sigmoid, tanh, ReLU, LeakyReLU, GELU,
              SiLU/Swish - derivatives, saturation, dead neurons.
  DL-TH-0004  Why did ReLU replace sigmoid/tanh in hidden layers, and
              what is the dying-ReLU problem?
  DL-TH-0005  Loss functions: when is MSE wrong for classification, and
              why does cross-entropy with softmax work?
  DL-TH-0006  Optimizers: how do momentum, RMSProp, and Adam each
              improve on plain SGD? What does AdamW change (decoupled
              weight decay)?
  DL-TH-0007  Vanishing vs exploding gradients: what causes each, and
              what are the three fixes (init, norm, architecture)?
  DL-TH-0008  Why is initialization critical? What do Xavier and He init
              actually preserve (variance of activations/gradients)?
  DL-TH-0009  Dropout: what does it regularize, and why does it act like
              an implicit ensemble?
  DL-TH-0010  BatchNorm vs LayerNorm vs GroupNorm vs RMSNorm: what is
              normalized across which axes, and why does BN fail for
              transformers/NLP?
  DL-TH-0011  Residual connections: why does x + f(x) fix vanishing
              gradients and make very deep nets trainable?
  DL-TH-0012  Learning rate schedules: warmup, cosine decay, step decay -
              what does each assume about the loss landscape?
  DL-TH-0013  Gradient clipping: what does it protect, and why is it
              essential for RNNs?
  DL-TH-0014  What is the difference between a model that underfits,
              overfits, and is mis-specified? How do you tell from curves?
  DL-TH-0015  Why do deeper networks need more data and why do they
              overfit more easily without regularization?
WHY QUESTIONS
  DL-TH-0016  Why does a 2-layer ReLU net with enough width approximate
              any continuous function, yet learn poorly in practice?
  DL-TH-0017  Why does batch size affect generalization, not just speed?
              (noise in gradients, flat minima argument)
  DL-TH-0018  Why do we need nonlinearity at all? What would a deep
              linear net collapse to?
  DL-TH-0019  Why does weight decay in Adam need decoupling (AdamW)? What
              goes wrong with naive L2 inside Adam's adaptive step?
  DL-TH-0020  Why does a larger learning rate sometimes find better
              minima (implicit regularization, basin of attraction)?
MATHEMATICAL
  DL-MATH-0001  Derive the backward pass for a 2-layer MLP: given loss,
              compute dL/dW1, dL/dW2, dL/db1, dL/db2.
  DL-MATH-0002  Compute the derivative of GELU (with erf) and SiLU
              (sigmoid * x).
  DL-MATH-0003  Show that softmax + cross-entropy backward simplifies to
              (p - y).
  DL-MATH-0004  For a hidden layer h = Wx + b with ReLU, write the
              gradient w.r.t. W as an outer product with the mask.
  DL-MATH-0005  Xavier init: derive the variance condition
              Var(W) = 2/(fan_in + fan_out) from activation-variance
              preservation.
  DL-MATH-0006  He init: why 2/fan_in for ReLU (the factor-of-2
              argument)?
  DL-MATH-0007  Adam update equations: write m, v, bias correction, and
              the update for step t=1,2.
  DL-MATH-0008  Dropout at test time: why multiply by (1-p)? Derive the
              expectation-matching argument.
  DL-MATH-0009  BatchNorm: write the forward (normalize, scale, shift)
              and what happens with batch size 1.
  DL-MATH-0010  Compute the parameter count of an MLP 784-256-128-10 with
              biases, then its FLOPs per forward.
CODING
  DL-CODE-0001  [L2] Implement a 2-layer MLP from scratch (no torch.nn)
              with manual backward for MSE.
  DL-CODE-0002  [L3] Implement SGD with momentum by hand and verify
              against torch.optim.SGD.
  DL-CODE-0003  [L4] Implement Dropout with inverted scaling and verify
              train/eval behavior matches torch.nn.Dropout.
  DL-CODE-0004  [L4] Implement LayerNorm and RMSNorm from scratch and
              verify against torch.
  DL-CODE-0005  [L5] Implement Adam and AdamW by hand; verify the closed
              form against torch on a random problem.
  DL-CODE-0006  [L6] Implement a mini training loop with gradient
              accumulation, clipping, warmup + cosine decay - all manual.
  DL-CODE-0007  [L6] Implement gradcheck: numerical gradient vs autograd
              for a small net, with the central-difference caveats.
DEBUGGING
  DL-DEBUG-0001  Loss is NaN at step 3. Ordered checklist: LR, log(0),
              inf in softmax, init std, label range, mixed-precision.
  DL-DEBUG-0002  Training loss decreases but val loss increases from step
              1. What is different from classic overfitting?
  DL-DEBUG-0003  All ReLU outputs are exactly 0 after 10 steps. What
              happened, and what are the two standard fixes?
  DL-DEBUG-0004  Loss plateaus at a value equal to -log(1/C). What is
              the model doing, and what does that plateau prove?
  DL-DEBUG-0005  Same seed, same data, loss differs between runs on GPU.
              Name the nondeterminism sources and how to reduce them.
  DL-DEBUG-0006  Your validation accuracy drops when you switch from
              train() to eval() mode. What mode-dependent ops are to blame?
  DL-DEBUG-0007  Adding BatchNorm makes a tiny batch train WORSE. Explain
              the batch-statistics effect.
  DL-DEBUG-0008  Gradient norms explode only after layer 10. What
              diagnostic plot do you make and what fix applies?
CODE REVIEW
  DL-REVIEW-0001  Review: `x = torch.relu(x)` in the forward but no
              dropout/inference-mode handling in validation.
  DL-REVIEW-0002  Review: learning rate 1.0 with Adam on a small net.
  DL-REVIEW-0003  Review: `loss.backward()` twice without zero_grad.
  DL-REVIEW-0004  Review: saving only the model state but restoring the
              optimizer from a different config.
  DL-REVIEW-0005  Review: a test set used to pick the epoch for early
              stopping, then reported as final.
INTERVIEW
  DL-IV-0001  Explain backpropagation to a CS student in 2 minutes, then
              derive the update for one weight numerically.
  DL-IV-0002  Why is ReLU used everywhere despite being non-differentiable
              at 0? What do modern alternatives (GELU/SiLU) add?
  DL-IV-0003  You have 30 minutes of GPU. How do you decide whether to
              increase data, capacity, or training time?
  DL-IV-0004  Walk through diagnosing a model stuck at 50% on 2 classes
              (is it random? inspect loss, logits, labels).
  DL-IV-0005  What is the difference between regularization that reduces
              capacity and regularization that changes the objective?
SCENARIO
  DL-SCEN-0001  Your training is 10x slower than expected on a new GPU.
              Where do you look (data loading, GPU util, batch size,
              kernel)?
  DL-SCEN-0002  A colleague's model trains to 0 loss in 2 epochs. Is that
              good news? What could it mean?
  DL-SCEN-0003  You must reproduce a paper with no public code. Walk
              through your order of operations.
PROJECTS
  DL-PROJ-0001  [D] From-scratch MLP on MNIST: manual backward, verify
                against autograd, report curves.
  DL-PROJ-0002  [C] Training-tricks project: measure the effect of each
                trick (init, norm, dropout, schedule) via ablations.
  DL-PROJ-0003  [B] Reproduce a small paper result end-to-end with a
                clean experiment harness (seeds, ablations, report).
  DL-PROJ-0004  [A] Train a small transformer from scratch on a language
                task with a full experiment log and model card.
RESEARCH
  DL-RES-0001  Design an ablation isolating why a trick works: e.g. does
              warmup help via gradient noise or via scale? Control for
              both.
  DL-RES-0002  Measure the flat-minima hypothesis: perturb weights after
              training, compare loss sensitivity across batch sizes.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 10 - PYTORCH
================================================================================
THEORY / CONCEPTUAL
  PT-TH-0001  What is a tensor? Shape, dtype, device, and how they
              interact. What does a 'view' vs a 'copy' mean in torch?
  PT-TH-0002  How does autograd work? What does the computational graph
              store, and when is it freed?
  PT-TH-0003  requires_grad, grad, grad_fn: what does each attribute
              mean, and when is grad None?
  PT-TH-0004  torch.no_grad vs inference_mode vs detach: when is each the
              right choice?
  PT-TH-0005  What is the difference between an nn.Module, a parameter,
              and a buffer? When do you need a buffer (running stats)?
  PT-TH-0006  Dataset vs DataLoader: what does each do, and what does a
              collate_fn actually control?
  PT-TH-0007  Why is DataLoader num_workers > 0 important, and what
              breaks when workers > 0 with a lambda collate?
  PT-TH-0008  What is a checkpoint vs a full training state (optimizer,
              scheduler, RNG)? Why resume from the full state?
  PT-TH-0009  What is mixed precision (AMP)? What is the difference
              between fp16 + loss scaling and bf16?
  PT-TH-0010  What is gradient accumulation and how does it emulate a
              large batch? What changes about normalization when you use it?
  PT-TH-0011  What does model.train() vs model.eval() toggle, and why
              does forgetting eval() silently corrupt metrics?
  PT-TH-0012  What is torch.compile and what does it do at a high level
              (graph capture, fusion)?
  PT-TH-0013  What is a state_dict? How do strict vs non-strict loading
              differ, and when would you use each?
WHY QUESTIONS
  PT-TH-0014  Why does moving data to GPU per-batch inside the loop kill
              throughput? What is the pinned-memory + non_blocking fix?
  PT-TH-0015  Why can't you call backward() twice by default, and what
              does retain_graph actually cost?
  PT-TH-0016  Why should you call zero_grad(set_to_none=True) instead of
              zero_grad()?
  PT-TH-0017  Why does a DataLoader worker get slower with more workers
              past a point? (spawn overhead, I/O saturation)
  PT-TH-0018  Why does `torch.cuda.synchronize()` exist and when does
              PyTorch hide async execution from you?
  PT-TH-0019  Why does training run out of memory but inference not? What
              stores activations and how does checkpointing trade compute
              for memory?
COMPARISON
  PT-TH-0020  Sequential vs ModuleList vs ModuleDict: registration
              semantics and why a plain list breaks .to(device)/state_dict.
  PT-TH-0021  F.cross_entropy vs manual softmax + nll_loss: what does
              F.cross_entropy fuse and why does it matter numerically?
  PT-TH-0022  .item() vs .tolist() vs .detach().cpu().numpy(): what each
              copies and when it synchronizes.
  PT-TH-0023  torch.save whole-model vs state_dict: why state_dict is
              portable and whole-model is fragile.
  PT-TH-0024  nn.Linear vs manual weight @ x + bias: autograd behavior
              and initialization differences.
MATHEMATICAL
  PT-MATH-0001  Trace the shapes through a (64, 3, 32, 32) image batch:
              conv3x3 (16 ch) -> maxpool2 -> flatten -> linear(256, 10).
  PT-MATH-0002  What is the memory of one training step for a 1B-param
              model in fp16 (params, grads, Adam states, activations)?
  PT-MATH-0003  Batch size 32 -> 128 via accumulation: how many steps,
              and how does BN/LN behave differently?
  PT-MATH-0004  With AMP, why does the master weight stay fp32 while the
              forward runs fp16? Where does the copy happen?
  PT-MATH-0005  Compute the theoretical FLOPs of a (T=512, d=768)
              attention layer with 12 heads.
CODING
  PT-CODE-0001  [L2] Build a custom Dataset over a folder of images with
              transforms, and a DataLoader with num_workers=4.
  PT-CODE-0002  [L3] Implement a training loop with AMP, gradient
              clipping, checkpointing (best + last), and resume.
  PT-CODE-0003  [L4] Write a custom nn.Module for a residual block and
              verify it trains vs an equivalent Sequential.
  PT-CODE-0004  [L4] Implement a custom collate_fn that pads sequences to
              the batch max and builds an attention mask.
  PT-CODE-0005  [L5] Implement gradient accumulation correctly with
              proper loss scaling (divide by accumulation steps).
  PT-CODE-0006  [L5] Write a memory profiler: log peak alloc, activation
              sizes per layer, and a strategy to reduce the top consumer.
  PT-CODE-0007  [L6] Implement a custom autograd.Function (e.g. a
              straight-through estimator) and gradcheck it.
  PT-CODE-0008  [L6] Build a distributed data-parallel training script
              using torch.distributed (spawn or torchrun) with correct
              seed handling per rank.
  PT-CODE-0009  [L7] Implement torch.compile-compatible dynamic shape
              handling and benchmark vs eager on a variable-length task.
  PT-CODE-0010  [L7] Write a custom CUDA-free kernel using torch's
              vmap/torch.func to benchmark vs a manual loop.
DEBUGGING
  PT-DEBUG-0001  CUDA OOM on a 24 GB card at batch 16 but not 8. What
              are the top consumers and the 5 fixes in order of impact?
  PT-DEBUG-0002  After resume, loss is higher than the saved checkpoint's
              loss. What did you forget to save/restore?
  PT-DEBUG-0003  Model.eval() not called: dropout produces random val
              metrics. How do you catch this systematically (CI check)?
  PT-DEBUG-0004  DataLoader workers hang forever when num_workers=8 on
              Windows. What is the canonical cause and fix?
  PT-DEBUG-0005  Gradients are None after backward for one parameter.
              List the causes (detach, no requires_grad, not in graph,
              frozen).
  PT-DEBUG-0006  All inputs and outputs are NaN only in fp16, not fp32.
              What is the fix sequence (loss scaling, clip, bf16)?
  PT-DEBUG-0007  Training is slow but GPU util is 30%. Rank the
              suspects (data loading, CPU collate, small batch, syncs).
  PT-DEBUG-0008  state_dict load fails with size mismatch after a
              checkpoint from a different config. How do you load the
              matching subset safely?
  PT-DEBUG-0009  Two DataLoader workers produce DIFFERENT augmentations
              for the same seed. What is the worker-seed rule?
  PT-DEBUG-0010  torch.compile crashes on a dynamic-shape model. What are
              the workarounds (static shapes, mode, disable)?
CODE REVIEW
  PT-REVIEW-0001  Review: `optimizer = AdamW(model.parameters(), lr)`
              where model contains a frozen backbone (grads wasted).
  PT-REVIEW-0002  Review: `.item()` inside the training loop per batch.
  PT-REVIEW-0003  Review: `loss.backward(); optimizer.step()` without
              zero_grad in the first iteration.
  PT-REVIEW-0004  Review: loading a checkpoint with `strict=False` and
              silently ignoring missing keys.
  PT-REVIEW-0005  Review: comparing two models' accuracies with a single
              seed and a shared test set, no CI.
INTERVIEW
  PT-IV-0001  Walk through the full lifecycle of a training step: from
              forward to optimizer.step - what does autograd actually
              record and when?
  PT-IV-0002  Your model trains on GPU 0 but not GPU 1 (different
              results). What differs and how do you isolate it?
  PT-IV-0003  Explain why `model.to(device)` after optimizer creation
              breaks the optimizer's parameter reference.
  PT-IV-0004  What is the difference between data parallel, DDP, and FSDP
              at a high level, and when is each appropriate?
  PT-IV-0005  How would you profile a training step and find the
              bottleneck in 5 minutes?
SCENARIO
  PT-SCEN-0001  A 70B model must fine-tune on 2x 48 GB GPUs. Which
              memory techniques do you stack (bf16, grad ckpt, FSDP,
              LoRA) and in what order?
  PT-SCEN-0002  Inference latency must drop 3x on the same GPU. Rank:
              quantization, KV cache, batching, compile, kernel.
  PT-SCEN-0003  Training reproducibility across 2 machines is mandatory.
              Enumerate every seed/source you must pin.
PROJECTS
  PT-PROJ-0001  [D] Image classifier with a clean training harness:
                dataloader, AMP, checkpoints, TensorBoard-free logging.
  PT-PROJ-0002  [C] Experiment harness: yaml config -> train -> eval ->
                artifact registry, with resume support.
  PT-PROJ-0003  [B] Memory-optimized training: gradient checkpointing +
                accumulation + bf16 on a large model, with memory report.
  PT-PROJ-0004  [A] Multi-GPU training with DDP or FSDP on a real model,
                with scaling benchmark (1->2->4 GPUs).
RESEARCH
  PT-RES-0001  Design a benchmark comparing torch.compile vs eager on
              your model: control warmup, batch, shape; report speedup
              and variance.
  PT-RES-0002  Measure AMP's effect on final accuracy vs fp32 across 3
              tasks and 3 seeds; when does it hurt?
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 11 - CNN + COMPUTER VISION
================================================================================
THEORY / CONCEPTUAL
  CV-TH-0001  What does a convolution compute, and why is weight sharing
              the key idea (vs a fully connected layer)?
  CV-TH-0002  What are channels, kernels, stride, padding, and dilation?
              How do they trade off receptive field vs resolution?
  CV-TH-0003  What is a receptive field, and why does depth (not just
              kernel size) grow it?
  CV-TH-0004  Why does pooling reduce sensitivity to translation and cut
              compute? What information does it destroy?
  CV-TH-0005  Why did VGG stack small 3x3 kernels instead of one large
              kernel? (parameter count, receptive field, nonlinearity)
  CV-TH-0006  What problem did ResNet solve, and what is the actual
              mechanism of the skip connection?
  CV-TH-0007  Bottleneck blocks: what do the 1x1 convs do (channels up/
              down), and why are they efficient?
  CV-TH-0008  Data augmentation: why is it regularization? What
              augmentations are safe for medical images and which are not?
  CV-TH-0009  What is transfer learning for vision: frozen vs
              fine-tuned backbones, and when does freezing make sense?
  CV-TH-0010  Object detection: what is an anchor, and what does a
              one-stage (YOLO) vs two-stage (Faster R-CNN) pipeline look
              like?
  CV-TH-0011  Segmentation: what is the difference between semantic and
              instance segmentation, and what is the U-Net structure for?
  CV-TH-0012  What is a vision transformer (ViT), and what inductive
              bias does it drop vs a CNN?
WHY QUESTIONS
  CV-TH-0013  Why do CNNs need far fewer parameters than MLPs on images?
              (locality + weight sharing + translation equivariance)
  CV-TH-0014  Why does augmentation act as regularization, and why does
              it help more for small datasets?
  CV-TH-0015  Why is a 1x1 conv equivalent to a pointwise MLP, and why
              is it everywhere in modern nets?
  CV-TH-0016  Why do deeper nets with plain stacks get WORSE than shallow
              ones (degradation problem), and what does ResNet change?
  CV-TH-0017  Why do ViTs need more data (or distillation/augmentation)
              to match CNNs? What inductive bias are they missing?
MATHEMATICAL
  CV-MATH-0001  Output size formula: input 32x32, kernel 3, padding 1,
              stride 1 -> what size? Derive the general formula.
  CV-MATH-0002  Parameter count: conv(3->16, 3x3) + conv(16->32, 3x3) +
              linear(32*8*8, 10) - compute each.
  CV-MATH-0003  Receptive field after 2x (3x3, stride 1) layers =
              5x5; prove it. After a 2x2 pool?
  CV-MATH-0004  FLOPs of a 3x3 conv over a 224x224x64 feature map to
              128 channels: compute.
  CV-MATH-0005  Strided conv vs maxpool + conv: how does downsampling
              location change the parameter count?
CODING
  CV-CODE-0001  [L2] Implement a conv2d forward via im2col or direct
              loops and verify against torch.nn.Conv2d.
  CV-CODE-0002  [L3] Build a ResNet-style block (with/without bottleneck)
              as nn.Module and verify shapes.
  CV-CODE-0003  [L4] Implement a training loop with augmentation,
              fine-tuning a pretrained backbone, with correct freeze/
              unfreeze.
  CV-CODE-0004  [L5] Implement mixup or cutmix augmentation and measure
              its effect on val accuracy.
  CV-CODE-0005  [L6] Implement a simple U-Net for segmentation and train
              on a toy dataset with Dice loss.
  CV-CODE-0006  [L7] Implement gradient-based class activation (CAM)
              from a feature map + head weights.
DEBUGGING
  CV-DEBUG-0001  Train acc 99%, val 50% on images. Before tuning,
              inspect: label order, augmentation leaking to val, class
              imbalance, data duplication.
  CV-DEBUG-0002  Val accuracy is EXACTLY the majority-class fraction -
              the model outputs one class. What does the loss/gradient
              look like, and what fixes it?
  CV-DEBUG-0003  Model works on 224x224 but not 256x256 at serving.
              Where is the size dependence (resize, pool, interpolate)?
  CV-DEBUG-0004  Augmentation flips the image and the model learns
              mirrored classes - what does the confusion matrix show?
  CV-DEBUG-0005  Memory spikes during validation. What mode-dependent
              graph is being built and how do you stop it?
CODE REVIEW
  CV-REVIEW-0001  Review: data normalized with train stats at train time
              but raw 0-255 at inference.
  CV-REVIEW-0002  Review: augmentation applied to validation data.
  CV-REVIEW-0003  Review: `model.conv1.weight.data.normal_(0, 0.5)` on a
              ResNet before fine-tuning the head only.
  CV-REVIEW-0004  Review: flipping an X-ray horizontally as augmentation.
INTERVIEW
  CV-IV-0001  Why do we use 3x3 kernels instead of 7x7? Follow-up: when
              would 7x7 be justified?
  CV-IV-0002  Your CNN fails on rotated images. What is the failure mode
              and what are the fixes (augmentation, equivariant layers,
              data)?
  CV-IV-0003  How would you build a detection system for a single
              product category with 200 labeled images? Full plan.
  CV-IV-0004  What is the difference between how a CNN and a ViT attend
              to spatial information?
  CV-IV-0005  Explain maxpool to a non-technical PM, including what it
              discards.
PROJECTS
  CV-PROJ-0001  [D] MNIST/Fashion classifier with augmentation sweeps.
  CV-PROJ-0002  [C] Transfer learning project: fine-tune a pretrained
                backbone on a small custom dataset, report freezing
                strategies.
  CV-PROJ-0003  [B] Object detection with a pretrained detector on a
                custom class, with mAP evaluation.
  CV-PROJ-0004  [A] Segmentation project (U-Net) on a public dataset
                with Dice/IoU, augmentation, and failure analysis.
  CV-PROJ-0005  [S] Production vision service: FastAPI, GPU inference,
                image preprocessing parity tests, monitoring.
RESEARCH
  CV-RES-0001  Design an ablation: does Mixup help more on small or large
              datasets? Quantify and hypothesize why.
  CV-RES-0002  Compare CNN vs ViT on your dataset with matched compute;
              report the honest data-efficiency curve.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 12 - SEQUENCE MODELS (RNN / LSTM / GRU)
================================================================================
THEORY / CONCEPTUAL
  SQ-TH-0001  What does an RNN's recurrence compute, and why is the same
              weight shared across time?
  SQ-TH-0002  Why do RNNs suffer vanishing/exploding gradients, and why
              is this specifically a depth-in-time problem?
  SQ-TH-0003  LSTM: what do the forget, input, and output gates each
              control, and what is the cell state's role?
  SQ-TH-0004  GRU: which two gates, and how does it merge cell and hidden
              state vs the LSTM?
  SQ-TH-0005  What is BPTT (backprop through time), and what is truncated
              BPTT a compromise for?
  SQ-TH-0006  What is teacher forcing, and why does it make training
              easier but expose exposure bias at inference?
  SQ-TH-0007  Sequence-to-sequence: what is the encoder-decoder split,
              and where does the information bottleneck sit?
  SQ-TH-0008  Why did transformers replace RNNs for large-scale language
              modeling (parallelism, path length, memory)?
WHY QUESTIONS
  SQ-TH-0009  Why can't RNNs easily learn long-range dependencies even
              with LSTM? (gradient path length, capacity)
  SQ-TH-0010  Why is attention the fix for long-range information flow?
              What is the O(1) path between any two positions?
  SQ-TH-0011  Why does the same RNN layer process sequences in parallel
              across the batch but sequentially within a sequence?
  SQ-TH-0012  Why does a simple 'last hidden state' bottleneck hurt
              seq2seq, and what did attention-as-soft-lookup change?
MATHEMATICAL
  SQ-MATH-0001  Write the RNN update h_t = tanh(W_hh h_{t-1} + W_xh x_t)
              and derive dL/dW_hh via the chain rule (show the product
              of Jacobians).
  SQ-MATH-0002  Prove why the gradient over T steps contains a product
              of T Jacobians and why ||J|| < 1 => vanishing.
  SQ-MATH-0003  Write the full LSTM update equations (f, i, o, c, h) and
              label every gate.
  SQ-MATH-0004  GRU equations: update gate z, reset gate r, candidate h~.
  SQ-MATH-0005  Parameter count: LSTM with input 100, hidden 256: how
              many parameters in the 4 gates (with biases)?
CODING
  SQ-CODE-0001  [L2] Implement an RNN cell forward by hand and verify
              against torch.nn.RNNCell.
  SQ-CODE-0002  [L3] Implement an LSTM cell forward with all gates and
              verify against torch.nn.LSTMCell.
  SQ-CODE-0003  [L4] Implement truncated BPTT training for a char-level
              model with proper hidden-state carry.
  SQ-CODE-0004  [L5] Implement teacher forcing vs free-running and
              measure the exposure-bias gap.
  SQ-CODE-0005  [L6] Implement a seq2seq with attention (Bahdanau or
              Luong) from scratch and verify on a toy task.
DEBUGGING
  SQ-DEBUG-0001  Gradients vanish after 20 steps: how do you confirm with
              gradient norms per time step, and what are the fixes?
  SQ-DEBUG-0002  LSTM learns the first char of every word but nothing
              else. What does that output pattern mean?
  SQ-DEBUG-0003  At inference, the model repeats the last token forever.
              What is the decode loop doing wrong?
  SQ-DEBUG-0004  Hidden state carried across batches incorrectly - what
              does the loss curve look like and why?
  SQ-DEBUG-0005  A bidirectional RNN throws a shape error only on odd
              sequence lengths. What is the cause?
INTERVIEW
  SQ-IV-0001  Explain why LSTM's cell state creates a 'gradient highway'
              and how the forget gate can destroy it.
  SQ-IV-0002  When would you STILL choose an LSTM over a transformer
              today (tiny models, streaming, strict causal latency)?
  SQ-IV-0003  Walk through training a char-level language model end to
              end, including the exact label shift.
PROJECTS
  SQ-PROJ-0001  [D] Char-level RNN that learns a name generator.
  SQ-PROJ-0002  [C] Stock/temperature forecasting with LSTM vs ARIMA vs
                XGBoost - honest comparison.
  SQ-PROJ-0003  [B] Seq2seq with attention: date-format or math
                translation toy task with attention visualization.
  SQ-PROJ-0004  [A] Streaming sequence model deployed with causal
                inference and latency targets.
RESEARCH
  SQ-RES-0001  Design an experiment measuring the effective memory
              horizon of an LSTM (copy-task length sweep).
  SQ-RES-0002  Compare truncated BPTT vs full BPTT on a real task:
              compute vs quality trade-off with a fixed budget.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 13 - NLP
================================================================================
THEORY / CONCEPTUAL
  NLP-TH-0001  What is tokenization, and why can't we feed raw text to a
              model? What are the trade-offs of char/word/subword units?
  NLP-TH-0002  BPE vs WordPiece vs Unigram vs SentencePiece: the merge/
              objective difference and why SentencePiece is unicode-safe.
  NLP-TH-0003  What is an embedding, and why is semantic similarity a
              geometric property of the embedding space?
  NLP-TH-0004  Word2Vec (CBOW/Skip-gram) vs GloVe vs FastText: the
              training objective of each and what FastText adds for rare
              words.
  NLP-TH-0005  Why were static embeddings (Word2Vec/GloVe) insufficient
              - what does 'contextual' add (polysemy, syntax)?
  NLP-TH-0006  TF-IDF: what does each term mean and what bias does it
              correct? Why is it still used in hybrid search?
  NLP-TH-0007  Text classification pipeline: preprocessing, features,
              and how far modern embeddings take you vs classic features.
  NLP-TH-0008  NER: what is it, and why is the sequence structure (BIO
              tagging) important?
  NLP-TH-0009  What is stemming vs lemmatization, and when does it hurt
              (vs help) retrieval?
WHY QUESTIONS
  NLP-TH-0010  Why do subword tokenizers exist instead of words
              (out-of-vocab, morphology, size)?
  NLP-TH-0011  Why is cosine similarity preferred over Euclidean for
              comparing embeddings?
  NLP-TH-0012  Why does a transformer embedding change by layer - what do
              lower vs higher layers represent?
  NLP-TH-0013  Why does token count (not word count) drive LLM cost and
              context limits?
MATHEMATICAL
  NLP-MATH-0001  Compute TF-IDF for a term in a document given corpus
              statistics (derive the IDF formula).
  NLP-MATH-0002  Show how skip-gram's objective maximizes P(context |
              center) and why it factorizes a PMI matrix (conceptually).
  NLP-MATH-0003  Given two embedding vectors, write cosine similarity and
              its range; what angle gives sim 0?
  NLP-MATH-0004  BPE merge count: corpus vocab 10k, target vocab 32k -
              how many merges, and what does a byte-level fallback solve?
  NLP-MATH-0005  Count tokens for 'hello world' under a 32k BPE - walk
              the merge process conceptually with an example.
CODING
  NLP-CODE-0001  [L2] Implement TF-IDF by hand and verify against
              sklearn's vectorizer.
  NLP-CODE-0002  [L3] Train a skip-gram Word2Vec-style embedding from
              scratch on a small corpus and check nearest neighbors.
  NLP-CODE-0003  [L4] Implement BPE training (merge counting) on a small
              corpus and encode/decode round-trip.
  NLP-CODE-0004  [L5] Build a text classifier: embeddings -> pooling ->
              head, with an honest train/val split and error analysis.
  NLP-CODE-0005  [L6] Implement a subword-aware tokenizer fallback
              (unknown handling) and measure OOV behavior.
DEBUGGING
  NLP-DEBUG-0001  The model sees two different tokens for 'hello' and
              'Hello' - why, and how does the tokenizer fix it?
  NLP-DEBUG-0002  Retrieval misses documents that clearly match. The
              tokenizer is producing 3x more tokens than expected - what
              is happening (unicode, whitespace)?
  NLP-DEBUG-0003  Sentiment model is 0.5 accuracy - the tokenizer was
              fit on a different corpus and produces mostly UNK. Verify.
  NLP-DEBUG-0004  A BERT-style model's [CLS] embedding changes between
              runs with the same text. Where does nondeterminism enter?
  NLP-DEBUG-0005  Embedding similarity says 'cat' ~ 'dog' but also 'cat'
              ~ 'car' equally. What does that suggest about the training
              corpus?
CODE REVIEW
  NLP-REVIEW-0001  Review: dropping stopwords for a model that embeds
              text with a pretrained transformer.
  NLP-REVIEW-0002  Review: tokenizing the test set with a tokenizer that
              was trained on train+test.
  NLP-REVIEW-0003  Review: applying stemming to text that will be
              embedded by a subword model.
INTERVIEW
  NLP-IV-0001  Why is a subword tokenizer better than words for a
              multilingual model? Explain with an example.
  NLP-IV-0002  When would you use a bag-of-words model instead of an
              LLM? (small data, interpretability, cost, latency)
  NLP-IV-0003  How do you evaluate whether an embedding space is 'good'
              without labels? (intrinsic vs extrinsic)
PROJECTS
  NLP-PROJ-0001  [D] Spam/sentiment classifier with TF-IDF + linear
                model and error analysis.
  NLP-PROJ-0002  [C] Word2Vec from scratch with nearest-neighbor
                evaluation and analogy tests.
  NLP-PROJ-0003  [B] Fine-tune a small transformer for NER with BIO
                evaluation (entity-level F1).
  NLP-PROJ-0004  [A] Multilingual text classification with subword
                tokenization and domain-shift evaluation.
RESEARCH
  NLP-RES-0001  Design a study: how much does tokenizer choice (BPE vs
              Unigram, vocab size) change downstream task accuracy?
  NLP-RES-0002  Compare static vs contextual embeddings on a probing
              task (syntax, semantics, sentiment) - which layer wins?
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 14 - TRANSFORMERS
================================================================================
THEORY / CONCEPTUAL
  TF-TH-0001  What is self-attention, exactly? What are Q, K, V and what
              does each encode about a token?
  TF-TH-0002  Why scale QK^T by 1/sqrt(d_k)? What happens without it
              (variance growth, softmax saturation)?
  TF-TH-0003  Why multiple heads? What does each head get to specialize
              on (syntax vs position vs coreference)?
  TF-TH-0004  What is a causal mask and why is it mandatory in a
              decoder-only language model?
  TF-TH-0005  Positional encoding: why is attention permutation-invariant
              without it? Sinusoidal vs learned vs RoPE vs ALiBi.
  TF-TH-0006  RoPE: how does it encode relative positions via rotation,
              and why is it preferred in modern LLMs?
  TF-TH-0007  Transformer block anatomy: attention -> add&norm -> FFN ->
              add&norm. Why pre-norm vs post-norm, and why did pre-norm
              win?
  TF-TH-0008  FFN: why 4x expansion? GELU vs SwiGLU: what does gating
              buy? Compute the FFN parameter count.
  TF-TH-0009  Encoder vs decoder vs encoder-decoder: attention patterns,
              objectives, and canonical models (BERT/GPT/T5).
  TF-TH-0010  Why is self-attention O(T^2)? Where does the quadratic
              term come from (scores matrix)?
  TF-TH-0011  What is the residual stream interpretation of a
              transformer (tokens as a shared highway, layers as
              read/write)?
  TF-TH-0012  MHA vs MQA vs GQA: what is shared, and why does GQA cut KV
              memory at little quality cost?
  TF-TH-0013  What is the LM head, and why does the final hidden state
              get projected to vocab logits?
WHY QUESTIONS
  TF-TH-0014  Why can attention look at ANY token, while RNNs compress
              history into one hidden state?
  TF-TH-0015  Why does a deeper transformer need LayerNorm/RMSNorm for
              stability, and what does RMSNorm remove?
  TF-TH-0016  Why did models move from learned positional embeddings to
              RoPE for long context?
  TF-TH-0017  Why do transformers need a much bigger batch/parallelism
              budget to train efficiently vs RNNs?
  TF-TH-0018  Why is the FFN called a 'key-value memory' - what does a
              token 'retrieve' from it?
MATHEMATICAL
  TF-MATH-0001  Derive scaled dot-product attention fully: scores =
              QK^T/sqrt(d_k), softmax, output. Show all shapes.
  TF-MATH-0002  Prove why scaling by sqrt(d_k) keeps score variance ~1
              when Q,K are unit-variance.
  TF-MATH-0003  Parameter count of a transformer block: d=768, ff=3072,
              h=12, d_k=64: compute attention, FFN, norms, total.
  TF-MATH-0004  Compute the O(T^2 d) FLOPs and O(T^2) memory of
              attention at T=4096, d=768.
  TF-MATH-0005  GQA with 32 heads / 8 KV heads: how many KV pairs vs
              MHA, and the memory ratio.
  TF-MATH-0006  RoPE: derive the rotation applied to (x_0, x_1) pairs
              and show it makes the inner product depend only on the
              relative offset.
  TF-MATH-0007  Pre-norm vs post-norm: write both formulations of the
              block and show where the residual branch sits.
  TF-MATH-0008  SwiGLU: write the gated FFN (SiLU(W_g x) * W_u x) and
              its parameter count vs standard FFN (note the 2/3 rule).
CODING
  TF-CODE-0001  [L3] Implement scaled dot-product attention in torch
              with a causal mask, verified on a toy example.
  TF-CODE-0002  [L4] Implement MultiHeadAttention from scratch (QKV
              projections, heads, concat, output proj) and verify against
              nn.MultiheadAttention.
  TF-CODE-0003  [L4] Implement sinusoidal positional encoding and add it
              to embeddings; verify the distance property.
  TF-CODE-0004  [L5] Implement RoPE from scratch and verify that
              attention scores depend only on relative position.
  TF-CODE-0005  [L5] Implement a full transformer block (pre-norm,
              GQA or MHA, SwiGLU) and check output shapes.
  TF-CODE-0006  [L6] Implement a mini decoder-only GPT with KV cache
              support, and verify cached == recomputed logits.
  TF-CODE-0007  [L7] Implement FlashAttention-style tiling on CPU to
              show the IO argument (blocked softmax with running max/sum).
DEBUGGING
  TF-DEBUG-0001  Attention outputs are identical for all tokens. What
              does that say about the softmax (saturated)? Scale check.
  TF-DEBUG-0002  A causal mask built as (T, T) with -inf leaks the
              future by one position. Where is the off-by-one?
  TF-DEBUG-0003  Training diverges only at depth > 12. Pre-norm vs
              post-norm and init scale - which do you check first?
  TF-DEBUG-0004  KV cache gives different outputs than full forward at
              the 10th decoded token. What is the caching bug class?
  TF-DEBUG-0005  RoPE applied to K but not Q - what happens to attention
              scores, and how do you notice?
  TF-DEBUG-0006  After a head-count change, load fails with a size
              mismatch on the O projection. What is the math of the
              mismatch?
  TF-DEBUG-0007  Long-context inference OOMs at 8k. Which tensor is
              quadratic and what are the fixes (chunk, FlashAttn, GQA)?
CODE REVIEW
  TF-REVIEW-0001  Review: attention scores computed in fp32 but Q,K in
              fp16 - is that a bug? What about the reverse?
  TF-REVIEW-0002  Review: the mask added as zeros instead of -inf, with
              softmax never touching it.
  TF-REVIEW-0003  Review: hidden size 768, 12 heads - d_k=64 correct?
              What breaks if heads doesn't divide d?
  TF-REVIEW-0004  Review: applying dropout to attention weights at
              inference (no eval mode).
INTERVIEW
  TF-IV-0001  Derive attention on a whiteboard and explain why each
              scaling constant exists.
  TF-IV-0002  Why does GPT use a decoder-only stack? What does the
              objective become, and what can't it do that an
              encoder-decoder can?
  TF-IV-0003  Your model has 8k context and must serve 64k. List the
              options with trade-offs (RoPE scaling, sliding window,
              chunking, GQA, FlashAttention).
  TF-IV-0004  Explain the residual stream interpretation - why do
              researchers say attention reads and writes to it?
  TF-IV-0005  Why is pre-norm more stable but slightly worse in final
              quality (in some settings)? What does the norm placement
              change about gradient flow?
PROJECTS
  TF-PROJ-0001  [D] Attention from scratch with a visualization of the
                attention matrix.
  TF-PROJ-0002  [C] A mini transformer trained on a tiny language task
                (char-level), with loss curves and samples.
  TF-PROJ-0003  [B] Reimplement a small pretrained transformer head
                (e.g. GPT-2-small sized) on a cleaned corpus and evaluate
                perplexity vs the original.
  TF-PROJ-0004  [A] Long-context project: extend a small model with RoPE
                scaling + FlashAttention-style chunking and measure the
                effective context gain.
RESEARCH
  TF-RES-0001  Design an experiment on head specialization: ablate
              heads, measure task-specific drops, and interpret.
  TF-RES-0002  Compare MHA vs GQA quality at matched inference memory on
              a downstream task - design the matched budget.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 15 - LLMs (LANGUAGE MODELING + GENERATION + ALIGNMENT)
================================================================================
THEORY / CONCEPTUAL
  LLM-TH-0001  What is a language model? Define P(x1..xn) and derive the
              autoregressive factorization.
  LLM-TH-0002  What is next-token prediction, and why is it sufficient
              (with enough scale) for so many tasks?
  LLM-TH-0003  Logits vs probabilities: what is a logit, why do we keep
              them raw, and what does softmax add?
  LLM-TH-0004  Temperature: what does T>1 vs T<1 do to the distribution,
              and why does T->0 behave like argmax?
  LLM-TH-0005  Top-k vs top-p: what does each truncate, and why is
              top-p (nucleus) often preferred?
  LLM-TH-0006  Perplexity: define it from cross-entropy and explain why
              it is exp(loss). Limitations (in-domain, length bias).
  LLM-TH-0007  What is pretraining vs instruction tuning vs alignment
              (SFT -> RLHF/DPO)? What does each stage change about
              behavior?
  LLM-TH-0008  Why does pretraining need trillions of tokens? What is
              the scaling-law framing (loss ~ f(N, D, C))?
  LLM-TH-0009  What is the knowledge cutoff, and why does the model
              'know' facts only from its training distribution?
  LLM-TH-0010  Hallucination: what are its three families (factual,
              reasoning, instruction-following) and why does the LM have
              no internal truth checker?
  LLM-TH-0011  What is in-context learning vs fine-tuning? Why can a
              prompt teach a task the weights never saw?
  LLM-TH-0012  Context window: what limits it (positional encoding,
              attention memory), and what happens when input exceeds it?
  LLM-TH-0013  What is alignment, and why is a base model often unsafe/
              unhelpful? What does RLHF change that SFT cannot?
  LLM-TH-0014  DPO vs RLHF: what does DPO eliminate (reward model,
              PPO), and what is the closed-form preference objective?
WHY QUESTIONS
  LLM-TH-0015  Why does a bigram model plateau, and why does scaling BOTH
              parameters and data move the loss curve down?
  LLM-TH-0016  Why is greedy decoding deterministic but often worse than
              sampling? (mode collapse, repetition)
  LLM-TH-0017  Why can sampling produce contradictions and repetition,
              and what do frequency/presence penalties change?
  LLM-TH-0018  Why do LLMs fail on arithmetic unless trained/tool-aug-
              mented? What does tokenization do to numbers?
  LLM-TH-0019  Why does an LLM confidently state false facts, and why is
              calibration on open-ended text hard to fix?
  LLM-TH-0020  Why is benchmark improvement not the same as capability
              improvement (contamination, saturation, gaming)?
MATHEMATICAL
  LLM-MATH-0001  Given logits [2, 1, 0.1], compute softmax probs at T=1
              and T=2; show how the ratio of two probs changes.
  LLM-MATH-0002  Perplexity: loss 2.5 -> ppl? How many tokens does a
              model need to halve its ppl (assuming scaling law)?
  LLM-MATH-0003  Compute the probability of a 20-token sentence under
              a model given per-token probs; why use log domain?
  LLM-MATH-0004  Top-p: given probs [0.4, 0.3, 0.15, 0.1, 0.05], which
              tokens survive p=0.9? p=0.7?
  LLM-MATH-0005  Cross-entropy loss for a single next-token example
              where the model assigns 0.01 to the true token - compute
              and interpret.
  LLM-MATH-0006  Estimate training compute for a 7B model over 2T tokens
              (FLOPs ~ 6*N*D) - show the number.
  LLM-MATH-0007  KV cache for a 7B decoder at 4k context (32 layers,
              32 heads, d_head 128, fp16) - compute GB.
  LLM-MATH-0008  Temperature equivalence: show sampling from softmax(
              logits/T) as T->0 approaches greedy.
CODING
  LLM-CODE-0001  [L2] Implement temperature sampling from a logits
              vector with numpy, including T->0 handling.
  LLM-CODE-0002  [L3] Implement top-k and top-p sampling and verify the
              distribution shapes on a toy logits vector.
  LLM-CODE-0003  [L4] Implement a greedy + sampling generation loop for a
              small model with repetition penalty.
  LLM-CODE-0004  [L4] Compute perplexity for a small model on a corpus
              and compare vs a bigram baseline.
  LLM-CODE-0005  [L5] Implement log-softmax + NLL training loop for a
              mini GPT on a char corpus (teacher-forced labels shifted).
  LLM-CODE-0006  [L6] Implement beam search with a length penalty and
              compare quality vs sampling.
  LLM-CODE-0007  [L7] Implement speculative decoding (draft + verify) on
              a small pair of models and measure the acceptance rate.
DEBUGGING
  LLM-DEBUG-0001  The model repeats 'the the the...' - which decoding
              knob fixes it and why?
  LLM-DEBUG-0002  Temperature 0 still produces different outputs across
              runs. Where is the nondeterminism (sampling argmax ties)?
  LLM-DEBUG-0003  Perplexity drops but downstream QA gets worse. What
              does ppl not capture?
  LLM-DEBUG-0004  The model answers in the wrong language. What is the
              likely training-data or prompt cause?
  LLM-DEBUG-0005  A math prompt works at temperature 0 but fails at 1.0.
              Explain with the probability mass story.
  LLM-DEBUG-0006  Context window exceeded -> garbage. What are the three
              mitigations (truncation, summarization, chunking)?
  LLM-DEBUG-0007  The model sees a leaked answer in the prompt via
              retrieval - how do you detect and fix contamination?
CODE REVIEW
  LLM-REVIEW-0001  Review: passing user input directly into the system
              prompt (prompt injection surface).
  LLM-REVIEW-0002  Review: computing ppl on the training set and
              reporting it as model quality.
  LLM-REVIEW-0003  Review: a benchmark with questions the model was
              fine-tuned on.
  LLM-REVIEW-0004  Review: no max_tokens bound on a generation endpoint.
INTERVIEW
  LLM-IV-0001  Explain why scaling laws exist and what they do NOT
              predict (capabilities, safety, emergent jumps).
  LLM-IV-0002  You have to reduce hallucinations in a customer-support
              LLM. Order your interventions (RAG, prompt, fine-tune,
              eval, guardrails) and justify.
  LLM-IV-0003  When would you fine-tune instead of prompt-engineer? Give
              the cost/benefit line.
  LLM-IV-0004  What is the difference between a base model and a chat
              model, technically (templates, chat tokens, alignment)?
  LLM-IV-0005  Your CEO wants the LLM to do math reliably. Design the
              solution (tool use, few-shot, fine-tune, verification).
SCENARIO
  LLM-SCEN-0001  Model answers customer questions with confident
              fabrications. Design a mitigation stack with evaluation.
  LLM-SCEN-0002  A fine-tuned model forgot its base knowledge (catastrophic
              forgetting). Diagnose and design the fix.
  LLM-SCEN-0003  You must pick one of 3 LLMs for a cost-sensitive
              product. Design the evaluation that decides.
PROJECTS
  LLM-PROJ-0001  [D] Bigram + trigram language model with sampling.
  LLM-PROJ-0002  [C] Mini GPT trained on a real corpus with sampling
                during training (like mini_gpt_lab.py).
  LLM-PROJ-0003  [B] Generation-quality lab: temperature/top-k/top-p
                sweeps with automatic quality metrics.
  LLM-PROJ-0004  [A] Eval harness: perplexity + benchmark + LLM-as-judge
                on 3 open models with honest reporting.
RESEARCH
  LLM-RES-0001  Design an experiment on the temperature-quality curve:
              does optimal T vary by task? Measure and hypothesize.
  LLM-RES-0002  Design a contamination test: how do you detect benchmark
              overlap in a training corpus?
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 16 - LLM ENGINEERING (APIS + PROMPTING + STREAMING)
================================================================================
THEORY / CONCEPTUAL
  LE-TH-0001  What is an LLM API request/response cycle? What are the
              common parameters (temperature, max_tokens, top_p, stop)?
  LE-TH-0002  What is structured output / function calling, and why is
              schema-validated JSON better than 'return JSON'?
  LE-TH-0003  What is a chat template (system/user/assistant roles)? Why
              does chat formatting differ from raw text?
  LE-TH-0004  What is streaming, and why does time-to-first-token matter
              for UX? What are SSE vs WebSocket trade-offs?
  LE-TH-0005  What is prompt injection, and why is it different from
              other injection classes (indirect via retrieved docs)?
  LE-TH-0006  What is model routing (small/cheap vs big/expensive)? What
              signals decide the route?
  LE-TH-0007  What is a token budget, and why does prompt+completion
              length drive cost nonlinearly?
  LE-TH-0008  What is an LLM cache (exact/prefix/semantic), and what
              risks does caching answers introduce?
WHY QUESTIONS
  LE-TH-0009  Why does retrying with backoff improve reliability, and
              what happens with no retry on a rate limit?
  LE-TH-0010  Why is JSON mode not a guarantee of correctness, and what
              does validation + retry add?
  LE-TH-0011  Why should prompts be versioned like code? What does a
              prompt registry buy you?
  LE-TH-0012  Why is 'ask the model to check its own answer' unreliable,
              and what tools (code exec, retrieval, verification) actually
              reduce error?
  LE-TH-0013  Why do you need an eval set for prompt changes? What does
              a one-off 'seems better' miss?
  LE-TH-0014  Why is measuring tokens/requests/cost per user essential
              for a paid product?
COMPARISON
  LE-TH-0015  Prompt engineering vs few-shot vs fine-tuning vs RAG: cost,
              latency, quality, maintenance.
  LE-TH-0016  Synchronous vs streaming vs async response: UX, infra,
              failure handling.
  LE-TH-0017  LLM-as-judge vs human eval vs benchmark eval: when each is
              valid and their failure modes.
  LE-TH-0018  Structured output via JSON mode vs function calling vs
              output-constrained decoding.
CODING
  LE-CODE-0001  [L2] Write a typed LLM client wrapper: call, timeout,
              retry with jitter, structured error classes.
  LE-CODE-0002  [L3] Implement a streaming chat endpoint (SSE) that
              forwards tokens and handles client disconnect.
  LE-CODE-0003  [L4] Implement function calling: schema -> model picks a
              tool -> execute -> feed result back for a final answer.
  LE-CODE-0004  [L4] Build a prompt-version registry (hash, template,
              eval results) with rollback.
  LE-CODE-0005  [L5] Implement a token/cost meter: per-request tokens,
              cost, per-user totals, alerts on budget.
  LE-CODE-0006  [L5] Implement semantic caching (embedding similarity
              threshold) with a correctness guard.
  LE-CODE-0007  [L6] Implement a model router: classify request difficulty
              -> route to small/big model -> fallback on failure.
  LE-CODE-0008  [L6] Implement an LLM-as-judge harness with rubric,
              sampling, and agreement stats vs human labels.
DEBUGGING
  LE-DEBUG-0001  The model intermittently returns invalid JSON. List the
              layers to fix (schema, temperature, retry, validation).
  LE-DEBUG-0002  Latency spikes to 30s for short prompts. Where do you
              look (TTFT vs TPS, batching, queue, provider)?
  LE-DEBUG-0003  Streaming stops mid-sentence at the same point. What
              server-side limit is at play?
  LE-DEBUG-0004  Costs doubled with no traffic change. What telemetry do
              you need (tokens per call, prompt sizes, cache hits)?
  LE-DEBUG-0005  A cached answer is served for a NEW question (semantic
              cache false positive). How do you audit and guard?
  LE-DEBUG-0006  Function calling returns a tool argument that crashes
              the executor (untrusted input). Where is the validation gap?
CODE REVIEW
  LE-REVIEW-0001  Review: user text interpolated into the prompt with
              f-strings and no delimiters/sanitization.
  LE-REVIEW-0002  Review: API key stored in the frontend bundle.
  LE-REVIEW-0003  Review: unbounded max_tokens with a queue; what is the
              DoS vector?
  LE-REVIEW-0004  Review: eval on 3 hand-picked prompts reported as
              'improved quality'.
  LE-REVIEW-0005  Review: no logging of prompts/responses (compliance and
              debugging gap).
INTERVIEW
  LE-IV-0001  Design an LLM API platform for 1M requests/day: auth,
              routing, caching, rate limits, cost, observability.
  LE-IV-0002  How do you make an LLM reliably output a fixed schema?
              Compare every technique available.
  LE-IV-0003  A customer says answers are wrong 20% of the time. How do
              you measure 'wrong' and triage the causes?
  LE-IV-0004  How do you A/B test a prompt change without leaking?
  LE-IV-0005  What is your incident response for a provider outage?
              (fallbacks, degradation, caching, queueing)
SCENARIO
  LE-SCEN-0001  Your startup's bill is $40k/month on LLM calls. Design a
              cost-reduction plan with an order of operations.
  LE-SCEN-0002  A competitor's model is cheaper and as good on your
              tests. How do you decide whether to switch? (eval design)
PROJECTS
  LE-PROJ-0001  [C] Chat API with streaming, retries, rate limits, and
                structured error responses.
  LE-PROJ-0002  [B] Tool-calling assistant with a calculator + search
                tools, schema validation, and error handling.
  LE-PROJ-0003  [A] Production LLM gateway: routing, caching, cost
                metering, evals on every prompt change.
  LE-PROJ-0004  [S] Multi-tenant LLM platform: per-tenant quotas, prompt
                isolation, compliance logging, dashboards.
RESEARCH
  LE-RES-0001  Design a study of cache hit rate vs embedding threshold -
              quality vs cost curve with real traffic replay.
  LE-RES-0002  Measure the actual quality/cost trade-off of routing: is
              a 5% error increase worth a 10x cost cut? How do you decide
              with an eval?
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 17 - FINE-TUNING + PEFT (LoRA / QLoRA / PREFIX / ADAPTERS)
================================================================================
THEORY / CONCEPTUAL
  FT-TH-0001  Full fine-tuning vs PEFT: what changes about weights,
              memory, and deployment artifacts?
  FT-TH-0002  What is catastrophic forgetting, and why does continued
              fine-tuning on one domain damage others?
  FT-TH-0003  Instruction tuning vs continued pretraining vs chat
              adaptation: what does each change about the model?
  FT-TH-0004  What is LoRA exactly? W' = W + (alpha/r) BA - explain
              every symbol and why B,A are low rank.
  FT-TH-0005  Why does LoRA reduce optimizer memory (Adam states) far
              more than it reduces forward compute?
  FT-TH-0006  What is QLoRA? What is NF4, and why quantize the base while
              keeping adapters fp16?
  FT-TH-0007  What is a soft prompt (prefix tuning), and how does it
              differ from full LoRA in parameter count?
  FT-TH-0008  What is an adapter layer (bottleneck), and how does it
              compare to LoRA's additive form?
  FT-TH-0009  What is loss masking in instruction tuning, and why must
              the prompt portion be masked?
  FT-TH-0010  What is the difference between training data for SFT vs
              preference data for DPO? Give one line each.
WHY QUESTIONS
  FT-TH-0011  Why does LoRA often match full fine-tuning on small
              domains? (low-rank update hypothesis)
  FT-TH-0012  Why does full fine-tuning overfit small instruction sets
              while LoRA generalizes better (implicit regularization)?
  FT-TH-0013  Why is data quality > data quantity in fine-tuning? What
              does 1000 clean examples do that 100k noisy ones don't?
  FT-TH-0014  Why do you need an eval set that the fine-tune never saw,
              and why must it be diverse (not just the same domain)?
  FT-TH-0015  Why does merging LoRA weights back (W + BA) change nothing
              mathematically but everything for deployment?
MATHEMATICAL
  FT-MATH-0001  LoRA param count: W is (4096, 4096), r=16: parameters in
              A and B, vs full FT - compute the ratio.
  FT-MATH-0002  Memory: full FT of a 7B model with Adam in fp16 vs LoRA
              r=16 - estimate both (params, grads, optimizer states).
  FT-MATH-0003  QLoRA: base in 4-bit (7B) + LoRA fp16 - estimate total
              memory vs 7B fp16 full FT.
  FT-MATH-0004  Derive why the LoRA update is low rank: rank(W+BA) <= r
              + rank(W) and what that implies about expressivity.
  FT-MATH-0005  Scaling: with alpha/r, show that doubling r and keeping
              alpha fixes the update scale.
  FT-MATH-0006  Loss masking: given a prompt+response sequence, write
              the mask that zeroes prompt losses - and the gradient
              effect.
CODING
  FT-CODE-0001  [L3] Implement a LoRA layer by hand (nn.Module with A/B)
              and verify it trains on a toy task.
  FT-CODE-0002  [L4] Implement LoRA on a small model: freeze base, train
              adapters, merge/unmerge, verify identical outputs after
              merge.
  FT-CODE-0003  [L4] Implement loss masking for instruction tuning
              (prompt tokens ignored in the loss).
  FT-CODE-0004  [L5] Build an SFT pipeline: dataset -> chat template ->
              packing -> training -> eval, with a holdout domain.
  FT-CODE-0005  [L6] Implement DPO on a small model with a preference
              dataset (compute the implicit reward loss).
  FT-CODE-0006  [L6] QLoRA-style pipeline: 4-bit base + fp16 adapters,
              verify adapter-only training works.
DEBUGGING
  FT-DEBUG-0001  After fine-tuning, the model forgot its base knowledge
              (+0.7 nats on the old domain). What metric proves it and
              what are the fixes?
  FT-DEBUG-0002  Fine-tuned model repeats training answers verbatim on
              new questions. What is it doing (memorization) and how do
              you measure it?
  FT-DEBUG-0003  Loss decreases but eval worsens after 1 epoch. What are
              the top-3 causes (overfit, template mismatch, eval leak)?
  FT-DEBUG-0004  The chat template was applied twice in the pipeline -
              what does the model output look like?
  FT-DEBUG-0005  Merging LoRA changes outputs slightly. Is that a bug?
              (fp16 rounding) How do you verify the merge math?
  FT-DEBUG-0006  QLoRA trains slower than expected. What is the 4-bit
              compute overhead, and what fixes exist (compute dtype)?
CODE REVIEW
  FT-REVIEW-0001  Review: fine-tuning on data that includes the eval
              set.
  FT-REVIEW-0002  Review: no loss mask - the model learns to copy the
              prompt.
  FT-REVIEW-0003  Review: LoRA applied to embeddings and LM head with
              r=1024 - is that still 'efficient'?
  FT-REVIEW-0004  Review: one epoch over 10 examples reported as a
              fine-tuned model.
INTERVIEW
  FT-IV-0001  When is fine-tuning the wrong tool vs RAG vs prompting?
              Give the decision tree.
  FT-IV-0002  How does LoRA interact with quantization (QLoRA)? Walk
              through memory at each stage.
  FT-IV-0003  Your fine-tune improves the target domain but breaks
              everything else. Design the fix (data mix, LoRA rank,
              eval suite, rehearsal).
  FT-IV-0004  What does a good fine-tuning eval set look like? Design
              one for a customer-support tone+knowledge model.
  FT-IV-0005  Explain why the rank of LoRA matters and how you'd choose
              it empirically.
SCENARIO
  FT-SCEN-0001  You must teach a model a new writing style without losing
              its general ability. Design the dataset and training plan.
  FT-SCEN-0002  Budget allows one fine-tune of a 7B model on 2x 24 GB
              GPUs. Walk through the exact memory plan.
PROJECTS
  FT-PROJ-0001  [C] LoRA fine-tune of a small model on a domain
                adaptation task (like lora_finetune_lab.py) with
                forgetting metrics.
  FT-PROJ-0002  [B] Instruction-tuning pipeline with loss masking,
                template handling, and a held-out eval set.
  FT-PROJ-0003  [A] Preference optimization project: dataset -> DPO vs
                SFT comparison with win-rate evaluation.
  FT-PROJ-0004  [S] Production fine-tuning platform: data versioning,
                reproducible training, eval gates, model registry.
RESEARCH
  FT-RES-0001  Design an experiment: does LoRA rank interact with task
              complexity? Sweep r on easy/hard tasks.
  FT-RES-0002  Measure catastrophic forgetting vs data ratio: how much
              old-domain data in the mix prevents it? Find the curve.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 18 - QUANTIZATION + INFERENCE OPTIMIZATION
================================================================================
THEORY / CONCEPTUAL
  QI-TH-0001  Why quantize? What is the accuracy vs memory/latency
              trade-off curve for FP16/BF16/INT8/INT4?
  QI-TH-0002  BF16 vs FP16: range vs precision - why is BF16 preferred
              for training and often fine for inference?
  QI-TH-0003  What is post-training quantization (PTQ) vs
              quantization-aware training (QAT)? When is QAT necessary?
  QI-TH-0004  Weight-only quantization vs weight+activation quantization:
              why is weight-only so much easier?
  QI-TH-0005  What is GPTQ (second-order, layer-wise) vs AWQ (activation-
              aware scaling) vs GGUF (k-quants)? One line each.
  QI-TH-0006  What is KV cache and why is it the memory bottleneck at
              long context? Where do MQA/GQA cut it?
  QI-TH-0007  Prefill vs decode: why is prefill compute-bound and decode
              memory-bandwidth-bound?
  QI-TH-0008  What is continuous batching, and why does it beat static
              batching for LLM serving?
  QI-TH-0009  What is speculative decoding? Draft, verify, accept -
              why can it be lossless?
  QI-TH-0010  What is FlashAttention? Why does it change speed without
              changing the math (IO awareness)?
  QI-TH-0011  What is paged attention / PagedAttention? What OS concept
              does it borrow?
  QI-TH-0012  What is prefix caching, and when does it save real cost?
WHY QUESTIONS
  QI-TH-0013  Why does INT4 activation quantization lose more than INT4
              weights? (outlier channels, dynamic ranges)
  QI-TH-0014  Why does quantization hurt small models more than large
              ones? (parameter redundancy)
  QI-TH-0015  Why is the decode step bandwidth-bound? Show the math:
              bytes moved per token vs FLOPs per token.
  QI-TH-0016  Why does batch size improve throughput but not latency,
              and what does continuous batching do to that trade-off?
  QI-TH-0017  Why can speculative decoding give >1x speedup without
              changing outputs? What must hold for acceptance?
MATHEMATICAL
  QI-MATH-0001  Memory: 7B params in FP32/FP16/INT8/INT4 - compute GB
              each (no overhead).
  QI-MATH-0002  KV cache: 32 layers, 32 heads, d_head=128, fp16: bytes
              per token; size at 4k / 128k context.
  QI-MATH-0003  Decode bandwidth: a 7B model at fp16 - bytes per token
              vs the 13 GB/s CPU / 2 TB/s GPU bandwidth; compute max
              tokens/sec.
  QI-MATH-0004  Quantization error: uniform 8-bit rounding of a weight
              with range [-2, 2] - max absolute error; relative error for
              a small weight.
  QI-MATH-0005  Speculative decoding: gamma=4, acceptance 0.7 - expected
              forwards per token and the speedup bound.
  QI-MATH-0006  Arithmetic intensity of attention: FLOPs/bytes at
              T=4096, d=128, fp16 - compute and classify the regime.
CODING
  QI-CODE-0001  [L3] Implement symmetric vs asymmetric INT8 quantization
              (scale/zero-point) and measure MSE error.
  QI-CODE-0002  [L4] Implement weight-only INT4 (group-wise) quantization
              and dequantized inference, report accuracy drop on a task.
  QI-CODE-0003  [L4] Implement a KV cache with GQA-style sharing and
              verify logit equivalence with full forward.
  QI-CODE-0004  [L5] Implement continuous batching on a toy server
              (schedule requests across steps) and measure throughput.
  QI-CODE-0005  [L5] Implement speculative decoding on a small draft/
              target pair and measure wall-clock vs plain decode.
  QI-CODE-0006  [L6] Implement online softmax (running max/sum) for a
              chunked attention forward - the FlashAttention core.
DEBUGGING
  QI-DEBUG-0001  INT8 model quality collapses on one task but not others.
              What layers are sensitive, and how do you find them?
  QI-DEBUG-0002  Quantized model is SLOWER than fp16 on your GPU. What
              is the classic cause (no INT8 kernels, dequant overhead)?
  QI-DEBUG-0003  KV cache OOMs at 32k context on a 24 GB GPU. Compute
              the budget and list fixes in order.
  QI-DEBUG-0004  Speculative decoding produces DIFFERENT outputs than
              greedy. What invariant did you break (sampling with draft)?
  QI-DEBUG-0005  Latency is fine but throughput tanks under load. What
              is the batching/queue interplay, and what metrics separate
              them?
  QI-DEBUG-0006  BF16 outputs differ from FP32 by 1e-3 - is this a bug
              or expected? How do you set a tolerance correctly?
CODE REVIEW
  QI-REVIEW-0001  Review: quantizing with a calibration set that
              includes the eval data.
  QI-REVIEW-0002  Review: dequantizing to fp32 before every matmul (the
              perf anti-pattern).
  QI-REVIEW-0003  Review: KV cache evicted by FIFO with no attention
              consideration.
  QI-REVIEW-0004  Review: reporting only memory savings, not the
              accuracy delta, for a quantized model.
INTERVIEW
  QI-IV-0001  You must serve a 70B model on 4x 80 GB GPUs. Walk through
              the memory budget and the techniques you stack.
  QI-IV-0002  Why is decode memory-bandwidth-bound? Give the numbers for
              a 7B model.
  QI-IV-0003  When would you choose QAT over PTQ? Give a concrete case.
  QI-IV-0004  How does continuous batching change the throughput/latency
              curve? Sketch the trade-off.
  QI-IV-0005  What is the difference between vLLM-style paging and naive
              KV preallocation, and when does it matter?
PROJECTS
  QI-PROJ-0001  [C] Quantization lab: FP16/BF16/INT8/INT4 with accuracy
                and memory reports (like quantization experiments).
  QI-PROJ-0002  [B] KV-cache inference engine: prefill + cached decode
                with a correctness check vs recompute.
  QI-PROJ-0003  [A] Speculative decoding on a small model pair with
                acceptance-rate analysis.
  QI-PROJ-0004  [S] Production serving stack: batching + prefix cache +
                quantized weights, benchmarked under load.
RESEARCH
  QI-RES-0001  Design a study of quantization sensitivity per layer:
              which layers need higher precision? Propose a mixed-precision
              scheme.
  QI-RES-0002  Compare speculative decoding draft strategies (same model
              shallow vs small model) on acceptance rate and wall-clock.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 19 - EMBEDDINGS
================================================================================
THEORY / CONCEPTUAL
  EM-TH-0001  What is an embedding? How does a dense vector encode
              semantics, and why is distance meaningful?
  EM-TH-0002  Cosine similarity vs dot product vs Euclidean: what does
              normalization change, and when does each mislead?
  EM-TH-0003  How are sentence/document embeddings built from token
              embeddings (pooling, CLS, mean, last-token)?
  EM-TH-0004  What is the difference between a bi-encoder (embedding
              model) and a cross-encoder (reranker)? Why can't you scale
              a cross-encoder to a corpus?
  EM-TH-0005  What makes a good embedding model for retrieval (contrastive
              training, hard negatives, matryoshka dims)?
  EM-TH-0006  What is embedding dimensionality, and why do 384/768/1536
              dims exist? What does dimensionality cost at scale?
  EM-TH-0007  What is embedding drift? Why can embeddings change between
              model versions and break your vector DB?
  EM-TH-0008  What is a multimodal embedding? How do text and image
              share one space?
WHY QUESTIONS
  EM-TH-0009  Why do you need to normalize embeddings before cosine
              search (or use dot product on normalized vectors)?
  EM-TH-0010  Why is cosine similarity weak at capturing 'is-a' vs
              'part-of' relations? What does that mean for retrieval?
  EM-TH-0011  Why can two models produce very different embeddings for
              the same sentence, and why does that break cross-version
              search?
  EM-TH-0012  Why do short queries need different handling than long
              documents in embedding retrieval?
MATHEMATICAL
  EM-MATH-0001  Cosine similarity of two 2D vectors: compute for (1,0)
              and (0.5, 0.866) - what is the angle?
  EM-MATH-0002  Show that for normalized vectors, dot product equals
              cosine and rank order is preserved.
  EM-MATH-0003  Matryoshka: 768-dim -> 384-dim - why can you truncate
              and keep most quality? (dimension ordering property)
  EM-MATH-0004  Contrastive loss: write InfoNCE for a batch and explain
              the temperature's role.
CODING
  EM-CODE-0001  [L2] Implement cosine-similarity search over a matrix
              (B, d) x (C, d) with top-k, no external DB.
  EM-CODE-0002  [L3] Train a small contrastive embedding model on a toy
              dataset (pairs + negatives) and measure retrieval hit@k.
  EM-CODE-0003  [L4] Implement a hard-negative mining loop and measure
              the embedding quality gain.
  EM-CODE-0004  [L5] Build an embedding evaluation: hit@k, MRR on a
              labeled query-doc set, with baselines (TF-IDF, BM25).
  EM-CODE-0005  [L6] Implement matryoshka truncation eval: quality vs
              dimension curve.
DEBUGGING
  EM-DEBUG-0001  All embeddings are nearly identical (cosine ~0.99).
              What causes collapsed embeddings and how do you verify?
  EM-DEBUG-0002  Retrieval is great on train pairs but fails on real
              queries. What distributional gap is likely?
  EM-DEBUG-0003  After re-embedding with a new model, old vectors and
              new vectors are incompatible. What do you do at scale?
  EM-DEBUG-0004  Cosine similarities come out negative for everything.
              What preprocessing is missing?
CODE REVIEW
  EM-REVIEW-0001  Review: query embedded with model A, corpus with
              model B.
  EM-REVIEW-0002  Review: unnormalized embeddings + cosine - is it a bug
              or equivalent?
  EM-REVIEW-0003  Review: evaluating hit@k on queries the embedder was
              trained on.
INTERVIEW
  EM-IV-0001  How do you choose an embedding model for a multilingual
              RAG? What eval would decide?
  EM-IV-0002  Why is 768-dim too expensive for a 100M-doc corpus? What
              do you do (dims, product quantization, two-stage)?
  EM-IV-0003  When would you prefer sparse (BM25) over dense embeddings,
              and what does hybrid fix?
PROJECTS
  EM-PROJ-0001  [D] Embedding playground: encode sentences, show
                neighbors + analogies.
  EM-PROJ-0002  [C] Embedding-based retrieval on a document set with
                hit@k/MRR eval vs lexical baselines.
  EM-PROJ-0003  [B] Fine-tune an embedding model on domain data with
                hard negatives and measure the gain.
  EM-PROJ-0004  [A] Embedding service: batch encoding API, caching,
                versioning, drift monitoring.
RESEARCH
  EM-RES-0001  Design an experiment on embedding collapse: what
              regularization/temperature prevents it?
  EM-RES-0002  Compare pooling strategies (mean/CLS/matryoshka) on
              retrieval quality with a fixed encoder.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 20 - INFORMATION RETRIEVAL
================================================================================
THEORY / CONCEPTUAL
  IR-TH-0001  What is retrieval? How is it different from search ranking,
              and where do the two overlap?
  IR-TH-0002  What is an inverted index? Why is it the backbone of
              lexical search, and what is its space cost?
  IR-TH-0003  TF-IDF vs BM25: what does BM25 add (saturation, document
              length normalization)?
  IR-TH-0004  What is dense retrieval? How does it differ from lexical in
              matching (semantic vs exact)?
  IR-TH-0005  What is hybrid retrieval, and why does it beat either alone
              (complementary failure modes)?
  IR-TH-0006  What is reranking, and why is a two-stage (cheap recall,
              expensive precision) design standard?
  IR-TH-0007  What is query expansion / query rewriting, and when does it
              help vs hurt?
  IR-TH-0008  What is a recall@k vs precision@k vs MRR vs NDCG? Write
              each definition.
WHY QUESTIONS
  IR-TH-0009  Why does BM25 still beat dense embeddings on exact-match
              and out-of-domain queries?
  IR-TH-0010  Why can't you rerank 10M documents with a cross-encoder?
              What is the cost curve?
  IR-TH-0011  Why does chunking size change retrieval quality, and what
              is the recall-precision tension?
  IR-TH-0012  Why does retrieval evaluation need labeled relevance, and
              what do proxy metrics (hit@k on generation) hide?
MATHEMATICAL
  IR-MATH-0001  Write the BM25 score formula and explain each term's
              role (IDF, saturation, length norm).
  IR-MATH-0002  Compute IDF for a term in 1,000 docs appearing in 10.
  IR-MATH-0003  Compute NDCG@3 for a ranking with graded relevance.
  IR-MATH-0004  Show the ANN-vs-exact trade-off: what does HNSW's
              recall/query-time curve look like, and what do M and
              efConstruction control?
  IR-MATH-0005  Multi-stage pipeline: retrieve 100, rerank to 10 - how
              many cross-encoder calls vs dense calls per query?
CODING
  IR-CODE-0001  [L3] Implement an inverted index with TF-IDF ranking
              (no libraries) and query it.
  IR-CODE-0002  [L4] Implement BM25 scoring from scratch and verify
              ranking on a toy corpus vs a reference.
  IR-CODE-0003  [L5] Implement hybrid search: fuse BM25 + dense scores
              with weighted reciprocal rank fusion.
  IR-CODE-0004  [L5] Implement a simple two-stage pipeline: dense recall
              -> cross-encoder rerank -> top-k.
  IR-CODE-0005  [L6] Implement a mini HNSW or IVF index and measure
              recall vs brute force.
DEBUGGING
  IR-DEBUG-0001  Dense retrieval misses exact-match queries that BM25
              nails. What does that reveal, and what is the hybrid fix?
  IR-DEBUG-0002  Reranking improves nothing. What could make the
              reranker's signal useless (same model, no diversity)?
  IR-DEBUG-0003  Recall@50 is high but recall@5 collapses. Where in the
              funnel is the loss, and how do you localize it?
  IR-DEBUG-0004  IDF overflows/spikes for a rare token - a query with
              one rare token dominates everything. Fix?
  IR-DEBUG-0005  After chunking change, retrieval scores all drop.
              What knobs (size/overlap/embedding of chunks) do you check?
CODE REVIEW
  IR-REVIEW-0001  Review: retrieving from a corpus that includes the
              query documents (self-retrieval inflation).
  IR-REVIEW-0002  Review: eval on 5 queries reported as recall@10.
  IR-REVIEW-0003  Review: reranker and retriever trained/evaluated on
              the same split.
INTERVIEW
  IR-IV-0001  Design retrieval for a 10M-doc support portal: index,
              hybrid, reranking, and the eval that proves it works.
  IR-IV-0002  Why is 'more recall' not always better for RAG quality?
              What does the generation stage change?
  IR-IV-0003  How would you detect and fix low-quality chunks in the
              index (duplicates, broken text, spam)?
PROJECTS
  IR-PROJ-0001  [D] Search engine on a small corpus: inverted index +
                TF-IDF + BM25 with quality comparison.
  IR-PROJ-0002  [C] Hybrid retrieval with RRF on a real document set,
                with hit@k/MRR eval.
  IR-PROJ-0003  [B] Two-stage retrieval with a reranker, end-to-end
                latency/quality report.
  IR-PROJ-0004  [A] Retrieval eval harness: labeled qrels, metric
                computation, regression testing on index changes.
RESEARCH
  IR-RES-0001  Design a study: when does dense beat hybrid? Vary domain
              and query type and map the decision boundary.
  IR-RES-0002  Compare fusion methods (RRF vs weighted vs learned) on
              fixed retrievers with a fixed budget.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 21 - RAG (RETRIEVAL-AUGMENTED GENERATION)
================================================================================
THEORY / CONCEPTUAL
  RAG-TH-0001  What is RAG, and what problem does it solve that fine-
              tuning cannot (fresh knowledge, attribution, updates)?
  RAG-TH-0002  Draw the basic RAG pipeline: ingest (load -> clean ->
              chunk -> embed -> index) and query (embed -> retrieve ->
              rerank -> generate).
  RAG-TH-0003  What is chunking, and why is chunk size a recall-vs-
              precision dial? What are the chunking strategies (fixed,
              recursive, semantic, parent-child)?
  RAG-TH-0004  What is the context window budget in RAG? What happens
              when retrieved chunks crowd out the instruction?
  RAG-TH-0005  Why does RAG reduce hallucination, and why can it STILL
              hallucinate (wrong chunk, prompt pressure, model bias)?
  RAG-TH-0006  What is citation / grounding, and why does 'show the
              source' change how you evaluate answers?
  RAG-TH-0007  What is query rewriting (decomposition, HyDE, multi-
              query)? What does each change about retrieval input?
  RAG-TH-0008  What is parent-child / hierarchical retrieval? Why store
              small chunks for search but return bigger context?
  RAG-TH-0009  What is contextual compression? What gets compressed
              (retrieved docs, prompt) and why?
  RAG-TH-0010  What is agentic RAG vs naive RAG? When does the agent
              loop (retry, multi-hop, tool calls) actually help?
  RAG-TH-0011  What is Graph RAG? Why build a knowledge graph on top of
              chunks (multi-hop, aggregation across documents)?
  RAG-TH-0012  What is SQL RAG (text-to-SQL)? Why is it fundamentally
              different from vector RAG?
  RAG-TH-0013  What is RAG evaluation? Define retrieval metrics
              (hit@k, MRR, NDCG) vs generation metrics (faithfulness,
              answer relevance, groundedness).
  RAG-TH-0014  What is a 'good retrieval, bad answer' vs 'bad retrieval,
              good answer' failure? Why must you separate the stages?
WHY QUESTIONS
  RAG-TH-0015  Why does chunking at 200 vs 2000 tokens change answers
              dramatically? Trace the failure chain.
  RAG-TH-0016  Why can a 'perfect' retriever still produce a bad answer
              (context overload, order, prompt)?
  RAG-TH-0017  Why is RAG cheaper to update than fine-tuning (index
              rebuild vs retrain)? What is the operational cost of each?
  RAG-TH-0018  Why does adding MORE retrieved chunks sometimes reduce
              answer quality (distraction, position bias)?
  RAG-TH-0019  Why do you need metadata filtering (tenant, date,
              doc-type) in addition to similarity? What failure does
              pure similarity cause?
  RAG-TH-0020  Why is RAG evaluation hard: what does hit@k NOT tell you
              about the final answer?
COMPARISON
  RAG-TH-0021  RAG vs fine-tuning vs prompting: freshness, cost,
              latency, attribution, domain depth.
  RAG-TH-0022  Naive RAG vs advanced (rewrite + rerank + compression)
              vs agentic RAG: when does each earn its complexity?
  RAG-TH-0023  Vector-only vs hybrid vs graph RAG for: FAQ, legal docs,
              multi-hop research.
  RAG-TH-0024  Dense-only vs BM25-only vs hybrid: failure modes and
              when each wins.
MATHEMATICAL
  RAG-MATH-0001  Budget: model context 8k, system prompt 500, chunks
              400 tokens each - how many chunks fit? What if chunks are
              1500?
  RAG-MATH-0002  Multi-query: 3 rewritten queries each retrieve 20 -
              how many candidates before dedup/rerank to 5?
  RAG-MATH-0003  Recall@k of a pipeline: retriever recall@100 = 0.9,
              reranker recall@10 given 100 candidates = 0.95 - what is
              the system recall@10? (chain the probabilities)
  RAG-MATH-0004  Cost: 10M docs x 1 embed each vs 100k queries x 5
              chunks x generation - estimate the ingest vs query cost
              split.
  RAG-MATH-0005  Latency budget 1s: embed 5ms, retrieve 20ms, rerank
              150ms, generate 600ms - where is the headroom, and what
              would you cut?
CODING
  RAG-CODE-0001  [L2] Build RAG with pure numpy: TF-IDF or PPMI-SVD
              retrieval + template prompt + a stub generator.
  RAG-CODE-0002  [L3] Build a chunker: recursive splitter with overlap
              and metadata, with tests on edge cases.
  RAG-CODE-0003  [L4] Build an ingest pipeline: load -> clean -> chunk
              -> embed -> store in a vector index with metadata.
  RAG-CODE-0004  [L5] Implement query rewriting (multi-query + HyDE)
              and measure the retrieval gain on a labeled set.
  RAG-CODE-0005  [L5] Implement a two-stage pipeline with a reranker
              and an end-to-end eval (retrieval + generation).
  RAG-CODE-0006  [L6] Implement a groundedness checker: verify each
              claim against retrieved chunks (LLM or NLI-based).
  RAG-CODE-0007  [L6] Build a RAG eval harness: hit@k, MRR, faithfulness
              on a labeled dataset with regression tracking.
DEBUGGING
  RAG-DEBUG-0001  RAG answers use knowledge NOT in the chunks. Where do
              you check first (retrieval, prompt, model bias)?
  RAG-DEBUG-0002  Retrieval returns the right doc but the wrong section
              (chunking problem). What is the parent-child fix?
  RAG-DEBUG-0003  A user asks 'what is X' and gets an unrelated answer
              though X is in the index. List the failure chain to
              inspect (embed, chunk, query rewrite, rerank).
  RAG-DEBUG-0004  After a doc update, answers still reflect the old
              version. Where is the stale cache?
  RAG-DEBUG-0005  Answers are fluent but the citations are fabricated.
              What is the generation layer doing, and how do you enforce
              groundedness?
  RAG-DEBUG-0006  Multi-tenant RAG leaks one tenant's docs into another.
              Where is the isolation gap (index partition, metadata
              filter, cache)?
  RAG-DEBUG-0007  The system is slow: 2.5s per query. Instrument each
              stage and rank the fixes.
  RAG-DEBUG-0008  Evaluation says faithfulness 0.98 but users complain
              about wrong answers. What does the eval miss (judge bias,
              adversarial queries)?
CODE REVIEW
  RAG-REVIEW-0001  Review: retrieved chunks appended with no dedup or
              relevance cutoff.
  RAG-REVIEW-0002  Review: index built once and never rebuilt after doc
              deletions - stale retrieval.
  RAG-REVIEW-0003  Review: no access control on the retriever (any
              query can pull any doc).
  RAG-REVIEW-0004  Review: eval set built from the same docs in the
              index.
  RAG-REVIEW-0005  Review: the model prompted with 'always use the
              context' but given an empty context on fallback.
INTERVIEW
  RAG-IV-0001  Walk through building a production RAG system for a legal
              document repository, end to end.
  RAG-IV-0002  How do you measure whether RAG actually reduced
              hallucination? Design the eval.
  RAG-IV-0003  When would you use Graph RAG over vector RAG, and what
              does it cost to maintain?
  RAG-IV-0004  Your RAG is 90% accurate on the eval but fails in
              production on slang queries. What do you do?
  RAG-IV-0005  Why is 'retrieve 5 chunks' usually wrong as a fixed
              number? What adaptivity would you build?
SCENARIO
  RAG-SCEN-0001  A support bot must answer from 500k docs updated
              daily. Design ingest, index, retrieval, and freshness.
  RAG-SCEN-0002  RAG for an internal knowledge base where access is
              role-based. Design the isolation and audit model.
  RAG-SCEN-0003  Costs tripled after adding reranking. Where is the
              spend, and what is the cheapest correct fix?
PROJECTS
  RAG-PROJ-0001  [D] RAG from scratch with numpy retrieval (like
                embedding_rag_lab.py) on a small corpus.
  RAG-PROJ-0002  [C] Full RAG pipeline with a real vector store: ingest,
                hybrid search, prompt, eval on labeled qa pairs.
  RAG-PROJ-0003  [B] Advanced RAG: query rewrite + rerank + parent-child
                retrieval, with per-stage ablation eval.
  RAG-PROJ-0004  [A] RAG eval + observability: faithfulness tracking,
                retrieval traces, regression tests, drift alerts.
  RAG-PROJ-0005  [S] Production RAG server: auth, rate limits, caching,
                logging, multi-tenant isolation (like rag_agent_server_
                prod.py).
  RAG-PROJ-0006  [S+] Enterprise RAG platform: hybrid + rerank + graph
                augmentation, 10M docs, cost controls.
RESEARCH
  RAG-RES-0001  Design a study: chunk size vs faithfulness - find the
              curve and the mechanism (context overload).
  RAG-RES-0002  Does query rewriting help more for short or long
              queries? Design the controlled experiment.
  RAG-RES-0003  Measure grounding enforcement: citation checker vs
              constrained decoding vs prompt - which actually reduces
              fabricated citations?
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 22 - VECTOR DATABASES + ANN INDEXING
================================================================================
THEORY / CONCEPTUAL
  VD-TH-0001  What does a vector database add over a numpy matrix + arg-
              sort? (persistence, indexing, filtering, concurrency)
  VD-TH-0002  Why is exact kNN too slow at 10M+ vectors, and what does
              ANN trade away (recall for speed)?
  VD-TH-0003  HNSW: what are the layers, the navigable small-world idea,
              and what do M and efConstruction control?
  VD-TH-0004  IVF: how does inverted-file clustering (nlist) shrink the
              search space, and what is the probe trade-off?
  VD-TH-0005  Product quantization: how does compressing vectors into
              codes cut memory, and what does it cost?
  VD-TH-0006  FAISS vs Chroma vs Qdrant vs Weaviate vs Milvus vs
              Pinecone: one-line positioning each (library vs server,
              filtering, scaling, cloud).
  VD-TH-0007  What is metadata filtering, and why does post-filtering
              break recall? What is pre-filtering vs hybrid filtering?
  VD-TH-0008  What is sharding for vectors? Why is partitioning by
              similarity hard (vs hashing by tenant)?
WHY QUESTIONS
  VD-TH-0009  Why does HNSW memory grow ~ (M * 12 bytes) per edge per
              vector, and why does that matter at 100M vectors?
  VD-TH-0010  Why can't you just sort by dot product in a normal SQL
              database at scale? When is SQL + pgvector good enough?
  VD-TH-0011  Why does the index need rebuilding when embedding models
              change? What does versioning the index buy you?
  VD-TH-0012  Why is recall@10 of 0.9 in the lab dangerous in
              production (query distribution, filters)?
MATHEMATICAL
  VD-MATH-0001  HNSW memory: 1M vectors, M=32 - estimate index memory
              from edges (approx formula).
  VD-MATH-0002  IVFPQ: 1M x 768-dim fp32 = ? GB raw; with PQ m=96,
              bytes = ? - compute both.
  VD-MATH-0003  IVF nlist=1024, probes=16: what fraction of the corpus
              is scanned, and what is the recall ceiling?
  VD-MATH-0004  Brute force at 10M x 768: FLOPs per query and latency
              at 100 GFLOP/s - is it viable? Compute.
  VD-MATH-0005  Product quantization: code length for m=96, k=256 -
              bits per vector and total for 10M vectors.
CODING
  VD-CODE-0001  [L3] Implement IVF (k-means centroids + per-cell brute
              force) and measure recall vs full scan.
  VD-CODE-0002  [L4] Implement HNSW from scratch (insert, search,
              layers) on a small dataset and verify recall.
  VD-CODE-0003  [L4] Implement product quantization encode/decode and
              measure the distortion vs raw vectors.
  VD-CODE-0004  [L5] Build a metadata-filtered search benchmark:
              pre-filter vs post-filter recall and latency.
  VD-CODE-0005  [L6] Build an index versioning + rebuild pipeline with
              shadow queries and rollback.
DEBUGGING
  VD-DEBUG-0001  Search returns nothing after a doc delete. What index
              consistency issue is at play?
  VD-DEBUG-0002  Recall drops after adding filters. Why (post-filter
              starvation) and what fixes exist?
  VD-DEBUG-0003  Index build OOMs on a 32 GB machine with 20M vectors.
              What knobs (dim, PQ, batch, HNSW M) do you change?
  VD-DEBUG-0004  Same query returns different neighbors across replicas.
              What nondeterminism exists (insert order, quantization)?
  VD-DEBUG-0005  pgvector query is slower than a dedicated vector DB for
              your workload. What workload characteristics explain it?
CODE REVIEW
  VD-REVIEW-0001  Review: two embedding models writing into one index
              with no version field.
  VD-REVIEW-0002  Review: HNSW M=512 'for better recall' - what is the
              memory blowup?
  VD-REVIEW-0003  Review: filtering applied client-side after fetching
              top-100 (the recall trap).
INTERVIEW
  VD-IV-0001  Design the vector store for 100M docs with per-tenant
              isolation and 99.9% availability.
  VD-IV-0002  When would you NOT use a vector database (small corpus,
              SQL already there, exact match)?
  VD-IV-0003  How do you migrate from embedding model v1 to v2 without
              downtime and without losing queries?
PROJECTS
  VD-PROJ-0001  [C] Benchmark FAISS vs brute force: recall/latency curve
                across M and nlist on a real dataset.
  VD-PROJ-0002  [B] A self-hosted vector service: embed -> index ->
                search with metadata filters and health checks.
  VD-PROJ-0003  [A] Multi-tenant vector platform: per-tenant indexes,
                quotas, rebuild pipelines, monitoring.
  VD-PROJ-0004  [S] Hybrid search service: BM25 + vector + RRF with
                cache and cost tracking.
RESEARCH
  VD-RES-0001  Design a study: HNSW recall vs M vs efSearch on your
              embedding distribution - where is the knee?
  VD-RES-0002  Compare quantization (PQ) vs dimensionality reduction for
              memory savings at matched recall - measure both.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 23 - RAG EVALUATION
================================================================================
THEORY / CONCEPTUAL
  RE-TH-0001  Why must retrieval and generation be evaluated SEPARATELY?
              What does each stage's metric cover?
  RE-TH-0002  Retrieval metrics: hit@k, recall@k, precision@k, MRR,
              NDCG - define each and when each is the right headline.
  RE-TH-0003  Generation metrics: faithfulness, answer relevance,
              context relevance, groundedness - define each precisely.
  RE-TH-0004  What is LLM-as-judge, and what biases does it have
              (position, verbosity, self-preference)?
  RE-TH-0005  What is a qrels file (query-doc-relevance judgments), and
              why is labeling expensive but necessary?
  RE-TH-0006  What is the difference between offline eval (benchmarks)
              and online eval (A/B, user feedback)?
  RE-TH-0007  What does hit@k NOT measure about answer quality? Give a
              case where hit@10 = 1.0 but the answer is wrong.
WHY QUESTIONS
  RE-TH-0008  Why is faithfulness (groundedness) harder to measure than
              retrieval metrics, and what proxies exist?
  RE-TH-0009  Why can an LLM-judge give high scores while users are
              unhappy (eval set mismatch, judge blindness)?
  RE-TH-0010  Why should you sample hard/adversarial queries in the eval
              set, not just average queries?
  RE-TH-0011  Why is 'human eval is the ground truth' true but
              unscalable - and what is the honest hybrid?
MATHEMATICAL
  RE-MATH-0001  Given 5 relevant docs and retrieved [rel, non, rel, non,
              rel] - compute hit@3, recall@5, precision@3, MRR.
  RE-MATH-0002  NDCG@3 with graded rel [3,2,1]: compute DCG and IDCG.
  RE-MATH-0003  Faithfulness as a rate: 8/10 claims supported by chunks -
              score and its CI with n=10.
  RE-MATH-0004  Eval set size: how many queries to detect a 5% metric
              drop with 95% confidence (approximate)?
  RE-MATH-0005  Judge agreement: 70 of 100 matches human labels -
              Cohen's kappa (compute from a 2x2 agreement table).
CODING
  RE-CODE-0001  [L3] Implement hit@k / MRR / NDCG from predictions and
              qrels, with unit tests.
  RE-CODE-0002  [L4] Build an LLM-judge for faithfulness with a rubric
              and a sampling strategy; measure agreement vs humans.
  RE-CODE-0003  [L5] Build a regression harness: run the eval set on
              every pipeline change, gate merges on metric deltas.
  RE-CODE-0004  [L5] Implement a claim-extraction groundedness checker
              (claims -> chunks support -> score).
  RE-CODE-0005  [L6] Build an online eval: log user feedback + sampled
              LLM-judge double scoring, with dashboards.
DEBUGGING
  RE-DEBUG-0001  Faithfulness 0.95 in eval, complaints in production.
              What is the eval's blind spot (curated vs real queries)?
  RE-DEBUG-0002  Retrieval recall@5 = 0.9 but answer quality low.
              Where is the generation failure (context order, prompt,
              model)?
  RE-DEBUG-0003  The judge gives every answer 5/5. What degenerate
              behavior is happening and how do you detect it?
  RE-DEBUG-0004  Eval score changes between runs with the same code.
              What nondeterminism (sampling temperature, order)?
  RE-DEBUG-0005  Your qrels were built from the same docs the retriever
              was tuned on - what is contaminated?
CODE REVIEW
  RE-REVIEW-0001  Review: eval set of 5 hand-picked queries.
  RE-REVIEW-0002  Review: judge prompt includes the reference answer.
  RE-REVIEW-0003  Review: averaging retrieval and generation scores into
              one number.
  RE-REVIEW-0004  Review: evaluating the model on chunks it was trained
              on.
INTERVIEW
  RE-IV-0001  Design the full evaluation for a customer-support RAG
              system: offline, online, and the metrics per stage.
  RE-IV-0002  How do you know when to trust LLM-as-judge over human
              eval? What measurement decides?
  RE-IV-0003  Your RAG eval says 95% good, product says 60%. List every
              place the eval could be lying.
PROJECTS
  RE-PROJ-0001  [C] Retrieval eval toolkit: qrels format, metric
                implementations, report generation.
  RE-PROJ-0002  [B] Judge calibration project: build a judge, measure
                vs human labels, tune the rubric.
  RE-PROJ-0003  [A] Full RAG regression harness integrated into CI with
                per-stage metrics and alerting.
  RE-PROJ-0004  [S] Multi-signal online eval: user feedback, judge
                scores, cost, latency - unified dashboard.
RESEARCH
  RE-RES-0001  Design a study of judge bias: vary answer position and
              length, measure score shift, propose debiasing.
  RE-RES-0002  Can faithfulness be predicted from retrieval confidence
              alone? Build the correlation study.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 24 - LANGCHAIN (MODERN 1.x CONCEPTS - NO FABRICATED APIS)
================================================================================
THEORY / CONCEPTUAL
  LC-TH-0001  What problem does LangChain solve? What does it abstract
              (models, prompts, tools, retrievers) and what does it NOT
              solve?
  LC-TH-0002  What is a Runnable? Why is composition (|) the core
              abstraction, and what does the protocol guarantee (batch,
              async, stream, invoke)?
  LC-TH-0003  RunnableSequence vs RunnableParallel vs RunnableLambda vs
              RunnablePassthrough: what does each do in a pipeline?
  LC-TH-0004  What is a prompt template vs a chat prompt (system/human/
              AI messages)? Why is message-role structure important?
  LC-TH-0005  What is a structured output (with_structured_output /
              Pydantic), and why is schema validation better than raw
              JSON parsing?
  LC-TH-0006  What is a tool? How does the framework derive the schema
              from a function signature/docstring?
  LC-TH-0007  What is a retriever abstraction? How does it wrap a vector
              store and expose similarity + filters?
  LC-TH-0008  What is an agent (in LangChain terms) vs a chain? Where
              does the decision loop live?
  LC-TH-0009  What is LangGraph, and why did the framework split agent
              execution into it? (stateful graphs vs linear chains)
WHY QUESTIONS
  LC-TH-0010  Why does the Runnable protocol matter for production
              (streaming, retries, tracing) beyond convenience?
  LC-TH-0011  Why should prompts/tools/models be configured outside the
              graph (injectable), and what does that buy for testing?
  LC-TH-0012  Why is it dangerous to hide everything behind one
              abstraction? What must you understand underneath
              (token costs, prompt internals, retriever behavior)?
  LC-TH-0013  Why does streaming through a chain require the Runnable
              contract, and what breaks if a component buffers?
COMPARISON
  LC-TH-0014  LangChain vs calling the provider SDK directly: when is
              the abstraction worth it?
  LC-TH-0015  LCEL pipeline vs imperative code: error handling, testing,
              and debugging differences.
  LC-TH-0016  LangChain vs LangGraph for a multi-step, stateful workflow:
              where does each fit?
CODING
  LC-CODE-0001  [L2] Build a chain: prompt template -> model -> output
              parser, invoked and streamed.
  LC-CODE-0002  [L3] Build a parallel chain: two independent runs fused
              into one response with RunnableParallel.
  LC-CODE-0003  [L4] Build a retrieval chain: retriever -> format docs ->
              prompt -> model -> parser, with tracing.
  LC-CODE-0004  [L4] Define a tool with a typed signature + description
              and bind it to a model; execute a tool call end to end.
  LC-CODE-0005  [L5] Implement a retry/fallback wrapper around a model
              call (bad output -> retry with corrected prompt).
  LC-CODE-0006  [L6] Build a test harness: mock the model, assert the
              chain's inputs/outputs, run it in CI.
DEBUGGING
  LC-DEBUG-0001  A chain works in invoke() but not in stream(). What
              component is buffering, and how do you find it?
  LC-DEBUG-0002  Structured output fails intermittently with validation
              errors. Where do you add retries vs change the schema?
  LC-DEBUG-0003  The model calls a tool that doesn't exist in the
              schema. What is the mismatch, and how do you guard it?
  LC-DEBUG-0004  Prompts look right but the model ignores the system
              message. What role/template structure is wrong?
  LC-DEBUG-0005  A retrieval chain returns the SAME answer for different
              queries. What is cached, and where?
CODE REVIEW
  LC-REVIEW-0001  Review: secrets inside the chain code.
  LC-REVIEW-0002  Review: no timeout/retry on the provider call.
  LC-REVIEW-0003  Review: tools with unrestricted access (files, shell)
              in a chain reachable by users.
  LC-REVIEW-0004  Review: a chain that logs the full prompt including
              PII.
  LC-REVIEW-0005  Review: no unit tests around the prompt templates.
INTERVIEW
  LC-IV-0001  When do you introduce LangChain vs writing the loop
              yourself? What is the actual cost of the abstraction?
  LC-IV-0002  Walk through building a RAG chain that streams, with
              retries, and how you would test it.
  LC-IV-0003  Your chain breaks silently when the model returns bad
              JSON. Where do you add validation and retry?
  LC-IV-0004  How do you version a chain for production (prompts, tools,
              model versions)?
PROJECTS
  LC-PROJ-0001  [C] RAG chain with a vector store retriever, structured
                output, and streaming.
  LC-PROJ-0002  [B] Tool-using chain: calculator + lookup tools with
                validation and graceful fallbacks.
  LC-PROJ-0003  [A] Production chain service: FastAPI endpoint, tracing,
                retries, cost tracking, eval in CI.
  LC-PROJ-0004  [S] A chain platform: versioned prompts/tools, A/B
                routing, observability, per-tenant config.
RESEARCH
  LC-RES-0001  Measure the overhead of the framework vs raw calls:
              latency, memory, and failure semantics.
  LC-RES-0002  Compare LCEL composition vs hand-written pipelines on
              maintainability: what breaks first?
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 25 - LANGGRAPH (STATEFUL WORKFLOWS + AGENTS)
================================================================================
THEORY / CONCEPTUAL
  LG-TH-0001  What is a state machine, and why is an AI agent naturally
              one (observe -> decide -> act -> observe...)?
  LG-TH-0002  What are the core LangGraph concepts: State, Nodes, Edges,
              START, END, conditional edges? How does execution flow?
  LG-TH-0003  What is a reducer (state update function)? Why is appending
              to a messages list different from overwriting a field?
  LG-TH-0004  What is a checkpoint, and what does persistence enable
              (resume, interrupts, conversation threads)?
  LG-TH-0005  What is human-in-the-loop, and how do interrupts/approvals
              work? Why is pausing execution a product feature?
  LG-TH-0006  What is the difference between a graph and a chain? When
              do you need cycles (retry, reflect, multi-hop)?
  LG-TH-0007  What is a supervisor multi-agent pattern? How does one
              graph route work to specialist sub-agents?
  LG-TH-0008  What are subgraphs and map-reduce patterns (fan-out,
              aggregate)? When do you use them?
  LG-TH-0009  What does 'durable execution' mean, and what happens to
              state when a node crashes?
WHY QUESTIONS
  LG-TH-0010  Why is a static linear chain insufficient for agents
              (loops, branching, retries)? What does the graph add?
  LG-TH-0011  Why does persistence change agent design (state survives
              restarts, users resume conversations)?
  LG-TH-0012  Why must tool-calling loops have termination conditions
              (max steps) instead of 'model decides'?
  LG-TH-0013  Why is state schema design critical (what must be in the
              shared state, what must stay node-local)?
  LG-TH-0014  Why do multi-agent systems need explicit coordination
              (supervisor, handoffs) instead of free-form delegation?
MATHEMATICAL
  LG-MATH-0001  Cost of a loop: agent that calls 3 tools per turn, max 5
              turns, model 1k in/1k out tokens - worst-case tokens per
              run.
  LG-MATH-0002  State growth: message history of k turns x 500 tokens -
              when do you need summarization, and at what size?
  LG-MATH-0003  Parallel fan-out: N nodes each calling the model once -
              total calls and latency (parallel vs sequential).
  LG-MATH-0004  Checkpoint size: full message state per checkpoint x
              users x threads - estimate storage for 1M users.
CODING
  LG-CODE-0001  [L3] Build a 3-node linear graph (START -> A -> B ->
              END) with typed state, and invoke it.
  LG-CODE-0002  [L4] Build a conditional graph: route to different
              nodes based on a classifier or tool decision.
  LG-CODE-0003  [L4] Build a loop with a max-iteration guard: model ->
              tool -> check -> repeat/end.
  LG-CODE-0004  [L5] Add checkpoints: persist state, interrupt for
              human approval, resume the graph.
  LG-CODE-0005  [L5] Build a supervisor multi-agent graph: supervisor
              routes to 2 specialists, results return to the supervisor.
  LG-CODE-0006  [L6] Build a map-reduce subgraph: fan out to N workers,
              aggregate results with a reducer.
  LG-CODE-0007  [L6] Implement a RAG-with-reflection graph: retrieve ->
              grade -> rewrite-or-generate -> verify.
DEBUGGING
  LG-DEBUG-0001  The graph never terminates - where is the loop missing
              a condition, and what is the diagnostic?
  LG-DEBUG-0002  State is lost between nodes. What schema/reducer
              mistake overwrote the field?
  LG-DEBUG-0003  After resume, the graph re-executes nodes it already
              ran. What checkpoint behavior is misconfigured?
  LG-DEBUG-0004  Two users' sessions leak into each other. What state
              key is shared, and how does a thread-id fix it?
  LG-DEBUG-0005  A tool call fails and the agent retries forever with
              the same error. Where do you add error handling and
              fallback?
  LG-DEBUG-0006  Parallel branches produce conflicting updates to the
              same state field. What reducer resolves it?
  LG-DEBUG-0007  The graph runs fine locally but times out in
              production. What is unbounded (messages, retries, tools)?
CODE REVIEW
  LG-REVIEW-0001  Review: tool execution without sandboxing or allowlist.
  LG-REVIEW-0002  Review: no max-steps on the agent loop.
  LG-REVIEW-0003  Review: full conversation history re-sent every turn
              with no truncation.
  LG-REVIEW-0004  Review: shared global state with no tenant isolation.
  LG-REVIEW-0005  Review: no error handling on the critical tool call -
              one failure kills the run.
INTERVIEW
  LG-IV-0001  Why use a graph instead of a while-loop for an agent?
              What specifically does the framework enforce?
  LG-IV-0002  Design a customer-support agent graph: state, nodes,
              routing, human-in-the-loop, and failure handling.
  LG-IV-0003  How do you prevent cost blowup in a multi-agent graph
              (budgets, step limits, routing)?
  LG-IV-0004  What happens to a graph run when the server restarts
              mid-run? Design for durability.
  LG-IV-0005  When would you choose NOT to use LangGraph (simple
              single-shot calls, no state)?
SCENARIO
  LG-SCEN-0001  A research agent must search, read, and write a report
              with verification. Design the graph and its failure modes.
  LG-SCEN-0002  An approval workflow must pause for a human before
              sending emails. Walk through interrupts + resume.
PROJECTS
  LG-PROJ-0001  [C] Stateful Q&A bot: conversation memory via checkpoints
                with thread isolation.
  LG-PROJ-0002  [B] Tool-using agent with loop guard, retries, and
                graceful degradation.
  LG-PROJ-0003  [A] RAG-with-reflection graph: grade/rewrite/verify,
                with eval vs the naive pipeline.
  LG-PROJ-0004  [S] Multi-agent supervisor system for research + writing
                with budgets, tracing, and human approval.
  LG-PROJ-0005  [S+] Enterprise agent platform: durable checkpoints,
                multi-tenant state, cost controls, observability.
RESEARCH
  LG-RES-0001  Design a study: does reflection (grade -> rewrite) improve
              answer quality enough to justify the latency? Measure.
  LG-RES-0002  Compare supervisor vs flat (all tools, one agent) on cost
              and quality for a fixed task set.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 26 - AI AGENTS
================================================================================
THEORY / CONCEPTUAL
  AG-TH-0001  What makes a system an 'agent' vs an LLM with tools? Where
              is the line (loop, state, autonomy, goals)?
  AG-TH-0002  Draw the agent loop: observe -> reason -> plan -> act
              (tool) -> observe result -> repeat -> final.
  AG-TH-0003  What is ReAct? How does interleaving reasoning traces with
              actions improve tool use?
  AG-TH-0004  What is tool calling / function calling? How does the
              model express 'call tool X with args Y'?
  AG-TH-0005  What is the difference between planning and execution in
              an agent? When is explicit planning (plan-then-execute)
              better than reactive?
  AG-TH-0006  What is agent memory? Distinguish short-term (session),
              long-term (persistent), and working state.
  AG-TH-0007  What is reflection / self-correction? What evidence exists
              that it helps, and what are its failure modes?
  AG-TH-0008  What is a stopping condition, and why must every loop have
              one (max steps, success signal, budget)?
  AG-TH-0009  What is excessive agency? Give an example of a tool that
              grants too much power for its task.
  AG-TH-0010  What is prompt injection via tool output (indirect prompt
              injection)? Why is it fundamentally hard to defend?
WHY QUESTIONS
  AG-TH-0011  Why is an LLM alone not an agent, and why is a loop +
              tools + state the minimal definition?
  AG-TH-0012  Why do agents fail silently? (model lies, tool errors,
              wrong assumptions) and what does verification add?
  AG-TH-0013  Why does giving an agent more tools reduce reliability,
              and how do you scope a toolset?
  AG-TH-0014  Why is deterministic tool execution (explicit code) better
              than letting the model 'imagine' results?
  AG-TH-0015  Why does cost grow superlinearly with agent steps, and
              what design rule caps it?
COMPARISON
  AG-TH-0016  ReAct vs plan-and-execute vs reflection loops: latency,
              reliability, and when each earns its complexity.
  AG-TH-0017  Single agent with many tools vs specialized agents
              coordinated: cost, quality, debugging.
  AG-TH-0018  Agent with tools vs RAG-only: when is a tool (calculator,
              code exec, DB) strictly better?
  AG-TH-0019  Agentic vs workflow (deterministic graph): what is the
              decision rule between them?
MATHEMATICAL
  AG-MATH-0001  Worst-case cost: n tools, k attempts per step, m steps -
              total model calls and tokens.
  AG-MATH-0002  Success probability: each step 90% reliable, 6 steps -
              end-to-end success rate. What step reliability gives 95%
              end-to-end?
  AG-MATH-0003  Latency: 5 sequential model calls at 300ms each vs 3
              parallel at 300ms - total time for both.
CODING
  AG-CODE-0001  [L3] Build the minimal agent loop by hand: model -> tool
              registry -> execute -> observe, with max steps.
  AG-CODE-0002  [L4] Build a safe tool executor: allowlist, argument
              validation, timeouts, error capture.
  AG-CODE-0003  [L4] Implement a ReAct-style prompt + parser and measure
              tool-use accuracy on a toy task.
  AG-CODE-0004  [L5] Add memory: session state + persistent user
              preferences, with explicit reads/writes.
  AG-CODE-0005  [L5] Add reflection: after failure, feed the error back
              and retry with a corrected plan; log the recovery rate.
  AG-CODE-0006  [L6] Add an indirect-injection guard: sanitize tool
              outputs, delimit them, and audit what the model saw.
DEBUGGING
  AG-DEBUG-0001  The agent repeats the same failing tool call 5 times.
              What loop control is missing, and what telemetry reveals
              it?
  AG-DEBUG-0002  The agent fabricates a tool result instead of calling
              the tool. What prompt/structure allows this, and how do you
              enforce real execution?
  AG-DEBUG-0003  A tool returns markdown that the model follows as
              instructions (injection). How do you reproduce and fix?
  AG-DEBUG-0004  The agent's plan is good but execution picks the wrong
              tool. Where do you instrument (tool selection log)?
  AG-DEBUG-0005  Costs explode on one user's session. What metric (calls,
              tokens, retries) do you alert on, and what cap do you add?
CODE REVIEW
  AG-REVIEW-0001  Review: `eval(input())`-style tool execution.
  AG-REVIEW-0002  Review: tool errors swallowed and reported as success.
  AG-REVIEW-0003  Review: no max-steps, no budget, no timeout on the
              loop.
  AG-REVIEW-0004  Review: agent can read/write arbitrary files from a
              user-facing endpoint.
  AG-REVIEW-0005  Review: no logging of what the model actually saw
              (prompt + tool outputs) - the debugging black hole.
INTERVIEW
  AG-IV-0001  Design an agent that can research, draft, and verify a
              report. Include tools, memory, stopping, and failure modes.
  AG-IV-0002  How do you evaluate an agent (task success, steps, cost,
              reliability)? What is an eval set for agents?
  AG-IV-0003  When should a task NOT be an agent? Give the threshold
              where a deterministic pipeline wins.
  AG-IV-0004  Your agent hallucinated a tool result and acted on it.
              Design the defense in depth.
  AG-IV-0005  How do you test an agent deterministically when the model
              is stochastic?
SCENARIO
  AG-SCEN-0001  A coding agent must edit files in a repo. Design the
              sandbox, permissions, and rollback.
  AG-SCEN-0002  A customer-facing agent must never perform irreversible
              actions without approval. Design the policy.
PROJECTS
  AG-PROJ-0001  [C] A calculator + lookup agent with a safe tool
                registry and a max-steps loop.
  AG-PROJ-0002  [B] A research agent: search -> read -> synthesize with
                citation and verification.
  AG-PROJ-0003  [A] An agent harness: eval set, step/cost tracking,
                failure taxonomy, regression tests.
  AG-PROJ-0004  [S] A production agent service: FastAPI, session state,
                budgets, tracing, human-approval for risky actions.
  AG-PROJ-0005  [S+] A multi-tool autonomous agent for a real workflow
                with injection defenses and full audit logging.
RESEARCH
  AG-RES-0001  Design an experiment: does reflection improve task success
              per dollar? Measure both.
  AG-RES-0002  Compare ReAct vs plan-execute on planning-heavy tasks:
              where does the plan actually help?
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 27 - MULTI-AGENT SYSTEMS
================================================================================
THEORY / CONCEPTUAL
  MA-TH-0001  Why build multiple agents instead of one? What do they
              gain (specialization, parallel work, isolation) and lose
              (coordination overhead, cost)?
  MA-TH-0002  Supervisor pattern: how does a supervisor route, and what
              happens when the supervisor misroutes?
  MA-TH-0003  What is a handoff? How do agents pass context without
              duplicating state?
  MA-TH-0004  Shared state vs message passing: what must be shared, and
              what should stay private per agent?
  MA-TH-0005  What is fan-out/fan-in (map-reduce)? When do parallel
              workers beat one agent doing everything?
  MA-TH-0006  What is the role of verification agents? Why is
              'produce then check' more reliable than 'produce once'?
  MA-TH-0007  What is the failure cascade? How does one agent's bad
              output poison the rest, and what breaks the chain?
WHY QUESTIONS
  MA-TH-0008  Why does multi-agent often INCREASE cost without improving
              quality? When is the overhead unjustified?
  MA-TH-0009  Why is coordination (supervisor, schema, contracts) the
              hard part, not the individual agents?
  MA-TH-0010  Why must each agent have a narrow, testable contract
              (input/output schema) for the system to be debuggable?
  MA-TH-0011  Why is infinite delegation dangerous, and what graph
              structure prevents it (DAG + max hops)?
MATHEMATICAL
  MA-MATH-0001  Cost model: supervisor + 3 specialists x 4 turns each vs
              one agent x 8 turns - compare total calls.
  MA-MATH-0002  Parallel speedup: 4 specialist calls at 500ms vs
              sequential - total latency, and the ceiling with 10
              workers.
  MA-MATH-0003  Reliability: 5 agents each 0.95 reliable, all must
              succeed - end-to-end probability; with a verifier that
              catches 80% of failures, what improves?
CODING
  MA-CODE-0001  [L4] Build a two-agent system: researcher produces,
              reviewer critiques; loop with a max round count.
  MA-CODE-0002  [L5] Build a supervisor graph: route by task type to
              specialists, collect and synthesize.
  MA-CODE-0003  [L5] Implement handoffs with explicit message contracts
              (typed state per agent).
  MA-CODE-0004  [L6] Build a map-reduce: fan out N research subtasks,
              aggregate into one report.
  MA-CODE-0005  [L6] Add a verifier agent that blocks bad outputs from
              propagating (check -> requeue or reject).
DEBUGGING
  MA-DEBUG-0001  One agent's output corrupts the shared state for
              everyone. What isolation rule was violated?
  MA-DEBUG-0002  The supervisor always routes to the same specialist.
              What is the routing signal (classification) doing?
  MA-DEBUG-0003  Parallel agents produce contradictory claims in one
              report. Where is the reconciliation step?
  MA-DEBUG-0004  The system costs 5x the single agent with no quality
              gain. What metric would have caught this in eval?
  MA-DEBUG-0005  A handoff loses context (agent B doesn't know what
              agent A found). What state schema is missing?
CODE REVIEW
  MA-REVIEW-0001  Review: all agents share one unrestricted toolset.
  MA-REVIEW-0002  Review: no limits on delegation depth.
  MA-REVIEW-0003  Review: verifier is the same model as the producer
              (self-review blindness).
  MA-REVIEW-0004  Review: agents write to a shared DB with no row-level
              ownership.
INTERVIEW
  MA-IV-0001  When does multi-agent beat single-agent? Give the decision
              criteria and a counterexample.
  MA-IV-0002  Design a multi-agent system for enterprise research with
              cost controls and failure isolation.
  MA-IV-0003  How do you evaluate each agent separately AND the system
              as a whole? Design both evals.
  MA-IV-0004  What happens when the supervisor model is worse than the
              specialists? How do you detect it?
PROJECTS
  MA-PROJ-0001  [C] Researcher + reviewer loop with a fixed round limit.
  MA-PROJ-0002  [B] Supervisor with 3 specialists on a real task set,
                with routing accuracy eval.
  MA-PROJ-0003  [A] Production multi-agent: parallel workers, verifier
                gate, budgets, tracing.
  MA-PROJ-0004  [S] Enterprise multi-agent platform: tenant isolation,
                per-agent quotas, audit logs, human approvals.
RESEARCH
  MA-RES-0001  Design a study: supervisor vs flat agent on cost-quality
              for a fixed task set - report the crossover.
  MA-RES-0002  Measure the value of a verifier agent: quality vs cost
              with different verifier models.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Review [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 28 - MULTIMODAL AI
================================================================================
THEORY / CONCEPTUAL
  MM-TH-0001  What is a multimodal model? How do text and image tokens
              share one embedding/latent space?
  MM-TH-0002  What is a vision-language model (VLM)? Encoder vs
              projector vs LLM - what does each component do?
  MM-TH-0003  What is OCR and document intelligence? Why is layout
              understanding different from raw OCR?
  MM-TH-0004  What is multimodal RAG? What gets embedded (text, images,
              tables) and how is retrieval fused?
  MM-TH-0005  What is a multimodal embedding? How do you compare a text
              query to an image in one space?
  MM-TH-0006  What are the failure modes of VLMs (hallucinated objects,
              OCR errors, counting failures)?
WHY QUESTIONS
  MM-TH-0007  Why is aligning modalities hard (semantic gap), and what
              does contrastive alignment (image-text pairs) learn?
  MM-TH-0008  Why does OCR-only extraction lose structure that
              layout-aware models keep?
  MM-TH-0009  Why is evaluating VLMs harder than text-only LLMs
              (visual grounding, detail level)?
CODING
  MM-CODE-0001  [L3] Build a multimodal retrieval index: embed images and
              text into one space (pretrained), query by text.
  MM-CODE-0002  [L4] Build a document-intelligence pipeline: page image
              -> OCR + layout -> chunk -> RAG index.
  MM-CODE-0003  [L5] Fine-tune a small VLM adapter on a task and
              evaluate with a held-out set.
DEBUGGING
  MM-DEBUG-0001  The VLM describes objects not in the image. What eval
              catches this, and what training data fixes it?
  MM-DEBUG-0002  Multimodal retrieval fails on rotated/scanned docs.
              What preprocessing and embedding choice is at fault?
  MM-DEBUG-0003  Image-text retrieval returns near-duplicate irrelevant
              images. What similarity collapse is happening?
INTERVIEW
  MM-IV-0001  Design a multimodal document QA system for invoices and
              contracts. Include extraction, retrieval, and generation.
  MM-IV-0002  When would you embed images vs describe them with a VLM
              and embed the text? Cost and quality trade-off.
PROJECTS
  MM-PROJ-0001  [C] Image-caption retrieval with a pretrained
                multimodal embedder.
  MM-PROJ-0002  [B] Document QA: OCR -> layout -> chunks -> RAG with
                citations to page/region.
  MM-PROJ-0003  [A] Multimodal eval harness: VLM QA with grounding and
                OCR-error metrics.
RESEARCH
  MM-RES-0001  Design a study: does multimodal retrieval beat
              text-only (VLM-description) retrieval on your docs?
  MM-RES-0002  Measure VLM hallucination across model sizes: does
              scaling reduce object hallucination?

================================================================================
DOMAIN 29 - COMPUTER VISION (PRODUCTION)
================================================================================
THEORY / CONCEPTUAL
  CV2-TH-0001  Classification vs detection vs segmentation vs tracking:
              task definitions and output formats (labels, boxes, masks).
  CV2-TH-0002  What is mAP in detection, and why is it a curve (AP per
              class, IoU thresholds)?
  CV2-TH-0003  What is NMS, and why is it needed in detection but not
              segmentation?
  CV2-TH-0004  What is IoU and Dice? When is each the right
              segmentation metric?
  CV2-TH-0005  What is a video-understanding task (action recognition,
              tracking)? What does temporal modeling add?
  CV2-TH-0006  What is image retrieval? How does it differ from
              classification (no fixed classes)?
WHY QUESTIONS
  CV2-TH-0007  Why is data augmentation critical for vision models, and
              why must test-time transforms match training?
  CV2-TH-0008  Why do detection models need anchor/label assignment
              strategies, and what breaks with tiny objects?
  CV2-TH-0009  Why does a vision model fail on a new camera (distribution
              shift: lighting, sensor, resolution)?
CODING
  CV2-CODE-0001  [L3] Implement IoU and NMS from scratch; verify on toy
              boxes.
  CV2-CODE-0002  [L4] Build an inference service for a detection model:
              image in, boxes/classes out, with preprocessing parity.
  CV2-CODE-0003  [L5] Implement an evaluation: mAP over a COCO-style
              annotation set.
DEBUGGING
  CV2-DEBUG-0001  Detection misses small objects after quantization.
              What sensitivity is at play, and what layers need fp16?
  CV2-DEBUG-0002  Segmentation masks are shifted by a few pixels. What
              resize/coordinate bug is likely?
  CV2-DEBUG-0003  Model works on train camera but not deployment camera.
              What drift tests and augmentation fixes apply?
INTERVIEW
  CV2-IV-0001  Design a real-time detection system at 30 FPS on edge
              hardware: model choice, quantization, batching, pipeline.
  CV2-IV-0002  How do you label and evaluate a custom detection dataset
              with 20k images and long-tail classes?
PROJECTS
  CV2-PROJ-0001  [C] Fine-tune a detector on a custom class and compute
                mAP.
  CV2-PROJ-0002  [B] Edge deployment: quantize + export + latency
                benchmark vs the server model.
  CV2-PROJ-0003  [A] Vision monitoring: distribution drift on images,
                error bucketing, retraining triggers.
RESEARCH
  CV2-RES-0001  Design a study: augmentation policy search vs manual
              augmentation on a small custom dataset.
  CV2-RES-0002  Measure long-tail detection: what is the AP gap for rare
              classes, and what methods close it?

================================================================================
DOMAIN 30 - GENERATIVE MODELS (VAE / GAN / DIFFUSION)
================================================================================
THEORY / CONCEPTUAL
  GN-TH-0001  What is a generative model? Likelihood-based vs implicit
              (GAN) - what does each optimize?
  GN-TH-0002  VAE: what is the ELBO, and what does the KL term regularize
              (latent prior)? Why are VAEs blurry?
  GN-TH-0003  GAN: generator vs discriminator - what is the min-max
              game, and what is mode collapse?
  GN-TH-0004  Diffusion: what is the forward (noise) process and the
              reverse (denoise) process? What is the training objective?
  GN-TH-0005  Why did diffusion beat GANs for image generation? What
              does likelihood/training stability change?
  GN-TH-0006  What is latent diffusion (Stable Diffusion)? Where does
              the VAE latent space enter?
  GN-TH-0007  Text-to-image: how does conditioning (CLIP embeddings)
              steer generation?
WHY QUESTIONS
  GN-TH-0008  Why is GAN training unstable and how do techniques (spectral
              norm, gradient penalty) stabilize it?
  GN-TH-0009  Why does diffusion sample slowly, and what do
              schedulers/distillation do to speed it up?
  GN-TH-0010  Why can generative models memorize training data, and what
              are the copyright/privacy implications?
MATHEMATICAL
  GN-MATH-0001  VAE loss: reconstruction + KL - write both terms and
              identify what each enforces.
  GN-MATH-0002  Diffusion loss: write the noise-prediction objective
              epsilon_theta(x_t, t) ~ epsilon.
  GN-MATH-0003  GAN objective: write the min-max value function and the
              optimal discriminator D* = p_data/(p_data + p_gen).
CODING
  GN-CODE-0001  [L3] Train a small VAE on MNIST and sample from the
              latent prior.
  GN-CODE-0002  [L4] Train a small GAN on MNIST; diagnose mode collapse
              via sample diversity.
  GN-CODE-0003  [L5] Implement a minimal DDPM forward + reverse on
              MNIST, and sample.
DEBUGGING
  GN-DEBUG-0001  GAN loss oscillates and never converges. What stability
              knobs do you check in order?
  GN-DEBUG-0002  VAE reconstructions are sharp but samples are blurry -
              what does that say about the latent prior vs encoder?
  GN-DEBUG-0003  Diffusion generates only one class of images. What
              conditioning or data issue is at play?
INTERVIEW
  GN-IV-0001  Compare GAN, VAE, and diffusion on training stability,
              sample quality, and diversity.
  GN-IV-0002  How would you evaluate image generation quality (FID,
              IS, human)? What does FID measure and its limits?
PROJECTS
  GN-PROJ-0001  [C] VAE on a real dataset with latent-space
                interpolation visualization.
  GN-PROJ-0002  [B] DDPM image generation with FID-style evaluation.
  GN-PROJ-0003  [A] Conditional generation (class or text-conditioned)
                with sampling-speed optimization.
RESEARCH
  GN-RES-0001  Design a study: sampling steps vs quality (FID) for a
              fixed diffusion model - where is the knee?
  GN-RES-0002  Measure memorization: nearest-neighbor overlap between
              generated and training images vs model size.
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 31 - RECOMMENDER SYSTEMS
================================================================================
THEORY / CONCEPTUAL
  RC-TH-0001  Collaborative filtering vs content-based vs hybrid: what
              signal does each use?
  RC-TH-0002  What is matrix factorization? R ~ U V^T - what do the
              latent factors represent?
  RC-TH-0003  Why is the two-stage (retrieval then ranking) design
              standard for recommenders?
  RC-TH-0004  What is the cold-start problem (new users/items), and what
              are the fixes (popularity, content, hybrid)?
  RC-TH-0005  What is implicit vs explicit feedback? Why do implicit
              signals (clicks, views) dominate?
  RC-TH-0006  What is bias in recommender systems (popularity bias,
              exposure bias) and how does it self-reinforce?
WHY QUESTIONS
  RC-TH-0007  Why can't you train a recommender on all user-item pairs
              (negative sampling, imbalance)?
  RC-TH-0008  Why is offline accuracy (RMSE/NDCG) misleading for
              recommenders (position, diversity, novelty)?
  RC-TH-0009  Why do recommenders need freshness and how does serving
              (embeddings, ANN) enable real-time updates?
MATHEMATICAL
  RC-MATH-0001  Matrix factorization: write the objective with
              regularization and the SGD update for u_i, v_j.
  RC-MATH-0002  ALS: for fixed V, solve for U in closed form - write the
              normal equations.
  RC-MATH-0003  NDCG@10 vs precision@10: which rewards ranking the
              relevant item first, and by how much?
CODING
  RC-CODE-0001  [L3] Implement matrix factorization with SGD on a small
              dataset (like the MF-SGD recommender).
  RC-CODE-0002  [L4] Implement ALS and compare RMSE vs SGD (like
              als_vs_sgd_rmse.py).
  RC-CODE-0003  [L5] Build a two-stage recommender: retrieval (ANN) +
              ranking (lightGBM on features).
  RC-CODE-0004  [L6] Implement a recommendation eval: offline NDCG +
              online A/B design with bias controls.
DEBUGGING
  RC-DEBUG-0001  The recommender always shows the same top-10 popular
              items. What bias is at play and how do you measure it?
  RC-DEBUG-0002  Offline NDCG is high but CTR drops online. What offline
              metric is misleading here?
  RC-DEBUG-0003  New items never surface (cold start). What stage
              (retrieval, ranking, popularity prior) is blocking them?
  RC-DEBUG-0004  User A sees user B's items. Where did the user-item
              join leak?
INTERVIEW
  RC-IV-0001  Design a recommendation system for a video platform: data,
              two-stage architecture, freshness, and eval.
  RC-IV-0002  How do you evaluate a recommender you cannot A/B (cost)?
              What offline signals + guardrails do you use?
  RC-IV-0003  How do you prevent a recommender from creating filter
              bubbles, and how would you measure diversity?
PROJECTS
  RC-PROJ-0001  [C] MF recommender with RMSE eval on MovieLens-style
                data.
  RC-PROJ-0002  [B] Two-stage recommender with ANN retrieval + ranking
                model + NDCG eval.
  RC-PROJ-0003  [A] Production recommender service: embeddings, ANN,
                popularity fallback, logging, A/B infra.
RESEARCH
  RC-RES-0001  Design a study: implicit negative sampling ratio vs NDCG -
              where is the sweet spot?
  RC-RES-0002  Measure the echo chamber: how much does exposure bias
              reduce catalog diversity over time?

================================================================================
DOMAIN 32 - TIME SERIES
================================================================================
THEORY / CONCEPTUAL
  TS-TH-0001  Trend, seasonality, cycles, noise: define each and how you
              decompose a series.
  TS-TH-0002  What is stationarity and why do classical models require
              it? What does differencing fix?
  TS-TH-0003  AR vs MA vs ARIMA vs SARIMA: what does each term model,
              and what does SARIMA add?
  TS-TH-0004  Why do ML models (trees, linear) work on time series?
              What features do they need (lags, rolling stats)?
  TS-TH-0005  What is temporal validation (walk-forward)? Why is random
              CV invalid for time series?
  TS-TH-0006  What is forecast horizon vs lead time? Why does
              multi-step forecasting compound error?
WHY QUESTIONS
  TS-TH-0007  Why is 'predict tomorrow' different from 'predict next
              month'? What changes about features and error?
  TS-TH-0008  Why do lag features leak the future if shifted wrong?
              What is the canonical no-future guarantee?
  TS-TH-0009  Why is RMSE misleading for spiky series, and what does
              MAPE/MASE add?
  TS-TH-0010  Why do deep models need much more data than classical
              methods for time series?
MATHEMATICAL
  TS-MATH-0001  Write the ARIMA(p,d,q) equation for p=1, d=1, q=1.
  TS-MATH-0002  Exponential smoothing: write the recursive update and
              explain alpha.
  TS-MATH-0003  Walk-forward: for n=1000, horizon 30, step 30 - how many
              folds and what is train size per fold?
  TS-MATH-0004  Error compounding: per-step RMSE 1.0, 10 steps - is
              total RMSE 10? Explain (variance growth vs mean path).
CODING
  TS-CODE-0001  [L3] Build lag/rolling features with a strict
              no-future guarantee (shift with min_periods).
  TS-CODE-0002  [L4] Implement walk-forward CV and compare models with
              it (linear vs tree vs ARIMA).
  TS-CODE-0003  [L5] Implement exponential smoothing from scratch and
              fit alpha.
  TS-CODE-0004  [L6] Build a forecasting pipeline with backtest,
              MASE, and a retraining trigger.
DEBUGGING
  TS-DEBUG-0001  Walk-forward score is great but live forecasts lag one
              step behind reality. What shift bug is classic?
  TS-DEBUG-0002  ARIMA errors explode at the holiday. What
              exogenous/seasonal structure is missing?
  TS-DEBUG-0003  Lag features on a non-stationary series give perfect
              train scores. What is the real signal?
  TS-DEBUG-0004  A feature uses 'current month total' which is only
              known at month end. What is the point-in-time leak?
INTERVIEW
  TS-IV-0001  Design a demand-forecasting system for a retail chain:
              data, features, models, validation, retraining.
  TS-IV-0002  How do you handle holidays, promotions, and trends in one
              model? What feature engineering covers each?
  TS-IV-0003  When is a classical model (ARIMA/ETS) better than
              gradient boosting, and when is the reverse true?
PROJECTS
  TS-PROJ-0001  [C] Forecasting with lag features + trees vs ARIMA,
                walk-forward, honest report.
  TS-PROJ-0002  [B] Multi-horizon forecasting with a proper backtest
                harness and MASE.
  TS-PROJ-0003  [A] Production forecasting: point-in-time features,
                drift detection, automated retraining.
RESEARCH
  TS-RES-0001  Design a study: does adding MLP/LSTM beat trees on YOUR
              series, at matched features and compute?
  TS-RES-0002  Measure the value of exogenous features: ablation across
              horizons - where do they matter most?
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 33 - MLOPS
================================================================================
THEORY / CONCEPTUAL
  MO-TH-0001  What is MLOps, and how is it different from DevOps (data +
              model lifecycle on top of code)?
  MO-TH-0002  Experiment tracking: what must be logged per run (code,
              data version, config, metrics, artifacts)?
  MO-TH-0003  What is a model registry? What does 'promote to prod'
              actually gate (eval, tests, approvals)?
  MO-TH-0004  Data versioning (DVC) vs code versioning (Git): what does
              each pin, and why are they both needed?
  MO-TH-0005  What is a feature store? Why do train/serve consistency
              problems exist, and what does the store fix?
  MO-TH-0006  What is CI/CD for ML? What is tested in a 'training CI'
              vs a 'serving CI'?
  MO-TH-0007  Model packaging: what is in an artifact (weights, config,
              preprocessing, tokenizer), and why does the interface matter?
  MO-TH-0008  What is a rollout vs a rollback for a model, and why is a
              shadow deployment safer than a direct switch?
WHY QUESTIONS
  MO-TH-0009  Why do models degrade in production even with unchanged
              code (data drift, world drift)? What monitors catch it?
  MO-TH-0010  Why is 'it worked in the notebook' not deployable? What
              gaps (env, data access, monitoring, eval) break it?
  MO-TH-0011  Why must training be reproducible, and what does pinning
              seeds + versions + data hashes actually guarantee?
  MO-TH-0012  Why is model monitoring different from system monitoring
              (latency) - what quality metrics do you need?
CODING
  MO-CODE-0001  [L3] Wire experiment tracking into a training run:
              log params, metrics per epoch, and the artifact path.
  MO-CODE-0002  [L4] Build a training CI: lint + unit tests + a smoke
              train on a tiny dataset that gates merges.
  MO-CODE-0003  [L5] Build a model registry API: register, promote,
              rollback, with eval-metric gates.
  MO-CODE-0004  [L5] Implement DVC-style data versioning: hash datasets,
              cache artifacts, restore by hash.
  MO-CODE-0005  [L6] Build a deployment pipeline: package model -> test
              -> shadow -> canary -> full rollout with automatic rollback.
DEBUGGING
  MO-DEBUG-0001  The model works in the staging env but fails in prod
              with different preprocessing. What parity test is missing?
  MO-DEBUG-0002  A rerun of a 'reproducible' training gives different
              metrics. What was NOT pinned?
  MO-DEBUG-0003  Production model quality drops, but no code changed.
              What dashboards do you check in order?
  MO-DEBUG-0004  A model rollback fails because the old artifact can't
              load with the new serving code. What compatibility rule was
              violated?
INTERVIEW
  MO-IV-0001  Design the MLOps stack for a 5-person ML team deploying 10
              models: tools, gates, and what you'd skip first.
  MO-IV-0002  How do you test a data pipeline that feeds training
              (schema, ranges, drift, leakage)?
  MO-IV-0003  What is your definition of 'done' for a production ML
              system? Walk through all the layers.
PROJECTS
  MO-PROJ-0001  [C] Experiment tracking + model registry for one
                training pipeline.
  MO-PROJ-0002  [B] Training CI + deployment pipeline with shadow/canary
                rollout.
  MO-PROJ-0003  [A] Full MLOps platform: tracking, registry, drift
                monitoring, retraining triggers, dashboards.
RESEARCH
  MO-RES-0001  Design a study: how much does data versioning cost vs the
              cost of a silent data change? Quantify with a simulated
              incident.
  MO-RES-0002  Compare registry-based gating vs human review for model
              promotion: where do gates miss?

================================================================================
DOMAIN 34 - PRODUCTION AI ENGINEERING
================================================================================
THEORY / CONCEPTUAL
  PA-TH-0001  FastAPI vs Flask vs Django for an AI service: async,
              validation, docs - what matters for inference endpoints?
  PA-TH-0002  What is the difference between batch inference and online
              inference? When do you need streaming?
  PA-TH-0003  What is a rate limit vs a quota? Why both (protect the
              service vs meter the user)?
  PA-TH-0004  What does an API gateway do beyond routing (auth, rate
              limits, logging, canary)?
  PA-TH-0005  Why is idempotency important for retries, and how do you
              design an idempotent inference request?
  PA-TH-0006  What is a background worker / queue (Redis, Celery-style)?
              When is a queue the right answer for AI workloads?
  PA-TH-0007  What is graceful degradation? How should a model service
              behave when the LLM provider is down?
  PA-TH-0008  Why is a circuit breaker better than naive retries for a
              failing dependency?
WHY QUESTIONS
  PA-TH-0009  Why do you need a model server separate from the web app
              (scaling, GPU allocation, model reload)?
  PA-TH-0010  Why is GPU utilization low with single requests, and what
              does request batching at the server do?
  PA-TH-0011  Why must preprocessing live inside the served artifact
              (not in the notebook)?
  PA-TH-0012  Why does an inference service need health/ready probes
              that actually load the model?
CODING
  PA-CODE-0001  [L3] Build a FastAPI model endpoint: validation, error
              codes, logging, and a health check that loads the model.
  PA-CODE-0002  [L4] Add rate limiting + auth (API keys) and test with
              TestClient (401/429 paths).
  PA-CODE-0003  [L5] Add retries with backoff + circuit breaker around
              the model call.
  PA-CODE-0004  [L5] Build a batch inference worker: read a queue,
              predict, write results, with failure isolation.
  PA-CODE-0005  [L6] Implement request batching at the server: queue
              requests, batch by timeout, return per-request results.
  PA-CODE-0006  [L6] Add an idempotency layer (request hash -> cached
              response) with correct semantics.
DEBUGGING
  PA-DEBUG-0001  Latency spikes at 100 QPS. Where do you look (queue,
              GPU contention, garbage collection, DB)?
  PA-DEBUG-0002  The endpoint returns 500 randomly - what is the error
              taxonomy you need in logs to find it?
  PA-DEBUG-0003  Two replicas give different predictions for the same
              input. What state is not shared (model version, cache)?
  PA-DEBUG-0004  Requests hang forever under load. What timeout is
              missing at which layer?
CODE REVIEW
  PA-REVIEW-0001  Review: loading the model inside the request handler.
  PA-REVIEW-0002  Review: no timeouts on the upstream provider call.
  PA-REVIEW-0003  Review: unlimited retries on a 429.
  PA-REVIEW-0004  Review: full PII logged with every request.
INTERVIEW
  PA-IV-0001  Design an LLM inference service for 1000 QPS: serving,
              batching, caching, failover, cost.
  PA-IV-0002  How do you deploy a GPU model with zero downtime? Walk
              through the release process.
  PA-IV-0003  What happens to in-flight requests during a deploy, and
              how do you handle it?
PROJECTS
  PA-PROJ-0001  [C] Model serving API with auth, rate limits, and
                health checks (like rag_agent_server.py).
  PA-PROJ-0002  [B] Batch inference pipeline with queue + retries +
                failure reports.
  PA-PROJ-0003  [A] High-throughput service: server-side batching,
                caching, load testing, autoscaling config.
  PA-PROJ-0004  [S] Multi-model gateway: routing, quotas, observability,
                cost metering per tenant.
RESEARCH
  PA-RES-0001  Design a load test: find the batching timeout that
              maximizes throughput at p99 < 500ms.
  PA-RES-0002  Measure the latency/cost of server-side batching vs
              client batching on a real model.

================================================================================
DOMAIN 35 - CLOUD AI
================================================================================
THEORY / CONCEPTUAL
  CL-TH-0001  What is an instance vs a container vs serverless? Where
              does each fit for AI workloads (GPU persistence)?
  CL-TH-0002  Why are GPUs provisioned as instances/accelerators rather
              than serverless functions? What breaks on cold start?
  CL-TH-0003  Object storage vs block storage vs a database: where does
              each artifact class (weights, data, metadata) live?
  CL-TH-0004  What is a managed model endpoint vs self-hosting? When is
              each the right call?
  CL-TH-0005  What are the three pillars of cloud cost for AI (compute,
              storage, egress) and which one surprises teams?
WHY QUESTIONS
  CL-TH-0006  Why is GPU spot cheaper and what workloads tolerate
              interruption (batch, not online)?
  CL-TH-0007  Why does egress cost more than ingress, and how does that
              change where you put training vs serving?
  CL-TH-0008  Why is 'lift and shift' the wrong first move for ML
              workloads, and what is right-sized first?
CODING
  CL-CODE-0001  [L4] Write a Dockerfile for a GPU model server:
              correct base image, CUDA layers, non-root user.
  CL-CODE-0002  [L5] Write a deployment manifest (compose/k8s-style)
              with resource limits, probes, and autoscaling rules.
  CL-CODE-0003  [L6] Build a cost-report script: parse usage, attribute
              per service/tenant, alert on anomalies.
DEBUGGING
  CL-DEBUG-0001  The container OOMs on startup even though the model fits
              in RAM. What limit is set wrong?
  CL-DEBUG-0002  Training on spot instances dies randomly. What
              checkpoint frequency and resume logic fixes it?
  CL-DEBUG-0003  The bill doubled with no traffic change. What billing
              dimension (storage growth, egress, idle GPUs) do you check
              first?
INTERVIEW
  CL-IV-0001  Design the cloud architecture for a training + serving
              workload: regions, storage, compute, and cost controls.
  CL-IV-0002  On-prem vs cloud for GPU training: what is the break-even
              analysis?
PROJECTS
  CL-PROJ-0001  [C] Dockerize a model server and verify it runs
                identically locally.
  CL-PROJ-0002  [B] Deploy to a managed endpoint with autoscaling and
                load testing.
  CL-PROJ-0003  [A] Multi-region failover + cost dashboard for a serving
                platform.
RESEARCH
  CL-RES-0001  Design a study: spot-vs-on-demand for your training job -
              measure the effective cost per successful run.
  CL-RES-0002  Measure the cost of cold starts vs keeping a warm pool:
              where is the break-even?
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 36 - AI SECURITY
================================================================================
THEORY / CONCEPTUAL
  SC-TH-0001  What is prompt injection (direct vs indirect)? Why is it
              fundamentally different from SQL injection (no parser
              boundary)?
  SC-TH-0002  What is a jailbreak? How do encoding tricks, roleplay, and
              few-shot attacks bypass alignment?
  SC-TH-0003  What is data poisoning? How could poisoned training data
              create a backdoor that evals miss?
  SC-TH-0004  What is model extraction? What does stealing logits/API
              responses enable, and what limits it?
  SC-TH-0005  What is membership inference? What does a model leak about
              its training data, and how do DP-style defenses help?
  SC-TH-0006  What is RAG poisoning? Why is retrieved content a
              privileged injection channel?
  SC-TH-0007  What is excessive agency in an agent, and how do
              permission scoping + human approval reduce blast radius?
  SC-TH-0008  What is tenant isolation in a multi-tenant AI service?
              Where can data leak between tenants (index, cache,
              prompts)?
  SC-TH-0009  What is secret leakage? Where do API keys/tokens sneak
              into prompts, logs, and artifacts?
WHY QUESTIONS
  SC-TH-0010  Why can't you fully defend against indirect prompt
              injection with prompting alone? What controls remain?
  SC-TH-0011  Why is output validation as important as input validation
              for LLM systems (the model generates the attack)?
  SC-TH-0012  Why does a sandboxed tool executor matter even when the
              model is 'aligned'?
  SC-TH-0013  Why is rate limiting also a security control (cost
              abuse, DoS), not just a quota?
CODING
  SC-CODE-0001  [L4] Build an injection test suite: attack prompts
              against your system prompt, with expected/blocked lists.
  SC-CODE-0002  [L4] Implement a sandboxed tool executor: allowlist,
              argument validation, timeout, no shell escapes.
  SC-CODE-0003  [L5] Implement output sanitization: strip instructions
              from tool outputs before they enter the prompt.
  SC-CODE-0004  [L5] Build a tenant-isolation test: tenant A's docs must
              never appear in tenant B's retrieval results.
  SC-CODE-0005  [L6] Implement a secret scanner over prompts, logs, and
              artifacts.
DEBUGGING
  SC-DEBUG-0001  A user pastes 'ignore previous instructions...' and
              gets admin behavior. How do you reproduce, contain, and
              fix?
  SC-DEBUG-0002  Retrieved docs contain instructions the model follows.
              What defense layers do you add, and how do you verify?
  SC-DEBUG-0003  Tenant B retrieves tenant A's documents only at 3 AM.
              What cache/index isolation bug could be time-dependent?
  SC-DEBUG-0004  A 'safe' calculator tool executes arbitrary Python. How
              do you find the escape and harden it?
  SC-DEBUG-0005  Your eval shows no jailbreaks, but red team finds one
              in production. What is the eval's blind spot (adversarial
              coverage)?
INTERVIEW
  SC-IV-0001  Design the security architecture for an AI assistant with
              tool access: threat model, controls, and testing.
  SC-IV-0002  What is your red-team playbook for an LLM system? Walk
              through the categories of attacks you test.
  SC-IV-0003  How do you balance security and UX (approvals, rate
              limits, sandboxing) without destroying the product?
PROJECTS
  SC-PROJ-0001  [C] Injection defense lab: sanitize, delimit, and test
                against an attack set.
  SC-PROJ-0002  [B] Sandboxed tool platform with permission policy and
                audit logging.
  SC-PROJ-0003  [A] Security eval harness: automated red team, drift
                detection of attack success, regression gates.
  SC-PROJ-0004  [S] Multi-tenant secure AI platform: isolation proofs,
                secrets management, compliance logging.
RESEARCH
  SC-RES-0001  Design a study: how much do delimiters + input
              classification reduce indirect injection success?
  SC-RES-0002  Measure membership-inference risk vs model size on your
              own fine-tune - quantify the leak.

================================================================================
DOMAIN 37 - AI EVALUATION
================================================================================
THEORY / CONCEPTUAL
  AE-TH-0001  Offline vs online evaluation: what does each measure, and
              why do you need both?
  AE-TH-0002  What is a benchmark, and what makes a good one (coverage,
              difficulty, contamination-free)?
  AE-TH-0003  What is LLM-as-judge? What are its biases (position,
              verbosity, self-preference) and how do you control them?
  AE-TH-0004  What is statistical significance for ML results? Why is
              'model B is 0.5% better' meaningless without variance?
  AE-TH-0005  What is regression testing for models? What does 'the
              prompt change didn't break anything' require?
  AE-TH-0006  What is safety/adversarial evaluation? How is it different
              from capability eval?
WHY QUESTIONS
  AE-TH-0007  Why is a single aggregated number (accuracy) hiding the
              story, and what do error-bucketed reports add?
  AE-TH-0008  Why do benchmark scores saturate and stop discriminating,
              and what do you do about it?
  AE-TH-0009  Why is human evaluation still necessary despite judges and
              benchmarks? Where does it uniquely matter?
  AE-TH-0010  Why is contamination (eval data in training) a silent
              threat, and how do you audit for it?
CODING
  AE-CODE-0001  [L3] Build a benchmark runner: dataset, model, metrics,
              and a reproducible report.
  AE-CODE-0002  [L4] Implement a paired significance test (bootstrap or
              McNemar) for two models' scores.
  AE-CODE-0003  [L5] Build an LLM-judge with position-swap control and
              agreement stats vs human labels.
  AE-CODE-0004  [L6] Build a contamination checker: n-gram overlap
              between eval and training corpora.
DEBUGGING
  AE-DEBUG-0001  A prompt change 'improves' eval by 3% but worsens user
              satisfaction. What is the eval set missing?
  AE-DEBUG-0002  Two runs of the same eval give different scores. What
              nondeterminism (order, temperature, judge) - and how do you
              stabilize?
  AE-DEBUG-0003  The judge gives every answer 5/5. What degenerate
              pattern is happening and how do you detect it?
  AE-DEBUG-0004  Your eval set leaked into training data. What evidence
              (suspiciously high scores) and what fix?
INTERVIEW
  AE-IV-0001  How do you decide whether a new model is better than the
              current one? Walk through the full protocol.
  AE-IV-0002  What would you do if your eval can't distinguish two
              models at all (no signal)?
  AE-IV-0003  How do you evaluate an agent that takes many steps with
              stochastic behavior?
PROJECTS
  AE-PROJ-0001  [C] Eval harness for a model family with paired
                significance testing.
  AE-PROJ-0002  [B] Judge calibration: build, validate, and version an
                LLM judge for your domain.
  AE-PROJ-0003  [A] Continuous eval platform: regression gates on every
                change, contamination audit, dashboard.
RESEARCH
  AE-RES-0001  Design a study: judge agreement vs human variance - when
              is a judge 'good enough'?
  AE-RES-0002  Measure the contamination sensitivity: inject k% of eval
              into training, plot the score inflation curve.

================================================================================
DOMAIN 38 - AI OBSERVABILITY
================================================================================
THEORY / CONCEPTUAL
  OB-TH-0001  Logs vs metrics vs traces: what does each capture, and
              which one finds a hallucination?
  OB-TH-0002  What is an LLM span? What must it record (prompt,
              response, tokens, latency, model version)?
  OB-TH-0003  What is token accounting, and why is cost a first-class
              metric for LLM systems?
  OB-TH-0004  What is drift detection (data vs concept vs prediction)?
              What statistical tests are used (PSI, JS divergence)?
  OB-TH-0005  What are p50/p95/p99 latencies, and why does p99 matter
              for interactive AI UX?
  OB-TH-0006  What is an alert vs a dashboard? What makes an alert
              actionable (threshold, owner, runbook)?
WHY QUESTIONS
  OB-TH-0007  Why is observability the prerequisite for debugging
              agents (which tool, which step, what did the model see)?
  OB-TH-0008  Why is logging the exact prompt/response necessary for
              compliance and debugging - and what is the PII trade-off?
  OB-TH-0009  Why does 'it was slow' need traces, not just averages?
              What does a latency histogram show that mean doesn't?
  OB-TH-0010  Why is drift detection about action (retrain, alert,
              fallback), not just reporting?
CODING
  OB-CODE-0001  [L3] Add structured logging to a model call: latency,
              tokens, cost, model version, request id.
  OB-CODE-0002  [L4] Instrument a RAG pipeline with spans: retrieval
              latency, top-k scores, generation tokens.
  OB-CODE-0003  [L5] Implement JS-divergence drift detection over query
              topic distributions with alerting (like monitoring_lab.py).
  OB-CODE-0004  [L5] Build a cost dashboard: per-endpoint, per-tenant,
              per-day token/cost rollups with anomaly alerts.
  OB-CODE-0005  [L6] Build a trace viewer: capture spans, correlate
              request ids, visualize the agent's tool steps.
DEBUGGING
  OB-DEBUG-0001  p95 latency spiked but the mean is fine. What
              histogram shape indicates, and what subsystem is likely?
  OB-DEBUG-0002  Drift alerts fire constantly with no real change. What
              calibration (baseline window, threshold) is wrong?
  OB-DEBUG-0003  An agent failed and you have no idea what it saw.
              What telemetry gap exists, and how do you backfill it?
  OB-DEBUG-0004  Cost alert fired, but attribution is impossible.
              What per-request dimension is missing from the logs?
INTERVIEW
  OB-IV-0001  Design the observability stack for an LLM product:
              metrics, traces, cost, drift, and the alerting rules.
  OB-IV-0002  What do you monitor for a RAG system specifically, and
              what does each signal tell you?
  OB-IV-0003  Your model quality degraded silently for a month. What
              monitoring would have caught it, and what alerts now?
PROJECTS
  OB-PROJ-0001  [C] Structured logging + metrics for a model endpoint.
  OB-PROJ-0002  [B] RAG/agent tracing with a simple trace UI and
                request-correlated logs.
  OB-PROJ-0003  [A] Drift + cost monitoring platform with alerts,
                dashboards, and runbooks.
  OB-PROJ-0004  [S] Full observability: traces + metrics + drift + cost
                per tenant with alerting SLAs.
RESEARCH
  OB-RES-0001  Design a study: which drift test (PSI vs JS vs KS)
              detects real degradation earliest with fewest false alarms?
  OB-RES-0002  Measure the signal value of retrieval scores: does a
              score drop precede answer-quality drops?
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 39 - AI RESEARCH
================================================================================
THEORY / CONCEPTUAL
  RS-TH-0001  How do you read a paper efficiently (abstract -> claims ->
              method -> ablations -> limitations)? What is a 'result' vs
              an 'artifact'?
  RS-TH-0002  What is a research gap, and how do you find one (related-
              work matrix, baseline failures, unreproduced claims)?
  RS-TH-0003  What is an ablation? Why must you ablate one variable at a
              time, and what does a negative result prove?
  RS-TH-0004  What is reproducibility in ML (code, data, seeds, compute,
              metric definitions)? What is the minimum bar?
  RS-TH-0005  What is a baseline, and why is a strong baseline the most
              important experiment in a paper?
  RS-TH-0006  What is statistical validation in research (CIs, multiple
              comparisons, seed variance)?
WHY QUESTIONS
  RS-TH-0007  Why do 'improvements' often fail to replicate? Rank the
              causes (variance, tuning on test, weak baselines, data
              differences).
  RS-TH-0008  Why is a negative result valuable, and why is it hard to
              publish? What does it teach?
  RS-TH-0009  Why must you define the metric BEFORE running the
              experiment (garden of forking paths)?
  RS-TH-0010  Why does compute budget belong in a comparison (matched
              compute vs matched params)?
CODING
  RS-CODE-0001  [L5] Reproduce a small paper's headline result from its
              code/data and write what differed.
  RS-CODE-0002  [L6] Build an ablation harness: every experiment is a
              config diff from the baseline, logged and comparable.
  RS-CODE-0003  [L7] Implement a baseline properly (tuned, not default)
              and show the delta your method adds.
  RS-CODE-0004  [L7] Build a seed-sweep runner: N seeds per config,
              report mean/std and significance.
DEBUGGING
  RS-DEBUG-0001  Your reproduction gets 5 points lower than the paper.
              Ordered checklist (eval details, preprocessing, LR, seeds).
  RS-DEBUG-0002  An ablation shows your trick does nothing. Before
              abandoning, what confounds do you check (interaction,
              implementation bug, capacity)?
  RS-DEBUG-0003  Two researchers get different numbers from the same
              code. What is the reproducibility checklist?
INTERVIEW
  RS-IV-0001  Walk through how you'd design a research project from a
              gap to a paper: steps and failure checkpoints.
  RS-IV-0002  What makes a result 'publishable'? Distinguish novelty,
              rigor, and impact.
  RS-IV-0003  How do you decide between many small ablations and one
              comprehensive experiment with limited compute?
PROJECTS
  RS-PROJ-0001  [B] Reproduce one published result end-to-end.
  RS-PROJ-0002  [A] Run a rigorous ablation study on an existing method
                with a baseline + seed variance + report.
  RS-PROJ-0003  [S] A small novel contribution: hypothesis, method,
                controlled experiments, limitations, write-up.
RESEARCH
  RS-RES-0001  Design a meta-study: how much do seeds matter in your
              domain? Quantify seed variance vs method delta.
  RS-RES-0002  Design a baseline-quality study: how often does a tuned
              baseline beat the 'novel' method? Test on 3 tasks.

================================================================================
DOMAIN 40 - AI SYSTEM DESIGN
================================================================================
THEORY / CONCEPTUAL
  SD-TH-0001  What is the system-design interview shape (requirements ->
              scale -> architecture -> data -> models -> infra)?
  SD-TH-0002  What does 'design for 10x' mean? Where does the
              architecture break first (DB, GPU, retrieval, cost)?
  SD-TH-0003  What is the difference between latency, throughput, and
              concurrency, and how do they trade off in AI systems?
  SD-TH-0004  What is a cache hierarchy for AI (response cache, KV
              prefix cache, embedding cache)? What invalidates each?
  SD-TH-0005  What is the retrieval funnel (recall -> rerank -> context)?
              Why is a funnel cheaper than top-k-from-one-index?
  SD-TH-0006  What does multi-tenancy require (isolation, quotas,
              billing, audit)? Where do tenants share infra safely?
WHY QUESTIONS
  SD-TH-0007  Why is 'just call the LLM' not a system design? What are
              the non-model components (routing, cache, guardrails, eval)?
  SD-TH-0008  Why does cost belong in system design (GPU hours, tokens,
              storage)? Where does naive design double it?
  SD-TH-0009  Why is a queue better than synchronous fan-out for batch
              AI workloads, and worse for interactive ones?
  SD-TH-0010  Why must you design failure modes explicitly (provider
              down, model drift, cache poisoning)?
SYSTEM DESIGN PROMPTS (walk through: requirements, scale, architecture,
  data, models, storage, caching, APIs, queues, security, observability,
  eval, latency, throughput, cost, reliability, deployment)
  SD-SYS-0001  Design an enterprise RAG platform for 10M documents with
              multi-tenant isolation.
  SD-SYS-0002  Design an LLM inference platform serving 1M requests/day
              with cost controls.
  SD-SYS-0003  Design a multi-agent research system with budgets and
              failure isolation.
  SD-SYS-0004  Design a real-time fraud detection system (streaming
              features, low latency, explainability).
  SD-SYS-0005  Design a recommendation system for 100M users with
              freshness and cold start.
  SD-SYS-0006  Design an AI coding assistant with sandboxed execution.
  SD-SYS-0007  Design a multimodal document-intelligence platform
              (OCR, layout, retrieval, QA).
  SD-SYS-0008  Design an enterprise AI assistant with strict data
              isolation and compliance logging.
  SD-SYS-0009  Design a model monitoring + retraining system that
              detects drift and re-deploys safely.
  SD-SYS-0010  Design a real-time translation/summarization service
              with streaming output at p99 < 500ms.
CODING
  SD-CODE-0001  [L5] Build a capacity model: given QPS, tokens/request,
              and GPU specs, compute required replicas.
  SD-CODE-0002  [L6] Build a cost model for a RAG platform: ingest vs
              query costs, cache hit assumptions, break-even.
  SD-CODE-0003  [L7] Simulate a load model: queue depth, latency
              percentiles vs arrival rate (M/M/1 style or discrete
              event).
DEBUGGING
  SD-DEBUG-0001  The system is 99.9% available but users perceive
              failure. What is the actual contract (p99, timeouts)?
  SD-DEBUG-0002  A single tenant's traffic degrades everyone. What
              isolation gap (shared cache, shared GPU, shared queue)?
  SD-DEBUG-0003  Adding a cache improved p50 but hurt p99. What cache
              behavior (thundering herd, stale blocking) explains it?
INTERVIEW
  SD-IV-0001  Design a chatbot for a bank with 5M users: end-to-end,
              with security and compliance.
  SD-IV-0002  Your RAG platform must scale from 10k to 10M docs. Where
              do you invest first and why?
  SD-IV-0003  Estimate the cost of serving 1M conversations/month.
              Show the token math.
PROJECTS
  SD-PROJ-0001  [B] Design doc + capacity model for an LLM API product.
  SD-PROJ-0002  [A] Build a load-tested RAG service with caching,
                autoscaling config, and cost dashboard.
  SD-PROJ-0003  [S] Design + implement a multi-tenant AI platform with
                quotas, isolation tests, and observability.
RESEARCH
  SD-RES-0001  Design a study: cache hit rate vs staleness tolerance -
              where does caching break correctness?
  SD-RES-0002  Model the cost of 99.9% vs 99.99% availability for a GPU
              service - what does the extra 9 cost?
DOMAIN MASTERY CHECKLIST: [ ] Theory [ ] Math [ ] Coding [ ] Debugging
                          [ ] Interview [ ] Projects [ ] Research

================================================================================
DOMAIN 41 - PRODUCTION INCIDENTS
================================================================================
Each incident: describe causes (ranked), investigation steps, fixes,
monitoring improvements. This section is the interview 'have you ever
been paged' equivalent.
  INC-0001  Production model latency jumped 200ms -> 2.5s overnight.
  INC-0002  GPU memory grows after every request until OOM.
  INC-0003  A model was retrained on drifted data and silently degraded
           quality for 2 weeks.
  INC-0004  A RAG system started citing fabricated sources after an
           index rebuild.
  INC-0005  An agent entered an infinite tool loop, running up a $3k
           bill in one night.
  INC-0006  A prompt change improved eval by 5% but tanked user
           satisfaction (measured by support tickets).
  INC-0007  Tenant A's data appeared in tenant B's search results.
  INC-0008  The vector index became stale after a doc deletion wave -
           answers referenced deleted documents.
  INC-0009  Quantized model passed accuracy gates but crashed on a
           specific input (NaN logits).
  INC-0010  A fine-tuned model forgot its base knowledge; customer
           tickets about general topics exploded.
  INC-0011  LLM provider returned 429s; retries hammered the provider
           and amplified the outage.
  INC-0012  Training job was 'reproducible' but two runs gave different
           leaderboard scores.

================================================================================
DOMAIN 42 - MASTER INTERVIEW (500)
================================================================================
The full 500-question Q&A bank lives in ml_course/INTERVIEW_BANK.md
(B100 + I150 + A150 + X100, all with answers). This section adds the
cross-domain 500 with IDs; answers are expected in the learner's own
words - use the interview technique: answer, then follow-up.
  MI-0001  What is the difference between AI, ML, and DL? Give a real
          system for each.
  MI-0002  Walk through the full ML lifecycle for one product.
  MI-0003  What is the bias-variance trade-off in one sentence, then
          with the decomposition formula?
  MI-0004  When is a simple linear model better than XGBoost?
  MI-0005  How do you detect data leakage in someone else's notebook?
  MI-0006  Why is accuracy the wrong metric for fraud detection?
  MI-0007  What is the kernel trick, and when does RBF fail?
  MI-0008  Why does the normal equation fail for 1M features?
  MI-0009  How does gradient boosting work, and what is it fitting?
  MI-0010  Why is LightGBM faster than XGBoost on large data?
  MI-0011  What is the difference between bagging and boosting?
  MI-0012  How does PCA work, and when is it the wrong tool?
  MI-0013  What is the difference between k-means and DBSCAN?
  MI-0014  How does backpropagation work? Derive it for one layer.
  MI-0015  Why did ReLU replace sigmoid in hidden layers?
  MI-0016  What is vanishing gradient, and what are the fixes?
  MI-0017  Why does BatchNorm fail on transformers, and what replaces it?
  MI-0018  What does AdamW change vs Adam, and why?
  MI-0019  How do you debug a NaN loss?
  MI-0020  What is the difference between an RNN and a transformer?
  MI-0021  Explain self-attention with tensor shapes.
  MI-0022  Why is attention O(T^2)? How do GQA/FlashAttention help?
  MI-0023  What is RoPE and why does it matter for long context?
  MI-0024  Why is the decoder causal? What would change without masking?
  MI-0025  How does an LLM generate text? Walk through one token.
  MI-0026  What is temperature, top-k, and top-p? Give the math.
  MI-0027  What is perplexity, and what does it NOT measure?
  MI-0028  How does tokenization affect cost and quality?
  MI-0029  What is the difference between pretraining, SFT, and RLHF/DPO?
  MI-0030  What is LoRA, and why does it save memory?
  MI-0031  What is QLoRA? What is NF4?
  MI-0032  What is quantization, and why is 4-bit harder than 8-bit?
  MI-0033  What is the KV cache, and why is decode bandwidth-bound?
  MI-0034  What is speculative decoding, and why can it be lossless?
  MI-0035  What is prefill vs decode, and why do they differ?
  MI-0036  What is continuous batching?
  MI-0037  What is RAG? Draw the full pipeline.
  MI-0038  Why does RAG reduce hallucination - and why can it still
          hallucinate?
  MI-0039  How do you chunk documents well? What does chunk size change?
  MI-0040  What is hybrid retrieval, and why is it better than either
          alone?
  MI-0041  What is reranking, and why a two-stage design?
  MI-0042  What is BM25 vs dense retrieval? When does each win?
  MI-0043  How does a vector database work under the hood (HNSW)?
  MI-0044  What is a cross-encoder vs bi-encoder?
  MI-0045  How do you evaluate a RAG system end to end?
  MI-0046  What is an LLM-as-judge, and what are its biases?
  MI-0047  What is LangChain's Runnable abstraction and why does it
          matter?
  MI-0048  What is LangGraph, and when do you need it over a chain?
  MI-0049  What is a reducer in LangGraph state?
  MI-0050  What is human-in-the-loop, and how do interrupts work?
  MI-0051  What makes an LLM with tools an 'agent'?
  MI-0052  What is ReAct? When does it help?
  MI-0053  How do you prevent an agent loop from running forever?
  MI-0054  What is excessive agency, and how do you scope it?
  MI-0055  What is indirect prompt injection via tool output?
  MI-0056  When does multi-agent beat a single agent?
  MI-0057  What is a supervisor pattern, and its failure mode?
  MI-0058  How do you evaluate an agent (success, cost, steps)?
  MI-0059  What is the difference between fine-tuning and RAG? When
          each?
  MI-0060  How do you reduce LLM cost without reducing quality?
  MI-0061  What is model drift vs data drift? How do you detect each?
  MI-0062  What is a model registry and what gates promotion?
  MI-0063  What is train/serve skew? How do you prevent it?
  MI-0064  How do you serve a model with zero downtime?
  MI-0065  What is the difference between batch and online inference?
  MI-0066  Why is p99 more important than average latency?
  MI-0067  How do you design idempotent retries for an LLM API?
  MI-0068  What is a circuit breaker, and when does it beat retries?
  MI-0069  How do you do A/B testing on a model without leaking?
  MI-0070  What is statistical significance for model comparison?
  MI-0071  How do you detect benchmark contamination?
  MI-0072  What is a feature store, and what problem does it solve?
  MI-0073  What is the cold-start problem in recommenders?
  MI-0074  What is NDCG, and why do recommenders use it?
  MI-0075  How does matrix factorization work?
  MI-0076  What is walk-forward validation, and why random CV fails for
          time series?
  MI-0077  What is point-in-time correctness in feature engineering?
  MI-0078  How do you handle imbalanced data beyond resampling?
  MI-0079  What is calibration, and how do you fix miscalibration?
  MI-0080  What is SHAP vs LIME? When is each misleading?
  MI-0081  What is target leakage in feature engineering?
  MI-0082  How do you choose between many models with limited compute?
  MI-0083  What is the curse of dimensionality, concretely?
  MI-0084  How does the GIL affect ML workloads, and what do you do?
  MI-0085  What is the difference between threads, asyncio, and
          multiprocessing for an inference service?
  MI-0086  How do you profile a slow training loop in 5 minutes?
  MI-0087  What is mixed precision, and when does it hurt?
  MI-0088  What is gradient accumulation, and what does it change?
  MI-0089  How does DDP differ from FSDP? When is each right?
  MI-0090  What is a checkpoint, and what must it contain to resume?
  MI-0091  Why does the model behave differently at train vs eval time?
  MI-0092  What is a DataLoader worker, and what breaks with 8 workers?
  MI-0093  How do you make training deterministic?
  MI-0094  What is the difference between a tensor view and a copy?
  MI-0095  Why does autograd need retain_graph sometimes?
  MI-0096  What is a soft prompt? What does it change?
  MI-0097  What is context distillation vs fine-tuning?
  MI-0098  How does DPO differ from PPO-based RLHF?
  MI-0099  What is the scaling law, and what does it not predict?
  MI-0100  You have 5 minutes with a CEO. Explain why 'just prompt it'
          is not a system.
(Continue the pattern to 500: reuse the per-domain question sets in this
bank + ml_course/INTERVIEW_BANK.md for the remaining 400.)

================================================================================
DOMAIN 43 - RAPID FIRE (10-60 SECOND ANSWERS)
================================================================================
  RF-0001  What is bias?   RF-0002  What is variance?
  RF-0003  What is regularization?   RF-0004  What is gradient descent?
  RF-0005  What is overfitting?   RF-0006  What is a confusion matrix?
  RF-0007  What is precision?   RF-0008  What is recall?
  RF-0009  What is F1?   RF-0010  What is AUC?
  RF-0011  What is a feature?   RF-0012  What is an embedding?
  RF-0013  What is attention?   RF-0014  What is a transformer?
  RF-0015  What is a token?   RF-0016  What is a language model?
  RF-0017  What is logits?   RF-0018  What is softmax?
  RF-0019  What is temperature?   RF-0020  What is top-p?
  RF-0021  What is perplexity?   RF-0022  What is a KV cache?
  RF-0023  What is prefill?   RF-0024  What is decode?
  RF-0025  What is quantization?   RF-0026  What is LoRA?
  RF-0027  What is QLoRA?   RF-0028  What is an adapter?
  RF-0029  What is a checkpoint?   RF-0030  What is RAG?
  RF-0031  What is a retriever?   RF-0032  What is a reranker?
  RF-0033  What is a chunk?   RF-0034  What is a vector database?
  RF-0035  What is HNSW?   RF-0036  What is BM25?
  RF-0037  What is a hybrid search?   RF-0038  What is a cross-encoder?
  RF-0039  What is a bi-encoder?   RF-0040  What is groundedness?
  RF-0041  What is faithfulness?   RF-0042  What is a tool call?
  RF-0043  What is an agent?   RF-0044  What is agent state?
  RF-0045  What is a supervisor?   RF-0046  What is a handoff?
  RF-0047  What is a reducer?   RF-0048  What is a checkpoint (graph)?
  RF-0049  What is human-in-the-loop?   RF-0050  What is a graph node?
  RF-0051  What is prompt injection?   RF-0052  What is a jailbreak?
  RF-0053  What is a sandbox?   RF-0054  What is a rate limit?
  RF-0055  What is a quota?   RF-0056  What is drift?
  RF-0057  What is a baseline?   RF-0058  What is an ablation?
  RF-0059  What is an eval set?   RF-0060  What is contamination?
  RF-0061  What is an LLM judge?   RF-0062  What is a span (tracing)?
  RF-0063  What is token accounting?   RF-0064  What is TTFT?
  RF-0065  What is TPOT?   RF-0066  What is throughput vs latency?
  RF-0067  What is a circuit breaker?   RF-0068  What is idempotency?
  RF-0069  What is a warm pool?   RF-0070  What is a canary deploy?
  RF-0071  What is a shadow deploy?   RF-0072  What is a rollback?
  RF-0073  What is a feature store?   RF-0074  What is train/serve skew?
  RF-0075  What is a model card?   RF-0076  What is an SLA?
  RF-0077  What is p99?   RF-0078  What is autoscaling?
  RF-0079  What is spot instance?   RF-0080  What is egress cost?
  RF-0081  What is a DAG?   RF-0082  What is map-reduce (agents)?
  RF-0083  What is a subgraph?   RF-0084  What is durable execution?
  RF-0085  What is reflection?   RF-0086  What is ReAct?
  RF-0087  What is CoT?   RF-0088  What is test-time compute?
  RF-0089  What is a draft model?   RF-0090  What is acceptance rate?
  RF-0091  What is prefix caching?   RF-0092  What is paged attention?
  RF-0093  What is GQA?   RF-0094  What is MQA?
  RF-0095  What is SwiGLU?   RF-0096  What is RMSNorm?
  RF-0097  What is RoPE?   RF-0098  What is ALiBi?
  RF-0099  What is MoE?   RF-0100  What is a router (MoE)?
(Continue the pattern to 500 across every domain above.)

================================================================================
DOMAIN 44 - WHITEBOARD
================================================================================
  WB-0001  Draw a 2-layer neural network and derive the backward pass.
  WB-0002  Draw the transformer block and label every tensor shape.
  WB-0003  Draw the RAG pipeline with chunking and reranking stages.
  WB-0004  Draw the agent loop with termination conditions.
  WB-0005  Draw a LangGraph supervisor multi-agent diagram.
  WB-0006  Draw and derive gradient descent on a loss surface.
  WB-0007  Draw the KV cache: what is stored and why it grows.
  WB-0008  Draw an LLM serving architecture with batching and caching.
  WB-0009  Draw the bias-variance trade-off with model complexity axis.
  WB-0010  Draw a recommendation two-stage architecture (candidate +
          rank) with data flow.

================================================================================
DOMAIN 45 - MASTER CAPSTONE
================================================================================
  CP-0001  Design a production-grade autonomous enterprise AI knowledge
          platform: LLM + hybrid RAG + vector DB + PostgreSQL + Redis +
          LangChain + LangGraph + multi-agent + FastAPI + auth + Docker +
          monitoring + eval + security + cost control + human approval.
  CP-0002  Design the eval system that gates every prompt/model/agent
          change on that platform.
  CP-0003  Design the cost-control subsystem (budgets per agent per
          tenant, alerts, routing).
  CP-0004  Design the security layer: tenant isolation, injection
          defenses, sandboxed tools, audit logs.
  CP-0005  Design the observability layer: traces per agent step,
          token/cost metrics, drift detection, dashboards.
  CP-0006  Design the failure-recovery story: provider outage, node
          crash mid-graph, stale index.
  CP-0007  Design the human-approval workflow for irreversible actions.
  CP-0008  Design the capacity + cost model for 1M users.
  CP-0009  Design the regression-test suite that must pass before any
          deploy.
  CP-0010  Design the multi-region, high-availability deployment.

================================================================================
DOMAIN 46 - FINAL MASTER EXAM (STRUCTURE + SAMPLE)
================================================================================
100 theory + 100 math + 100 coding + 50 debugging + 50 ML + 50 DL +
50 LLM + 50 RAG + 50 agent + 50 system design + 50 production + 50
research questions, mixed and unpredictable. Samples:
  FX-0001  [Theory] 'RAG reduced hallucinations' - define hallucination,
          prove the claim with a mechanism, and give a case where RAG
          INCREASES hallucination.
  FX-0002  [Math] Attention with T=4, d=2: compute scores, mask, softmax,
          output. Show every number.
  FX-0003  [Coding] Implement top-p sampling with a loop-free mask.
  FX-0004  [Debugging] Retrieval hit@k=0.95 but answer quality is low -
          list the instrumentation you add and the expected findings.
  FX-0005  [System design] Design a 10M-doc multi-tenant RAG platform
          with a budget of 1s p95.
  FX-0006  [Research] Propose an experiment that would falsify the claim
          'reflection improves agent task success'.

================================================================================
+47 - DATA LEAKAGE (DEDICATED)
================================================================================
  DLK-0001  Target leakage: a feature that is a function of the label.
          Give 3 realistic examples (churn, fraud, medical).
  DLK-0002  Preprocessing leakage: scaler/encoder fit on full data.
  DLK-0003  Temporal leakage: future events in features at prediction
          time.
  DLK-0004  Duplicate leakage: same entity in train and test.
  DLK-0005  Embedding leakage: embeddings trained on the test set.
  DLK-0006  RAG eval leakage: qrels built from the indexed docs.
  DLK-0007  Benchmark contamination: eval prompts in the training
          corpus.
  DLK-0008  Prompt leakage: reference answers inside judge prompts.
  DLK-0009  SMOTE-before-split leakage.
  DLK-0010  Feature-store leakage: point-in-time violations.
  DLK-0011  How do you AUDIT for leakage systematically? Build the
          checklist.

================================================================================
+48 - FAILURE ANALYSIS (PATTERN: failure / causes / diagnosis / fix /
prevention / monitoring)
================================================================================
  FA-0001  Train 99% / val 61% -> leakage or overfit -> learning curves
          + feature audit -> split fix / regularization -> CI leakage
          check -> monitor gap.
  FA-0002  RAG right doc, wrong section -> chunking -> parent-child
          retrieval -> retrieval tests on section granularity.
  FA-0003  Agent loops forever -> missing termination -> step counter +
          budget -> default max steps -> alert on step spikes.
  FA-0004  Model degrades silently -> drift -> PSI/JS + quality sampling
          -> retraining trigger -> alert on drift + quality floor.
  FA-0005  Quantized model crashes on an input -> outlier activations ->
          per-layer calibration audit -> mixed precision + QAT ->
          canary on hard inputs.
  FA-0006  Costs explode -> unbounded loops/prompts -> token budgets +
          prompt size caps -> per-request cost logging -> alert on
          per-user cost.

================================================================================
QUALITY CONTROL / AUDIT CHECKLIST
================================================================================
[ ] All 46 domains present with theory, math, coding, debugging,
    interview, projects, research where meaningful.
[ ] Python / NumPy / Pandas / Math / Statistics covered.
[ ] ML (all major algorithms) + evaluation + features covered.
[ ] DL / PyTorch / CNN / sequence / NLP / transformers covered.
[ ] LLMs + LLM engineering + fine-tuning + quantization covered.
[ ] Embeddings / IR / RAG / vector DBs / RAG eval covered.
[ ] LangChain / LangGraph / agents / multi-agent covered.
[ ] Multimodal / generative / recommenders / time series covered.
[ ] MLOps / production / cloud / security / evaluation / observability
    covered.
[ ] Research + system design + incidents + interview + rapid fire +
    whiteboard + capstone + final exam + leakage + failure analysis
    included.
[ ] No fabricated framework APIs (LangChain/LangGraph use the modern
    1.x concept set: Runnable, graph State/Node/Edge/reducer).
[ ] Cross-linked: ml_course/INTERVIEW_BANK.md (500 Q&A), the course
    COURSE.txt / PRACTICE.txt / PROJECT.txt files, verify_course.py.

================================================================================
END OF QUESTION BANK - extend any domain by appending new IDs; the
master prompt target is 8,000+ questions and this bank is built to grow
without breaking the ID scheme.
================================================================================























